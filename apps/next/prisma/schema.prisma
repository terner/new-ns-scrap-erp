generator client {
  provider        = "prisma-client"
  output          = "../generated/prisma"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
  schemas  = ["auth", "public"]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model custom_oauth_providers {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider_type         String
  identifier            String    @unique
  name                  String
  client_id             String
  client_secret         String
  acceptable_client_ids String[]  @default([])
  scopes                String[]  @default([])
  pkce_enabled          Boolean   @default(true)
  attribute_mapping     Json      @default("{}")
  authorization_params  Json      @default("{}")
  enabled               Boolean   @default(true)
  email_optional        Boolean   @default(false)
  issuer                String?
  discovery_url         String?
  skip_nonce_check      Boolean   @default(false)
  cached_discovery      Json?
  discovery_cached_at   DateTime? @db.Timestamptz(6)
  authorization_url     String?
  token_url             String?
  userinfo_url          String?
  jwks_uri              String?
  created_at            DateTime  @default(now()) @db.Timestamptz(6)
  updated_at            DateTime  @default(now()) @db.Timestamptz(6)

  @@index([created_at])
  @@index([enabled])
  @@index([identifier])
  @@index([provider_type])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                 @id @db.Uuid
  user_id                String?                @db.Uuid
  auth_code              String?
  code_challenge_method  code_challenge_method?
  code_challenge         String?
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?              @db.Timestamptz(6)
  updated_at             DateTime?              @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?              @db.Timestamptz(6)
  invite_token           String?
  referrer               String?
  oauth_client_state_id  String?                @db.Uuid
  linking_target_id      String?                @db.Uuid
  email_optional         Boolean                @default(false)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String     @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime?  @db.Timestamptz(6)
  created_at      DateTime?  @db.Timestamptz(6)
  updated_at      DateTime?  @db.Timestamptz(6)
  email           String?    @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String           @id @db.Uuid
  user_id                      String           @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime         @db.Timestamptz(6)
  updated_at                   DateTime         @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?        @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?          @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        auth_users       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([friendly_name, user_id], map: "mfa_factors_user_friendly_name_unique", where: raw("(TRIM(BOTH FROM friendly_name) <> ''::text)"))
  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 auth_users?                @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at], map: "oauth_auth_pending_exp_idx", where: raw("(status = 'pending'::auth.oauth_authorization_status)"))
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                         String                  @id @db.Uuid
  client_secret_hash         String?
  registration_type          oauth_registration_type
  redirect_uris              String
  grant_types                String
  client_name                String?
  client_uri                 String?
  logo_uri                   String?
  created_at                 DateTime                @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at                 DateTime?               @db.Timestamptz(6)
  client_type                oauth_client_type       @default(confidential)
  token_endpoint_auth_method String
  oauth_authorizations       oauth_authorizations[]
  oauth_consents             oauth_consents[]
  sessions                   sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         auth_users    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([client_id], map: "oauth_consents_active_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, client_id], map: "oauth_consents_active_user_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  users      auth_users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  auth_users       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([oauth_client_id])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model auth_users {
  instance_id                 String?                @db.Uuid
  id                          String                 @id @db.Uuid
  aud                         String?                @db.VarChar(255)
  role                        String?                @db.VarChar(255)
  email                       String?                @unique(map: "users_email_partial_key", where: raw("(is_sso_user = false)")) @db.VarChar(255)
  encrypted_password          String?                @db.VarChar(255)
  email_confirmed_at          DateTime?              @db.Timestamptz(6)
  invited_at                  DateTime?              @db.Timestamptz(6)
  confirmation_token          String?                @unique(map: "confirmation_token_idx", where: raw("((confirmation_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  confirmation_sent_at        DateTime?              @db.Timestamptz(6)
  recovery_token              String?                @unique(map: "recovery_token_idx", where: raw("((recovery_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  recovery_sent_at            DateTime?              @db.Timestamptz(6)
  email_change_token_new      String?                @unique(map: "email_change_token_new_idx", where: raw("((email_change_token_new)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  email_change                String?                @db.VarChar(255)
  email_change_sent_at        DateTime?              @db.Timestamptz(6)
  last_sign_in_at             DateTime?              @db.Timestamptz(6)
  raw_app_meta_data           Json?
  raw_user_meta_data          Json?
  is_super_admin              Boolean?
  created_at                  DateTime?              @db.Timestamptz(6)
  updated_at                  DateTime?              @db.Timestamptz(6)
  phone                       String?                @unique
  phone_confirmed_at          DateTime?              @db.Timestamptz(6)
  phone_change                String?                @default("")
  phone_change_token          String?                @default("") @db.VarChar(255)
  phone_change_sent_at        DateTime?              @db.Timestamptz(6)
  confirmed_at                DateTime?              @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current  String?                @unique(map: "email_change_token_current_idx", where: raw("((email_change_token_current)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  email_change_confirm_status Int?                   @default(0) @db.SmallInt
  banned_until                DateTime?              @db.Timestamptz(6)
  reauthentication_token      String?                @unique(map: "reauthentication_token_idx", where: raw("((reauthentication_token)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  reauthentication_sent_at    DateTime?              @db.Timestamptz(6)
  is_sso_user                 Boolean                @default(false)
  deleted_at                  DateTime?              @db.Timestamptz(6)
  is_anonymous                Boolean                @default(false)
  identities                  identities[]
  mfa_factors                 mfa_factors[]
  oauth_authorizations        oauth_authorizations[]
  oauth_consents              oauth_consents[]
  one_time_tokens             one_time_tokens[]
  sessions                    sessions[]
  webauthn_challenges         webauthn_challenges[]
  webauthn_credentials        webauthn_credentials[]
  app_activity_logs           app_activity_logs[]
  app_audit_logs              app_audit_logs[]
  app_auth_events             app_auth_events[]
  app_users                   app_users?
  audit_logs                  audit_logs[]

  @@index([created_at(sort: Desc)], map: "idx_users_created_at_desc")
  @@index([email], map: "idx_users_email")
  @@index([last_sign_in_at(sort: Desc)], map: "idx_users_last_sign_in_at_desc")
  @@index([instance_id])
  @@index([is_anonymous])
  @@map("users")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model webauthn_challenges {
  id             String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String?     @db.Uuid
  challenge_type String
  session_data   Json
  created_at     DateTime    @default(now()) @db.Timestamptz(6)
  expires_at     DateTime    @db.Timestamptz(6)
  users          auth_users? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at])
  @@index([user_id])
  @@schema("auth")
}

model webauthn_credentials {
  id               String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id          String     @db.Uuid
  credential_id    Bytes      @unique
  public_key       Bytes
  attestation_type String     @default("")
  aaguid           String?    @db.Uuid
  sign_count       BigInt     @default(0)
  transports       Json       @default("[]")
  backup_eligible  Boolean    @default(false)
  backed_up        Boolean    @default(false)
  friendly_name    String     @default("")
  created_at       DateTime   @default(now()) @db.Timestamptz(6)
  updated_at       DateTime   @default(now()) @db.Timestamptz(6)
  last_used_at     DateTime?  @db.Timestamptz(6)
  users            auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id])
  @@schema("auth")
}

model accounts {
  name                                          String
  type                                          String
  bank_name                                     String?
  account_no                                    String?
  currency                                      String?                     @default("THB")
  opening_balance                               Decimal?                    @default(0) @db.Decimal
  od_limit                                      Decimal?                    @default(0) @db.Decimal
  active                                        Boolean?                    @default(true)
  created_at                                    DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_at                                    DateTime?                   @db.Timestamptz(6)
  updated_by                                    String?
  version                                       Int?                        @default(1)
  bank                                          String?
  bank_branch                                   String?
  subtype                                       String?
  branch_id                                     BigInt?
  code                                          String                      @unique
  id                                            BigInt                      @id @default(autoincrement())
  branches                                      branches?                   @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  bank_statement                                bank_statement[]
  customer_receipts                             customer_receipts[]
  expenses                                      expenses[]
  loan_payments                                 loan_payments[]
  payment_account_splits                        payment_account_splits[]
  payment_status_logs                           payment_status_logs[]
  payments                                      payments[]
  petty_advance_returns                         petty_advance_returns[]
  petty_advances                                petty_advances[]
  receipts                                      receipts[]
  supplier_advance_payments                     supplier_advance_payments[]
  transfers_transfers_from_account_idToaccounts transfers[]                 @relation("transfers_from_account_idToaccounts")
  transfers_transfers_to_account_idToaccounts   transfers[]                 @relation("transfers_to_account_idToaccounts")

  @@schema("public")
}

model assets {
  code                 String            @unique
  name                 String
  category             String?
  department           String?
  location             String?
  responsible_person   String?
  purchase_date        DateTime?         @db.Date
  acquisition_type     String?
  original_cost        Decimal?          @default(0) @db.Decimal
  vat_amount           Decimal?          @default(0) @db.Decimal
  net_asset_cost       Decimal?          @default(0) @db.Decimal
  salvage_value        Decimal?          @default(0) @db.Decimal
  useful_life_months   Int?              @default(60)
  depreciation_method  String?           @default("Straight Line")
  asset_status         String?           @default("Active")
  serial_no            String?
  license_plate        String?
  chassis_no           String?
  engine_no            String?
  insurance_policy_no  String?
  warranty_expire_date DateTime?         @db.Date
  notes                String?
  created_by           String?
  created_at           DateTime?         @default(now()) @db.Timestamptz(6)
  branch_id            BigInt?
  supplier_id          BigInt?
  id                   BigInt            @id @default(autoincrement())
  purchase_bill_id     BigInt?
  asset_disposals      asset_disposals[]
  branches             branches?         @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  suppliers            suppliers?        @relation(fields: [supplier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  depreciations        depreciations[]

  @@schema("public")
}

model asset_categories {
  id         BigInt    @id @default(autoincrement())
  code       String    @unique
  name       String
  active     Boolean?  @default(true)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model departments {
  id         BigInt    @id @default(autoincrement())
  code       String    @unique
  name       String
  active     Boolean?  @default(true)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model audit_logs {
  user_id     String?     @db.Uuid
  username    String?
  action      String
  table_name  String?
  before_data Json?
  after_data  Json?
  ip_address  String?
  created_at  DateTime?   @default(now()) @db.Timestamptz(6)
  id          BigInt      @id @default(autoincrement())
  users       auth_users? @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_at(sort: Desc)], map: "idx_audit_date")
  @@index([user_id], map: "idx_audit_user")
  @@schema("public")
}

model bank_statement {
  date                   DateTime                 @db.Date
  ref_type               String?
  ref_id                 String?
  ref_no                 String?
  description            String?
  amount_in              Decimal?                 @default(0) @db.Decimal
  amount_out             Decimal?                 @default(0) @db.Decimal
  cash_flow_category     String?
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  created_by             String?
  desc                   String?
  type                   String?
  balance                Decimal?                 @db.Decimal
  note                   String?
  updated_at             DateTime?                @db.Timestamptz(6)
  updated_by             String?
  version                Int?                     @default(1)
  id                     BigInt                   @id @default(autoincrement())
  account_id             BigInt?
  doc_no                 String                   @unique(map: "uq_bank_statement_doc_no")
  accounts               accounts?                @relation(fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_receipts      customer_receipts?
  payment_account_splits payment_account_splits[]

  @@index([account_id], map: "idx_bank_account")
  @@index([date(sort: Desc)], map: "idx_bank_date")
  @@index([ref_type, ref_id], map: "idx_bank_statement_source", where: raw("((ref_type IS NOT NULL) AND (ref_id IS NOT NULL))"))
  @@schema("public")
}

model company_profiles {
  name        String
  name_en     String?
  tax_id      String?
  branch_code String?   @default("00000")
  address     String
  phone       String
  fax         String?
  email       String?
  website     String?
  bank_info   String?
  footer_note String?
  logo_url    String?
  created_at  DateTime  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime  @default(now()) @db.Timestamptz(6)
  updated_by  String?
  id          BigInt    @id @default(autoincrement())
  branch_id   BigInt?   @unique(map: "company_profiles_branch_id_unique", where: raw("(branch_id IS NOT NULL)"))
  branches    branches? @relation(fields: [branch_id], references: [id], onDelete: Restrict, onUpdate: NoAction)

  @@index([branch_id], map: "idx_company_profiles_branch_id")
  @@schema("public")
}

model bank_names {
  name                   String                   @unique
  active                 Boolean                  @default(true)
  created_at             DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at             DateTime                 @default(now()) @db.Timestamptz(6)
  symbol                 String?
  code                   String                   @unique
  id                     BigInt                   @id @default(autoincrement())
  supplier_bank_accounts supplier_bank_accounts[]

  @@index([active])
  @@schema("public")
}

model account_subtypes {
  code       String   @unique
  name       String   @unique
  sort_order Int      @default(0)
  active     Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  id         BigInt   @id @default(autoincrement())

  @@index([active], map: "idx_account_subtypes_active")
  @@index([sort_order], map: "idx_account_subtypes_sort_order")
  @@schema("public")
}

model bill_swap_history {
  bill_id            BigInt
  swap_date          DateTime? @default(now()) @db.Timestamptz(6)
  before_supplier_id BigInt?
  after_supplier_id  BigInt?
  item_index         Int?
  before_price       Decimal?  @db.Decimal
  after_price        Decimal?  @db.Decimal
  before_amount      Decimal?  @db.Decimal
  after_amount       Decimal?  @db.Decimal
  reason             String?
  changed_by         String?
  id                 BigInt    @id @default(autoincrement())
  event_key          String    @unique @default(dbgenerated("('BSH-'::text || (gen_random_uuid())::text)"))

  @@schema("public")
}

model branches {
  code                      String                      @unique
  name                      String
  address                   String?
  phone                     String?
  active                    Boolean?                    @default(true)
  created_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  id                        BigInt                      @id @default(autoincrement())
  accounts                  accounts[]
  app_user_branch_access    app_user_branch_access[]
  assets                    assets[]
  company_profiles          company_profiles?
  customer_receipts         customer_receipts[]
  expenses                  expenses[]
  grade_adjustments         grade_adjustments[]
  payments                  payments[]
  production_lines          production_lines[]
  production_machines       production_machines[]
  production_orders         production_orders[]
  purchase_bills            purchase_bills[]
  receipts                  receipts[]
  sales_bills               sales_bills[]
  customer_branches         customer_branches[]
  stock_cost_pool_entries   stock_cost_pool_entries[]
  stock_holds               stock_holds[]
  stock_issues              stock_issues[]
  stock_ledger              stock_ledger[]
  stock_transfers_from      stock_transfers[]           @relation("stock_transfers_from_branch_idTobranches")
  stock_transfers_to        stock_transfers[]           @relation("stock_transfers_to_branch_idTobranches")
  supplier_advance_payments supplier_advance_payments[]
  supplier_branches         supplier_branches[]
  suppliers                 suppliers[]
  warehouses                warehouses[]
  weight_tickets            weight_tickets[]

  @@schema("public")
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model currencies {
  name        String
  symbol      String?
  rate_to_thb Decimal?  @default(1) @db.Decimal
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)
  code        String    @unique
  id          BigInt    @id @default(autoincrement())

  @@schema("public")
}

model director_employees {
  code              String           @unique(map: "directors_code_key")
  name              String
  type              String?
  phone             String?
  bank_account      String?
  active            Boolean          @default(true)
  created_at        DateTime         @default(now()) @db.Timestamptz(6)
  updated_at        DateTime         @default(now()) @db.Timestamptz(6)
  bank_name         String?
  account_no        String?
  id                BigInt           @id(map: "directors_pkey") @default(autoincrement())
  name_title        String?
  first_name        String?
  last_name         String?
  bank_account_name String?
  bank_branch       String?
  petty_advances    petty_advances[]

  @@index([active])
  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model customers {
  code                     String                     @unique(map: "uq_customers_code")
  name                     String
  tax_id                   String?
  phone                    String?
  email                    String?
  address                  String?
  credit_term              Int?                       @default(0)
  active                   Boolean?                   @default(true)
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  updated_by               String?
  version                  Int?                       @default(1)
  type                     String?
  credit_limit             Decimal?                   @db.Decimal
  address_no               String?
  address_moo              String?
  address_village          String?
  address_road             String?
  address_subdistrict      String?
  address_district         String?
  address_province         String?
  address_postal_code      String?
  address_country          String?                    @default("ไทย")
  market_scope             String                     @default("ในประเทศ")
  legal_entity_type        String?
  name_title               String?
  first_name               String?
  last_name                String?
  country_code             String?
  address_line1            String?
  address_line2            String?
  address_city             String?
  address_state_region     String?
  address_postal_code_intl String?
  id                       BigInt                     @id @default(autoincrement())
  sales_id                 BigInt?
  customer_receipts        customer_receipts[]
  asset_disposals          asset_disposals[]
  customer_branches        customer_branches[]
  po_sells                 po_sells[]
  receipts                 receipts[]
  sales_bill_customer_advance_allocations sales_bill_customer_advance_allocations[]
  sales_bills                             sales_bills[]
  stock_issues                            stock_issues[]
  trading_allocation_facts                trading_allocation_facts[]
  trading_deals                           trading_deals[]
  weight_tickets                          weight_tickets[]

  @@index([name], map: "idx_customers_name")
  @@index([active], map: "idx_customers_active")
  @@index([country_code], map: "idx_customers_country_code")
  @@index([market_scope], map: "idx_customers_market_scope")
  @@index([type], map: "idx_customers_type")
  @@schema("public")
}

model customer_branches {
  id          BigInt    @id @default(autoincrement())
  customer_id BigInt
  branch_id   BigInt
  active      Boolean   @default(true)
  is_primary  Boolean   @default(false)
  created_at  DateTime  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime  @default(now()) @db.Timestamptz(6)
  created_by  String?
  updated_by  String?
  branches    branches  @relation(fields: [branch_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  customers   customers @relation(fields: [customer_id], references: [id], onDelete: Restrict, onUpdate: NoAction)

  @@unique([customer_id, branch_id], map: "customer_branches_customer_branch_unique")
  @@unique([customer_id], map: "uq_customer_branches_primary_active", where: raw("((active IS TRUE) AND (is_primary IS TRUE))"))
  @@index([branch_id, customer_id], map: "idx_customer_branches_branch_active_customer", where: raw("(active IS TRUE)"))
  @@index([customer_id, branch_id], map: "idx_customer_branches_customer_active_branch", where: raw("(active IS TRUE)"))
  @@schema("public")
}

model deletion_log {
  table_name String
  row_id     String
  deleted_at DateTime @default(now()) @db.Timestamptz(6)
  deleted_by String?
  id         BigInt   @id @default(autoincrement())

  @@index([deleted_at], map: "idx_deletion_log_deleted_at")
  @@index([table_name, deleted_at], map: "idx_deletion_log_table")
  @@schema("public")
}

model deletion_tombstones {
  table_name String
  record_id  String
  deleted_at DateTime? @default(now()) @db.Timestamptz(6)
  deleted_by String?
  reason     String?
  id         BigInt    @id @default(autoincrement())

  @@index([deleted_at], map: "idx_tombstones_deleted_at")
  @@index([table_name], map: "idx_tombstones_table")
  @@schema("public")
}

model depreciations {
  date            DateTime  @db.Date
  amount          Decimal   @db.Decimal
  accumulated     Decimal?  @default(0) @db.Decimal
  nbv             Decimal?  @default(0) @db.Decimal
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  id              BigInt    @id @default(autoincrement())
  asset_id        BigInt?
  period_year     Int?
  period_month    Int?
  status          String    @default("posted")
  posted_at       DateTime  @default(now()) @db.Timestamptz(6)
  posted_by       String?
  updated_at      DateTime  @default(now()) @db.Timestamptz(6)
  updated_by      String?
  reversed_at     DateTime? @db.Timestamptz(6)
  reversed_by     String?
  reversal_reason String?
  assets          assets?   @relation(fields: [asset_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([asset_id, period_year, period_month, status], map: "idx_depreciations_asset_period_status")
  @@index([status, date(sort: Desc), id(sort: Desc)], map: "idx_depreciations_status_date")
  @@schema("public")
}

model asset_disposals {
  id                     BigInt             @id @default(autoincrement())
  disposal_no            String             @unique(map: "uq_asset_disposals_disposal_no")
  asset_id               BigInt
  disposal_date          DateTime           @db.Date
  disposal_type          String
  selling_price          Decimal            @default(0) @db.Decimal
  book_value_at_disposal Decimal            @default(0) @db.Decimal
  gain_loss              Decimal            @default(0) @db.Decimal
  customer_id            BigInt?
  customer_receipt_id    BigInt?
  receipt_ref_no         String?
  reason                 String?
  notes                  String?
  status                 String             @default("draft")
  approved_at            DateTime?          @db.Timestamptz(6)
  approved_by            String?
  created_at             DateTime           @default(now()) @db.Timestamptz(6)
  created_by             String?
  updated_at             DateTime           @default(now()) @db.Timestamptz(6)
  updated_by             String?
  reversed_at            DateTime?          @db.Timestamptz(6)
  reversed_by            String?
  reversal_reason        String?
  assets                 assets             @relation(fields: [asset_id], references: [id], onUpdate: NoAction)
  customers              customers?         @relation(fields: [customer_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  customer_receipts      customer_receipts? @relation(fields: [customer_receipt_id], references: [id], onDelete: Restrict, onUpdate: NoAction)

  @@index([asset_id, disposal_date(sort: Desc), id(sort: Desc)], map: "idx_asset_disposals_asset_date")
  @@index([status, disposal_date(sort: Desc), id(sort: Desc)], map: "idx_asset_disposals_status_date")
  @@index([customer_id, disposal_date(sort: Desc), id(sort: Desc)], map: "idx_asset_disposals_customer_date", where: raw("(customer_id IS NOT NULL)"))
  @@index([customer_receipt_id], map: "idx_asset_disposals_receipt", where: raw("(customer_receipt_id IS NOT NULL)"))
  @@schema("public")
}

model equity {
  paid_up_capital         Decimal?  @default(0) @db.Decimal
  retained_earnings       Decimal?  @default(0) @db.Decimal
  owner_equity_adjustment Decimal?  @default(0) @db.Decimal
  updated_at              DateTime? @default(now()) @db.Timestamptz(6)
  id                      BigInt    @id @default(autoincrement())

  @@schema("public")
}

model expense_categories {
  name            String
  active          Boolean?       @default(true)
  type            String?
  code            String         @unique
  id              BigInt         @id @default(autoincrement())
  expense_type_id BigInt?
  expense_types   expense_types? @relation(fields: [expense_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  expenses        expenses[]

  @@schema("public")
}

model expense_types {
  name               String
  active             Boolean?             @default(true)
  created_at         DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?            @default(now()) @db.Timestamptz(6)
  code               String               @unique
  id                 BigInt               @id @default(autoincrement())
  expense_categories expense_categories[]

  @@schema("public")
}

model expenses {
  doc_no             String
  date               DateTime            @db.Date
  payee              String?
  amount             Decimal             @db.Decimal
  vat                Decimal?            @default(0) @db.Decimal
  wht                Decimal?            @default(0) @db.Decimal
  net_amount         Decimal?            @default(0) @db.Decimal
  tax_invoice_no     String?
  notes              String?
  status             String              @default("pending_approval")
  approved_by        String?
  approved_at        DateTime?           @db.Timestamptz(6)
  paid_at            DateTime?           @db.Timestamptz(6)
  created_by         String?
  created_at         DateTime?           @default(now()) @db.Timestamptz(6)
  vat_amount         Decimal?            @default(0) @db.Decimal
  wht_amount         Decimal?            @default(0) @db.Decimal
  description        String?
  items              Json?
  paid_status        String?             @default("pending")
  voucher_id         String?
  due_date           DateTime?           @db.Date
  ref_doc_no         String?
  wht_pct            Decimal?            @default(0) @db.Decimal
  updated_at         DateTime?           @default(now()) @db.Timestamptz(6)
  payee_bank         String?
  payee_account_no   String?
  payee_account_name String?
  updated_by         String?
  version            Int?                @default(1)
  category_id        BigInt?
  branch_id          BigInt?
  id                 BigInt              @id @default(autoincrement())
  account_id         BigInt?
  supplier_id        BigInt?
  accounts           accounts?           @relation(fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  branches           branches?           @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  expense_categories expense_categories? @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  suppliers          suppliers?          @relation(fields: [supplier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([date(sort: Desc)], map: "idx_exp_date")
  @@index([doc_no], map: "idx_expenses_doc_no")
  @@index([due_date], map: "idx_expenses_due_date")
  @@index([paid_status], map: "idx_expenses_paid_status")
  @@index([supplier_id], map: "idx_expenses_supplier")
  @@index([voucher_id], map: "idx_expenses_voucher_id")
  @@schema("public")
}

model fx_gain_loss {
  date            DateTime  @db.Date
  ref_type        String?
  ref_id          String?
  currency        String?
  amount_fc       Decimal?  @db.Decimal
  rate_book       Decimal?  @db.Decimal
  rate_settlement Decimal?  @db.Decimal
  gain_loss       Decimal   @db.Decimal
  notes           String?
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  id              BigInt    @id @default(autoincrement())

  @@schema("public")
}

model grade_adjustments {
  doc_no                                                 String
  date                                                   DateTime                      @db.Date
  qty_diff                                               Decimal                       @db.Decimal
  value_diff                                             Decimal                       @db.Decimal
  reason                                                 String?
  approved_by                                            String?
  notes                                                  String?
  created_at                                             DateTime?                     @default(now()) @db.Timestamptz(6)
  updated_at                                             DateTime?                     @db.Timestamptz(6)
  updated_by                                             String?
  version                                                Int?                          @default(1)
  warehouse_id                                           BigInt?
  id                                                     BigInt                        @id @default(autoincrement())
  product_id                                             BigInt?
  status                                                 String                        @default("posted")
  cost_status                                            String                        @default("allocated")
  source_type                                            String                        @default("Manual")
  allocation_method                                      String?
  branch_id                                              BigInt?
  source_product_id                                      BigInt?
  target_product_id                                      BigInt?
  source_qty                                             Decimal?                      @db.Decimal
  target_qty                                             Decimal?                      @db.Decimal
  source_lot_no                                          String?
  target_lot_no                                          String?
  reversed_at                                            DateTime?                     @db.Timestamptz(6)
  reversed_by                                            String?
  target_cost_policy                                     String                        @default("SOURCE_MATCHED")
  source_unit_cost                                       Decimal?                      @db.Decimal
  target_unit_cost                                       Decimal?                      @db.Decimal
  cost_variance                                          Decimal                       @default(0) @db.Decimal
  cost_override_reason                                   String?
  branches                                               branches?                     @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products                                               products?                     @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products_grade_adjustments_source_product_idToproducts products?                     @relation("grade_adjustments_source_product_idToproducts", fields: [source_product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products_grade_adjustments_target_product_idToproducts products?                     @relation("grade_adjustments_target_product_idToproducts", fields: [target_product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  warehouses                                             warehouses?                   @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  stock_cost_pool_allocations                            stock_cost_pool_allocations[]
  stock_cost_pool_entries                                stock_cost_pool_entries[]

  @@index([cost_status, date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_grade_adjustments_cost_status_date")
  @@index([status, date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_grade_adjustments_status_date")
  @@schema("public")
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model stock_cost_pool_entries {
  id                                                                                                    BigInt                        @id @default(autoincrement())
  pool_key                                                                                              String                        @unique @default(dbgenerated("('SCP-'::text || (gen_random_uuid())::text)"))
  source_type                                                                                           String
  source_ref_type                                                                                       String
  source_ref_id                                                                                         String?
  source_ref_no                                                                                         String?
  source_line_id                                                                                        String?
  source_ledger_id                                                                                      BigInt?
  regrade_adjustment_id                                                                                 BigInt?
  date                                                                                                  DateTime                      @db.Date
  branch_id                                                                                             BigInt?
  warehouse_id                                                                                          BigInt?
  product_id                                                                                            BigInt
  lot_no                                                                                                String?
  original_qty                                                                                          Decimal                       @db.Decimal
  allocated_qty                                                                                         Decimal                       @default(0) @db.Decimal
  released_qty                                                                                          Decimal                       @default(0) @db.Decimal
  unit_cost                                                                                             Decimal                       @db.Decimal
  original_value                                                                                        Decimal                       @db.Decimal
  status                                                                                                String                        @default("Available")
  notes                                                                                                 String?
  created_at                                                                                            DateTime?                     @default(now()) @db.Timestamptz(6)
  created_by                                                                                            String?
  updated_at                                                                                            DateTime?                     @db.Timestamptz(6)
  updated_by                                                                                            String?
  stock_cost_pool_allocations_stock_cost_pool_allocations_source_pool_entry_idTostock_cost_pool_entries stock_cost_pool_allocations[] @relation("stock_cost_pool_allocations_source_pool_entry_idTostock_cost_pool_entries")
  stock_cost_pool_allocations_stock_cost_pool_allocations_target_pool_entry_idTostock_cost_pool_entries stock_cost_pool_allocations[] @relation("stock_cost_pool_allocations_target_pool_entry_idTostock_cost_pool_entries")
  branches                                                                                              branches?                     @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products                                                                                              products                      @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  grade_adjustments                                                                                     grade_adjustments?            @relation(fields: [regrade_adjustment_id], references: [id], onUpdate: NoAction)
  stock_ledger                                                                                          stock_ledger?                 @relation(fields: [source_ledger_id], references: [id], onUpdate: NoAction)
  warehouses                                                                                            warehouses?                   @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([product_id, branch_id, warehouse_id, status, date, id], map: "idx_stock_cost_pool_entries_available", where: raw("(status = ANY (ARRAY['Available'::text, 'Partially Used'::text]))"))
  @@index([regrade_adjustment_id], map: "idx_stock_cost_pool_entries_regrade", where: raw("(regrade_adjustment_id IS NOT NULL)"))
  @@index([source_ref_type, source_ref_no], map: "idx_stock_cost_pool_entries_ref")
  @@schema("public")
}

model stock_cost_pool_allocations {
  id                                                                                                BigInt                   @id @default(autoincrement())
  allocation_key                                                                                    String                   @unique @default(dbgenerated("('SCA-'::text || (gen_random_uuid())::text)"))
  allocation_type                                                                                   String
  status                                                                                            String                   @default("active")
  grade_adjustment_id                                                                               BigInt
  source_pool_entry_id                                                                              BigInt
  target_pool_entry_id                                                                              BigInt?
  line_no                                                                                           Int                      @default(1)
  allocation_method                                                                                 String
  qty                                                                                               Decimal                  @db.Decimal
  unit_cost                                                                                         Decimal                  @db.Decimal
  total_cost                                                                                        Decimal                  @db.Decimal
  created_at                                                                                        DateTime?                @default(now()) @db.Timestamptz(6)
  created_by                                                                                        String?
  reversed_at                                                                                       DateTime?                @db.Timestamptz(6)
  reversed_by                                                                                       String?
  notes                                                                                             String?
  grade_adjustments                                                                                 grade_adjustments        @relation(fields: [grade_adjustment_id], references: [id], onUpdate: NoAction)
  stock_cost_pool_entries_stock_cost_pool_allocations_source_pool_entry_idTostock_cost_pool_entries stock_cost_pool_entries  @relation("stock_cost_pool_allocations_source_pool_entry_idTostock_cost_pool_entries", fields: [source_pool_entry_id], references: [id], onUpdate: NoAction)
  stock_cost_pool_entries_stock_cost_pool_allocations_target_pool_entry_idTostock_cost_pool_entries stock_cost_pool_entries? @relation("stock_cost_pool_allocations_target_pool_entry_idTostock_cost_pool_entries", fields: [target_pool_entry_id], references: [id], onUpdate: NoAction)

  @@index([grade_adjustment_id, status], map: "idx_stock_cost_pool_allocations_adjustment")
  @@index([source_pool_entry_id, status], map: "idx_stock_cost_pool_allocations_source")
  @@schema("public")
}

model historical_monthly {
  year        Int?
  month       Int?
  metric_type String?
  category_id String?
  amount      Decimal?  @default(0) @db.Decimal
  note        String?
  created_by  String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_by  String?
  updated_at  DateTime? @db.Timestamptz(6)
  id          BigInt    @id @default(autoincrement())

  @@schema("public")
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model impurities {
  name                String
  active              Boolean               @default(true)
  created_at          DateTime              @default(now()) @db.Timestamptz(6)
  updated_at          DateTime              @default(now()) @db.Timestamptz(6)
  code                String                @unique
  id                  BigInt                @id @default(autoincrement())
  weight_ticket_lines weight_ticket_lines[]

  @@index([active])
  @@schema("public")
}

model loan_payments {
  doc_no           String
  date             DateTime        @db.Date
  principal_amount Decimal?        @default(0) @db.Decimal
  interest_amount  Decimal?        @default(0) @db.Decimal
  total_amount     Decimal         @db.Decimal
  status           String?         @default("active")
  notes            String?
  created_at       DateTime?       @default(now()) @db.Timestamptz(6)
  id               BigInt          @id @default(autoincrement())
  account_id       BigInt?
  loan_id          BigInt?
  schedule_id      BigInt?
  accounts         accounts?       @relation(fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  loans            loans?          @relation(fields: [loan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  loan_schedules   loan_schedules? @relation(fields: [schedule_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model loan_schedules {
  installment_no   Int?
  due_date         DateTime        @db.Date
  principal_amount Decimal?        @default(0) @db.Decimal
  interest_amount  Decimal?        @default(0) @db.Decimal
  total_due_amount Decimal?        @default(0) @db.Decimal
  paid_amount      Decimal?        @default(0) @db.Decimal
  payment_status   String?         @default("Pending")
  paid_date        DateTime?       @db.Date
  notes            String?
  id               BigInt          @id @default(autoincrement())
  loan_id          BigInt?
  loan_payments    loan_payments[]
  loans            loans?          @relation(fields: [loan_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([due_date], map: "idx_sched_due")
  @@index([loan_id], map: "idx_sched_loan")
  @@schema("public")
}

model loans {
  contract_no        String           @unique
  loan_type          String
  lender_name        String?
  start_date         DateTime?        @db.Date
  end_date           DateTime?        @db.Date
  principal_amount   Decimal          @db.Decimal
  interest_rate      Decimal?         @db.Decimal
  term_months        Int?
  installment_amount Decimal?         @db.Decimal
  status             String?          @default("Active")
  notes              String?
  created_at         DateTime?        @default(now()) @db.Timestamptz(6)
  id                 BigInt           @id @default(autoincrement())
  loan_payments      loan_payments[]
  loan_schedules     loan_schedules[]

  @@schema("public")
}

model fx_rates {
  rate_date     DateTime @db.Date
  from_currency String
  to_currency   String   @default("THB")
  rate_type     String   @default("BOT Rate")
  rate          Decimal  @db.Decimal(18, 6)
  source        String?
  note          String?
  active        Boolean  @default(true)
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  updated_at    DateTime @default(now()) @db.Timestamptz(6)
  created_by    String?
  updated_by    String?
  id            BigInt   @id @default(autoincrement())

  @@unique([rate_date, from_currency, to_currency, rate_type], map: "fx_rates_unique_effective")
  @@index([active])
  @@index([rate_date(sort: Desc)], map: "fx_rates_date_idx")
  @@index([from_currency, to_currency, rate_date(sort: Desc)], map: "fx_rates_pair_idx")
  @@schema("public")
}

model opening_balance {
  data       Json?
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_by String?
  id         BigInt    @id @default(autoincrement())

  @@schema("public")
}

model overseas_recipients {
  name       String
  country    String?
  bank_name  String?
  account_no String?
  swift      String?
  currency   String?  @default("USD")
  active     Boolean? @default(true)
  id         BigInt   @id @default(autoincrement())
  code       String   @unique

  @@schema("public")
}

model production_machines {
  name                    String
  type                    String?
  capacity_kg_per_hr      Decimal?            @db.Decimal(14, 3)
  normal_yield_pct        Decimal?            @db.Decimal(7, 3)
  std_process_cost_per_hr Decimal?            @db.Decimal(14, 2)
  active                  Boolean             @default(true)
  created_at              DateTime            @default(now()) @db.Timestamptz(6)
  updated_at              DateTime            @default(now()) @db.Timestamptz(6)
  branch_id               BigInt?
  id                      BigInt              @id(map: "machines_pkey") @default(autoincrement())
  branches                branches?           @relation(fields: [branch_id], references: [id], onDelete: Restrict, onUpdate: NoAction, map: "machines_branch_id_fkey")
  production_orders       production_orders[]

  @@index([active])
  @@index([branch_id])
  @@schema("public")
}

model production_machine_types {
  name       String   @unique
  active     Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  id         BigInt   @id @default(autoincrement())

  @@index([active], map: "idx_production_machine_types_active")
  @@schema("public")
}

model payment_methods {
  name              String
  active            Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  type              String              @default("bank")
  code              String              @unique
  id                BigInt              @id @default(autoincrement())
  bank_name         String?
  account_no        String?
  customer_receipts customer_receipts[]

  @@index([type])
  @@index([active])
  @@schema("public")
}

model vat_settings {
  name           String
  rate_percent   Decimal   @default(7.00) @db.Decimal(5, 2)
  active         Boolean   @default(true)
  is_default     Boolean   @unique(map: "uq_vat_settings_default_active", where: raw("(active AND is_default)")) @default(false)
  effective_from DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  effective_to   DateTime? @db.Date
  created_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_by     String?
  id             BigInt    @id @default(autoincrement())

  @@index([active, is_default, effective_from(sort: Desc)], map: "idx_vat_settings_effective")
  @@schema("public")
}

model wht_settings {
  name           String
  rate_percent   Decimal   @default(3.00) @db.Decimal(5, 2)
  active         Boolean   @default(true)
  is_default     Boolean   @unique(map: "uq_wht_settings_default_active", where: raw("(active AND is_default)")) @default(false)
  effective_from DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  effective_to   DateTime? @db.Date
  created_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_by     String?
  id             BigInt    @id @default(autoincrement())

  @@index([active, is_default, effective_from(sort: Desc)], map: "idx_wht_settings_effective")
  @@schema("public")
}

model production_lines {
  name               String
  responsible_person String?
  active             Boolean             @default(true)
  created_at         DateTime            @default(now()) @db.Timestamptz(6)
  updated_at         DateTime            @default(now()) @db.Timestamptz(6)
  branch_id          BigInt?
  id                 BigInt              @id @default(autoincrement())
  branches           branches?           @relation(fields: [branch_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  production_orders  production_orders[]

  @@index([active])
  @@index([branch_id])
  @@schema("public")
}

model overseas_remittance_purposes {
  name       String
  active     Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  code       String   @unique
  id         BigInt   @id(map: "remittance_purposes_pkey") @default(autoincrement())

  @@index([active])
  @@schema("public")
}

model payments {
  doc_no                       String
  date                         DateTime                       @db.Date
  amount                       Decimal                        @db.Decimal
  withholding_tax              Decimal?                       @default(0) @db.Decimal
  fee                          Decimal?                       @default(0) @db.Decimal
  net_amount                   Decimal?                       @default(0) @db.Decimal
  vat_type                     String?
  lines                        Json?
  notes                        String?
  status                       String?                        @default("active")
  created_by                   String?
  created_at                   DateTime?                      @default(now()) @db.Timestamptz(6)
  voucher_id                   String?
  bank_fee                     Decimal?                       @default(0) @db.Decimal
  discount                     Decimal?                       @default(0) @db.Decimal
  method                       String?
  updated_at                   DateTime?                      @db.Timestamptz(6)
  updated_by                   String?
  version                      Int?                           @default(1)
  idempotency_key              String?
  supplier_id                  BigInt?
  branch_id                    BigInt?
  id                           BigInt                         @id @default(autoincrement())
  account_id                   BigInt?
  bill_id                      BigInt?
  payment_approval_id          BigInt?
  payment_account_splits       payment_account_splits[]
  payment_allocations          payment_allocations[]
  payment_approval_status_logs payment_approval_status_logs[]
  payment_status_logs          payment_status_logs[]
  accounts                     accounts?                      @relation(fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  branches                     branches?                      @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  payment_approvals            payment_approvals?             @relation(fields: [payment_approval_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  suppliers                    suppliers?                     @relation(fields: [supplier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([doc_no], map: "idx_payments_doc_no")
  @@index([payment_approval_id], map: "idx_payments_payment_approval")
  @@index([idempotency_key], map: "idx_payments_idempotency", where: raw("(idempotency_key IS NOT NULL)"))
  @@index([voucher_id], map: "idx_payments_voucher", where: raw("(voucher_id IS NOT NULL)"))
  @@index([bill_id], map: "idx_pmt_bill")
  @@index([date(sort: Desc)], map: "idx_pmt_date")
  @@index([supplier_id], map: "idx_pmt_supplier")
  @@index([bill_id, created_at, doc_no, amount, withholding_tax, discount, status], map: "idx_payments_active_bill_created", where: raw("(status <> 'cancelled'::text)"))
  @@schema("public")
}

model payment_approvals {
  source_type                          String
  source_id                            String
  source_doc_no_snapshot               String?
  source_date_snapshot                 DateTime?                      @db.Date
  party_id                             String?
  party_name_snapshot                  String?
  approved_amount                      Decimal                        @default(0) @db.Decimal
  destination_bank_account_id_snapshot String?
  destination_bank_name_snapshot       String?
  destination_account_no_snapshot      String?
  destination_payment_method_snapshot  String?
  note                                 String?
  status                               String                         @default("approved")
  approved_by                          String?
  approved_at                          DateTime?                      @db.Timestamptz(6)
  paid_at                              DateTime?                      @db.Timestamptz(6)
  voided_at                            DateTime?                      @db.Timestamptz(6)
  voided_by                            String?
  void_reason                          String?
  created_at                           DateTime                       @default(now()) @db.Timestamptz(6)
  updated_at                           DateTime                       @default(now()) @db.Timestamptz(6)
  doc_no                               String?                        @unique(map: "uq_payment_approvals_doc_no", where: raw("(doc_no IS NOT NULL)"))
  id                                   BigInt                         @id @default(autoincrement())
  payment_id                           BigInt?
  payment_allocations                  payment_allocations[]
  payment_approval_status_logs         payment_approval_status_logs[]
  payments                             payments[]

  @@index([source_type, source_id], map: "idx_payment_approvals_source")
  @@index([status], map: "idx_payment_approvals_status")
  @@index([approved_at(sort: Desc)], map: "idx_payment_approvals_approved_at")
  @@index([doc_no], map: "idx_payment_approvals_doc_no")
  @@index([source_id, status, doc_no], map: "idx_payment_approvals_purchase_bill_locked", where: raw("((source_type = 'purchase_bill'::text) AND (status = ANY (ARRAY['approved'::text, 'paid'::text])))"))
  @@schema("public")
}

model payment_approval_status_logs {
  event_key                String            @unique(map: "idx_payment_approval_status_logs_event_key") @default(dbgenerated("('PMALOG-'::text || (gen_random_uuid())::text)"))
  action                   String
  payment_approval_doc_no  String
  source_type              String
  source_id                String
  source_doc_no_snapshot   String?
  approved_amount_snapshot Decimal           @default(0) @db.Decimal
  from_status              String?
  to_status                String
  payment_doc_no           String?
  note                     String?
  meta                     Json?
  created_at               DateTime          @default(now()) @db.Timestamptz(6)
  created_by               String?
  id                       BigInt            @id @default(autoincrement())
  payment_approval_id      BigInt
  payment_id               BigInt?
  payment_approvals        payment_approvals @relation(fields: [payment_approval_id], references: [id], onUpdate: NoAction)
  payments                 payments?         @relation(fields: [payment_id], references: [id], onUpdate: NoAction)

  @@index([action], map: "idx_payment_approval_status_logs_action")
  @@index([payment_approval_doc_no], map: "idx_payment_approval_status_logs_approval_doc_no")
  @@index([payment_approval_id, created_at(sort: Desc)], map: "idx_payment_approval_status_logs_approval_created")
  @@index([payment_doc_no], map: "idx_payment_approval_status_logs_payment_doc_no", where: raw("(payment_doc_no IS NOT NULL)"))
  @@schema("public")
}

model payment_status_logs {
  event_key              String     @unique(map: "idx_payment_status_logs_event_key") @default(dbgenerated("('PMTLOG-'::text || (gen_random_uuid())::text)"))
  action                 String
  payment_doc_no         String
  payment_voucher_id     String?
  supplier_code_snapshot String?
  account_code_snapshot  String?
  amount_snapshot        Decimal    @default(0) @db.Decimal
  net_amount_snapshot    Decimal    @default(0) @db.Decimal
  from_status            String?
  to_status              String
  note                   String?
  meta                   Json?
  created_at             DateTime   @default(now()) @db.Timestamptz(6)
  created_by             String?
  id                     BigInt     @id @default(autoincrement())
  payment_id             BigInt?
  supplier_id            BigInt?
  account_id             BigInt?
  accounts               accounts?  @relation(fields: [account_id], references: [id], onUpdate: NoAction)
  payments               payments?  @relation(fields: [payment_id], references: [id], onUpdate: NoAction)
  suppliers              suppliers? @relation(fields: [supplier_id], references: [id], onUpdate: NoAction)

  @@index([action], map: "idx_payment_status_logs_action")
  @@index([payment_doc_no, created_at(sort: Desc)], map: "idx_payment_status_logs_doc_created")
  @@index([payment_id], map: "idx_payment_status_logs_payment", where: raw("(payment_id IS NOT NULL)"))
  @@index([payment_voucher_id], map: "idx_payment_status_logs_voucher", where: raw("(payment_voucher_id IS NOT NULL)"))
  @@schema("public")
}

model payment_allocations {
  allocation_key          String            @unique(map: "idx_payment_allocations_allocation_key")
  payment_doc_no          String
  payment_voucher_id      String?
  payment_approval_doc_no String
  source_type             String
  source_doc_no_snapshot  String?
  allocated_amount        Decimal           @default(0) @db.Decimal
  status                  String            @default("active")
  created_at              DateTime          @default(now()) @db.Timestamptz(6)
  created_by              String?
  updated_at              DateTime?         @db.Timestamptz(6)
  updated_by              String?
  id                      BigInt            @id @default(autoincrement())
  payment_id              BigInt?
  payment_approval_id     BigInt
  payment_approvals       payment_approvals @relation(fields: [payment_approval_id], references: [id], onUpdate: NoAction)
  payments                payments?         @relation(fields: [payment_id], references: [id], onUpdate: NoAction)

  @@index([payment_approval_id], map: "idx_payment_allocations_approval")
  @@index([payment_id], map: "idx_payment_allocations_payment", where: raw("(payment_id IS NOT NULL)"))
  @@index([status], map: "idx_payment_allocations_status")
  @@index([payment_voucher_id], map: "idx_payment_allocations_voucher", where: raw("(payment_voucher_id IS NOT NULL)"))
  @@schema("public")
}

model payment_account_splits {
  split_key             String          @unique(map: "idx_payment_account_splits_split_key")
  payment_doc_no        String
  payment_voucher_id    String?
  account_code_snapshot String?
  account_name_snapshot String?
  amount                Decimal         @default(0) @db.Decimal
  status                String          @default("active")
  bank_statement_doc_no String?
  created_at            DateTime        @default(now()) @db.Timestamptz(6)
  created_by            String?
  updated_at            DateTime?       @db.Timestamptz(6)
  updated_by            String?
  id                    BigInt          @id @default(autoincrement())
  payment_id            BigInt?
  account_id            BigInt?
  bank_statement_id     BigInt?
  accounts              accounts?       @relation(fields: [account_id], references: [id], onUpdate: NoAction)
  bank_statement        bank_statement? @relation(fields: [bank_statement_id], references: [id], onUpdate: NoAction)
  payments              payments?       @relation(fields: [payment_id], references: [id], onUpdate: NoAction)

  @@index([account_id], map: "idx_payment_account_splits_account", where: raw("(account_id IS NOT NULL)"))
  @@index([payment_id], map: "idx_payment_account_splits_payment", where: raw("(payment_id IS NOT NULL)"))
  @@index([status], map: "idx_payment_account_splits_status")
  @@index([payment_voucher_id], map: "idx_payment_account_splits_voucher", where: raw("(payment_voucher_id IS NOT NULL)"))
  @@schema("public")
}

model petty_advance_returns {
  date           DateTime       @db.Date
  amount         Decimal        @default(0) @db.Decimal
  notes          String?
  created_by     String?
  created_at     DateTime       @default(now()) @db.Timestamptz(6)
  updated_by     String?
  updated_at     DateTime       @default(now()) @db.Timestamptz(6)
  version        Int            @default(1)
  id             BigInt         @id @default(autoincrement())
  account_id     BigInt?
  advance_id     BigInt
  doc_no         String         @unique(map: "uq_petty_advance_returns_doc_no")
  status         String         @default("approved")
  approved_at    DateTime?      @db.Timestamptz(6)
  approved_by    String?
  voided_at      DateTime?      @db.Timestamptz(6)
  voided_by      String?
  void_reason    String?
  accounts       accounts?      @relation(fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  petty_advances petty_advances @relation(fields: [advance_id], references: [id], onUpdate: NoAction)

  @@index([account_id], map: "idx_petty_advance_returns_account")
  @@index([advance_id], map: "idx_petty_advance_returns_advance")
  @@index([date(sort: Desc)], map: "idx_petty_advance_returns_date")
  @@index([doc_no], map: "idx_petty_advance_returns_doc_no")
  @@index([status], map: "idx_petty_advance_returns_status")
  @@schema("public")
}

model petty_advances {
  doc_no                      String                  @unique
  date                        DateTime                @db.Date
  type                        String                  @default("DIRECTOR_LOAN")
  recipient_name              String
  amount                      Decimal                 @default(0) @db.Decimal
  status                      String                  @default("active")
  notes                       String?
  returned_amount             Decimal                 @default(0) @db.Decimal
  closed_at                   DateTime?               @db.Timestamptz(6)
  created_by                  String?
  created_at                  DateTime                @default(now()) @db.Timestamptz(6)
  updated_by                  String?
  updated_at                  DateTime                @default(now()) @db.Timestamptz(6)
  version                     Int                     @default(1)
  id                          BigInt                  @id @default(autoincrement())
  account_id                  BigInt?
  recipient_person_code       String?
  recipient_bank_name         String?
  recipient_bank_account_name String?
  recipient_account_no        String?
  recipient_bank_branch       String?
  petty_advance_returns       petty_advance_returns[]
  accounts                    accounts?               @relation(fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  recipient_person            director_employees?     @relation(fields: [recipient_person_code], references: [code], onDelete: NoAction, onUpdate: NoAction)

  @@index([account_id], map: "idx_petty_advances_account")
  @@index([date(sort: Desc)], map: "idx_petty_advances_date")
  @@index([recipient_person_code], map: "idx_petty_advances_recipient_person_code")
  @@index([status], map: "idx_petty_advances_status")
  @@index([type], map: "idx_petty_advances_type")
  @@schema("public")
}

model po_buys {
  doc_no                       String
  date                         DateTime                       @db.Date
  expected_delivery            DateTime?                      @db.Date
  total_amount                 Decimal?                       @default(0) @db.Decimal
  cut_amount                   Decimal?                       @default(0) @db.Decimal
  remaining_amount             Decimal?                       @default(0) @db.Decimal
  status                       String?                        @default("Open")
  items                        Json?
  notes                        String?
  created_at                   DateTime?                      @default(now()) @db.Timestamptz(6)
  delivery_date                DateTime?                      @db.Date
  qty                          Decimal?                       @db.Decimal
  unit_price                   Decimal?                       @db.Decimal
  remaining_qty                Decimal?                       @db.Decimal
  purpose                      String?
  note                         String?
  cost_deducted                Boolean?                       @default(false)
  cost_deducted_at             DateTime?                      @db.Timestamptz(6)
  cost_deducted_by             String?
  created_by                   String?
  updated_at                   DateTime?                      @db.Timestamptz(6)
  require_delivery             Boolean?                       @default(true)
  is_opening_pool              Boolean?                       @default(false)
  updated_by                   String?
  version                      Int?                           @default(1)
  cancel_note                  String?
  cancelled_at                 DateTime?                      @db.Timestamptz(6)
  cancelled_by                 String?
  short_closed_qty             Decimal?                       @default(0) @db.Decimal
  short_closed_amount          Decimal?                       @default(0) @db.Decimal
  short_closed_note            String?
  short_closed_at              DateTime?                      @db.Timestamptz(6)
  short_closed_by              String?
  supplier_id                  BigInt?
  branch_id                    BigInt?
  warehouse_id                 BigInt?
  id                           BigInt                         @id @default(autoincrement())
  channel_id                   BigInt?
  product_id                   BigInt?
  subtotal                     Decimal?                       @default(0) @db.Decimal
  has_vat                      Boolean?                       @default(false)
  vat_type                     String?                        @default("NONE")
  vat_rate_percent             Decimal                        @default(7.00) @db.Decimal(5, 2)
  vat_amount                   Decimal?                       @default(0) @db.Decimal
  po_buy_allocation_logs       po_buy_allocation_logs[]
  po_buy_status_logs           po_buy_status_logs[]
  suppliers                    suppliers?                     @relation(fields: [supplier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchase_bill_items          purchase_bill_items[]
  purchase_bill_po_allocations purchase_bill_po_allocations[]

  @@index([branch_id, date(sort: Desc), doc_no(sort: Desc)], map: "idx_po_buys_branch_date_doc_no", where: raw("(branch_id IS NOT NULL)"))
  @@index([date(sort: Desc), doc_no(sort: Desc)], map: "idx_po_buys_date_doc_no")
  @@index([doc_no], map: "idx_po_buys_doc_no")
  @@index([status, date(sort: Desc), doc_no(sort: Desc)], map: "idx_po_buys_status_date_doc_no", where: raw("(status IS NOT NULL)"))
  @@index([doc_no], map: "idx_po_buys_doc_no_pattern")
  @@schema("public")
}

model po_buy_allocation_logs {
  event_key             String               @unique(map: "idx_po_buy_allocation_logs_event_key") @default(dbgenerated("('POALLOC-'::text || (gen_random_uuid())::text)"))
  action                String
  po_buy_doc_no         String
  purchase_bill_doc_no  String?
  purchase_bill_line_no Int?
  product_code_snapshot String?
  product_name_snapshot String?
  allocated_qty         Decimal              @default(0) @db.Decimal
  allocated_amount      Decimal              @default(0) @db.Decimal
  unit_price_snapshot   Decimal              @default(0) @db.Decimal
  from_remaining_qty    Decimal?             @db.Decimal
  to_remaining_qty      Decimal?             @db.Decimal
  note                  String?
  meta                  Json?
  created_at            DateTime             @default(now()) @db.Timestamptz(6)
  created_by            String?
  id                    BigInt               @id @default(autoincrement())
  po_buy_id             BigInt
  purchase_bill_id      BigInt?
  purchase_bill_item_id BigInt?
  product_id            BigInt?
  po_buys               po_buys              @relation(fields: [po_buy_id], references: [id], onUpdate: NoAction)
  products              products?            @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  purchase_bills        purchase_bills?      @relation(fields: [purchase_bill_id], references: [id], onUpdate: NoAction)
  purchase_bill_items   purchase_bill_items? @relation(fields: [purchase_bill_item_id], references: [id], onUpdate: NoAction)

  @@index([po_buy_id, created_at(sort: Desc)], map: "idx_po_buy_allocation_logs_po_created")
  @@index([product_id], map: "idx_po_buy_allocation_logs_product")
  @@index([purchase_bill_id], map: "idx_po_buy_allocation_logs_purchase_bill")
  @@schema("public")
}

model po_buy_status_logs {
  to_status     String
  note          String?
  meta          Json?
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  created_by    String?
  id            BigInt   @id @default(autoincrement())
  po_buy_id     BigInt
  event_key     String   @unique
  action        String
  from_status   String?
  po_buy_doc_no String
  po_buys       po_buys  @relation(fields: [po_buy_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([po_buy_id, created_at(sort: Desc)], map: "idx_po_buy_status_logs_po_created")
  @@schema("public")
}

model po_sells {
  doc_no            String
  date              DateTime   @db.Date
  expected_delivery DateTime?  @db.Date
  total_amount      Decimal?   @default(0) @db.Decimal
  cut_amount        Decimal?   @default(0) @db.Decimal
  remaining_amount  Decimal?   @default(0) @db.Decimal
  status            String?    @default("Open")
  items             Json?
  notes             String?
  created_at        DateTime?  @default(now()) @db.Timestamptz(6)
  delivery_date     DateTime?  @db.Date
  qty               Decimal?   @db.Decimal
  unit_price        Decimal?   @db.Decimal
  remaining_qty     Decimal?   @db.Decimal
  purpose           String?
  note              String?
  created_by        String?
  updated_at        DateTime?  @db.Timestamptz(6)
  require_delivery  Boolean?   @default(true)
  updated_by        String?
  version           Int?       @default(1)
  customer_id       BigInt?
  branch_id         BigInt?
  warehouse_id      BigInt?
  id                BigInt     @id @default(autoincrement())
  channel_id        BigInt?
  product_id        BigInt?
  subtotal          Decimal?   @default(0) @db.Decimal
  has_vat           Boolean?   @default(false)
  vat_type          String?    @default("NONE")
  vat_rate_percent  Decimal    @default(7.00) @db.Decimal(5, 2)
  vat_amount        Decimal?   @default(0) @db.Decimal
  customers         customers? @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  po_sell_allocation_logs po_sell_allocation_logs[]
  sales_bill_po_sell_allocations sales_bill_po_sell_allocations[]

  @@index([doc_no], map: "idx_po_sells_doc_no")
  @@schema("public")
}

model production_inputs {
  date                                                         DateTime           @db.Date
  qty                                                          Decimal            @db.Decimal
  unit_cost                                                    Decimal?           @default(0) @db.Decimal
  total_cost                                                   Decimal?           @default(0) @db.Decimal
  source                                                       String?
  notes                                                        String?
  created_at                                                   DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at                                                   DateTime?          @db.Timestamptz(6)
  updated_by                                                   String?
  version                                                      Int?               @default(1)
  id                                                           BigInt             @id @default(autoincrement())
  order_id                                                     BigInt?
  product_id                                                   BigInt?
  doc_no                                                       String
  status                                                       String             @default("active")
  source_warehouse_id                                          BigInt?
  wip_warehouse_id                                             BigInt?
  lot_no                                                       String?
  wac_unit_cost                                                Decimal?           @db.Decimal
  reversed_at                                                  DateTime?          @db.Timestamptz(6)
  reversed_by                                                  String?
  reverse_reason                                               String?
  reversal_doc_no                                              String?
  production_orders                                            production_orders? @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  products                                                     products?          @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  warehouses_production_inputs_source_warehouse_idTowarehouses warehouses?        @relation("production_inputs_source_warehouse_idTowarehouses", fields: [source_warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  warehouses_production_inputs_wip_warehouse_idTowarehouses    warehouses?        @relation("production_inputs_wip_warehouse_idTowarehouses", fields: [wip_warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([order_id], map: "idx_pi_order")
  @@index([order_id, status], map: "idx_production_inputs_order_status")
  @@index([doc_no], map: "idx_production_inputs_doc_no")
  @@index([reversal_doc_no], map: "idx_production_inputs_reversal_doc_no", where: raw("(reversal_doc_no IS NOT NULL)"))
  @@index([status], map: "idx_production_inputs_status")
  @@index([source_warehouse_id, wip_warehouse_id], map: "idx_production_inputs_source_wip_warehouse")
  @@schema("public")
}

model production_orders {
  doc_no                       String
  date                         DateTime                       @db.Date
  qty_planned                  Decimal?                       @default(0) @db.Decimal
  status                       String?                        @default("Open")
  total_input_cost             Decimal?                       @default(0) @db.Decimal
  total_output_value           Decimal?                       @default(0) @db.Decimal
  variance                     Decimal?                       @default(0) @db.Decimal
  closed_at                    DateTime?                      @db.Timestamptz(6)
  closed_by                    String?
  notes                        String?
  created_at                   DateTime?                      @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime?                      @db.Timestamptz(6)
  updated_by                   String?
  version                      Int?                           @default(1)
  production_type              String?
  target_lot_no                String?
  shift                        String?
  supervisor_name              String?
  operator_name                String?
  planned_input_qty            Decimal?                       @default(0) @db.Decimal
  planned_output_qty           Decimal?                       @default(0) @db.Decimal
  cost_allocation_method       String?
  normal_loss_percent          Decimal?                       @default(0) @db.Decimal
  branch_id                    BigInt?
  warehouse_id                 BigInt?
  warehouse_from_id            BigInt?
  warehouse_wip_id             BigInt?
  warehouse_to_id              BigInt?
  warehouse_return_id          BigInt?
  id                           BigInt                         @id @default(autoincrement())
  machine_id                   BigInt?
  product_id                   BigInt?
  production_line_id           BigInt?
  process_costs                process_costs[]
  production_inputs            production_inputs[]
  production_order_status_logs production_order_status_logs[]
  branches                     branches?                      @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  production_machines          production_machines?           @relation(fields: [machine_id], references: [id])
  products                     products?                      @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  production_lines             production_lines?              @relation(fields: [production_line_id], references: [id])
  warehouses                   warehouses?                    @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  production_outputs           production_outputs[]

  @@index([machine_id])
  @@index([production_line_id])
  @@index([status, date])
  @@index([date(sort: Desc), doc_no(sort: Desc)], map: "idx_production_orders_active_date_doc", where: raw("(status <> 'Cancelled'::text)"))
  @@index([branch_id, date(sort: Desc), doc_no(sort: Desc)], map: "idx_production_orders_active_branch_date_doc", where: raw("((branch_id IS NOT NULL) AND (status <> 'Cancelled'::text))"))
  @@index([machine_id, date(sort: Desc), doc_no(sort: Desc)], map: "idx_production_orders_active_machine_date_doc", where: raw("((machine_id IS NOT NULL) AND (status <> 'Cancelled'::text))"))
  @@index([status, date(sort: Desc), doc_no(sort: Desc)], map: "idx_production_orders_status_date_doc", where: raw("(status IS NOT NULL)"))
  @@schema("public")
}

model production_order_status_logs {
  event_key         String            @unique(map: "idx_production_order_status_logs_event_key") @default(dbgenerated("('POSTATUS-'::text || (gen_random_uuid())::text)"))
  action            String
  order_doc_no      String
  from_status       String?
  to_status         String
  note              String?
  meta              Json?
  created_at        DateTime          @default(now()) @db.Timestamptz(6)
  created_by        String?
  id                BigInt            @id @default(autoincrement())
  order_id          BigInt
  production_orders production_orders @relation(fields: [order_id], references: [id], onUpdate: NoAction)

  @@index([order_id, created_at(sort: Desc)], map: "idx_production_order_status_logs_order_created")
  @@index([order_doc_no], map: "idx_production_order_status_logs_doc_no")
  @@index([action], map: "idx_production_order_status_logs_action")
  @@schema("public")
}

model process_costs {
  date                  DateTime          @db.Date
  cost_type             String
  amount                Decimal           @default(0) @db.Decimal
  include_in_production Boolean           @default(true)
  status                String            @default("active")
  notes                 String?
  created_by            String?
  created_at            DateTime          @default(now()) @db.Timestamptz(6)
  updated_at            DateTime          @default(now()) @db.Timestamptz(6)
  updated_by            String?
  version               Int               @default(1)
  id                    BigInt            @id @default(autoincrement())
  production_order_id   BigInt
  production_orders     production_orders @relation(fields: [production_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([date])
  @@index([status])
  @@index([production_order_id], map: "process_costs_order_idx")
  @@index([production_order_id, status, include_in_production], map: "idx_process_costs_order_status_include")
  @@schema("public")
}

model production_outputs {
  date                         DateTime                      @db.Date
  qty                          Decimal                       @db.Decimal
  unit_cost                    Decimal?                      @default(0) @db.Decimal
  total_cost                   Decimal?                      @default(0) @db.Decimal
  output_type                  String?                       @default("PO2")
  notes                        String?
  created_at                   DateTime?                     @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime?                     @db.Timestamptz(6)
  updated_by                   String?
  version                      Int?                          @default(1)
  output_status                String?
  id                           BigInt                        @id @default(autoincrement())
  order_id                     BigInt?
  output_category              BigInt?
  product_id                   BigInt?
  doc_no                       String
  status                       String                        @default("active")
  category_code                String?
  destination_warehouse_id     BigInt?
  lot_no                       String?
  source_wip_qty               Decimal?                      @db.Decimal
  reversed_at                  DateTime?                     @db.Timestamptz(6)
  reversed_by                  String?
  reverse_reason               String?
  reversal_doc_no              String?
  warehouses                   warehouses?                   @relation(fields: [destination_warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  production_orders            production_orders?            @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  production_output_categories production_output_categories? @relation(fields: [output_category], references: [id], onDelete: Restrict)
  products                     products?                     @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([order_id], map: "idx_po_order")
  @@index([order_id, status], map: "idx_production_outputs_order_status")
  @@index([doc_no], map: "idx_production_outputs_doc_no")
  @@index([output_category])
  @@index([reversal_doc_no], map: "idx_production_outputs_reversal_doc_no", where: raw("(reversal_doc_no IS NOT NULL)"))
  @@index([status], map: "idx_production_outputs_status")
  @@index([category_code], map: "idx_production_outputs_category_code")
  @@index([destination_warehouse_id], map: "idx_production_outputs_destination_warehouse")
  @@schema("public")
}

model production_output_categories {
  code               String               @unique
  name_th            String
  name_en            String?
  stock_effect       String               @default("stock_in")
  available_for_sale Boolean              @default(true)
  sort_order         Int                  @default(0)
  active             Boolean              @default(true)
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  id                 BigInt               @id @default(autoincrement())
  production_outputs production_outputs[]

  @@index([active])
  @@index([sort_order, code], map: "production_output_categories_sort_idx")
  @@schema("public")
}

model products {
  code                                                            String                            @unique(map: "uq_products_code")
  name                                                            String
  unit                                                            String?                           @default("kg")
  metal_group                                                     String?
  std_price                                                       Decimal?                          @default(0) @db.Decimal
  std_cost                                                        Decimal?                          @default(0) @db.Decimal
  active                                                          Boolean?                          @default(true)
  created_at                                                      DateTime?                         @default(now()) @db.Timestamptz(6)
  updated_at                                                      DateTime?                         @db.Timestamptz(6)
  updated_by                                                      String?
  version                                                         Int?                              @default(1)
  type                                                            String?
  grade                                                           String?
  id                                                              BigInt                            @id @default(autoincrement())
  image_names                                                     String[]                          @default([])
  image_storage_key                                               String?
  image_thumbnail_storage_key                                     String?
  grade_adjustments                                               grade_adjustments[]
  grade_adjustments_grade_adjustments_source_product_idToproducts grade_adjustments[]               @relation("grade_adjustments_source_product_idToproducts")
  grade_adjustments_grade_adjustments_target_product_idToproducts grade_adjustments[]               @relation("grade_adjustments_target_product_idToproducts")
  po_buy_allocation_logs                                          po_buy_allocation_logs[]
  po_sell_allocation_logs                                         po_sell_allocation_logs[]
  production_inputs                                               production_inputs[]
  production_orders                                               production_orders[]
  production_outputs                                              production_outputs[]
  purchase_bill_items                                             purchase_bill_items[]
  sales_bill_lines                                                sales_bill_lines[]
  sales_bill_po_sell_allocations                                  sales_bill_po_sell_allocations[]
  sales_bill_source_allocations                                   sales_bill_source_allocations[]
  stock_cost_pool_entries                                         stock_cost_pool_entries[]
  stock_holds                                                     stock_holds[]
  stock_ledger                                                    stock_ledger[]
  stock_transfer_items                                            stock_transfer_items[]
  trading_allocation_facts                                        trading_allocation_facts[]
  trading_cost_sources                                            trading_cost_sources[]
  trading_deals                                                   trading_deals[]
  weight_ticket_lines                                             weight_ticket_lines[]
  weight_ticket_product_summaries                                 weight_ticket_product_summaries[]
  weight_ticket_usage_logs                                        weight_ticket_usage_logs[]

  @@index([active(sort: Desc), code, name, id, unit], map: "idx_products_active_code_name")
  @@index([metal_group], map: "idx_products_metal_group")
  @@schema("public")
}

model product_units {
  name       String
  symbol     String?
  active     Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  code       String   @unique
  id         BigInt   @id @default(autoincrement())

  @@index([active])
  @@schema("public")
}

model product_types {
  name       String   @unique
  active     Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  code       String   @unique
  id         BigInt   @id @default(autoincrement())

  @@index([active])
  @@schema("public")
}

model purchase_bills {
  doc_no                            String                              @unique(map: "uq_purchase_bills_doc_no")
  ref_no                            String?
  tax_invoice_no                    String?
  transaction_mode                  String?                             @default("STOCK")
  vat_type                          String?                             @default("EXCLUDE")
  subtotal                          Decimal?                            @default(0) @db.Decimal
  discount                          Decimal?                            @default(0) @db.Decimal
  vat_amount                        Decimal?                            @default(0) @db.Decimal
  total_amount                      Decimal                             @db.Decimal
  paid_amount                       Decimal?                            @default(0) @db.Decimal
  payable_balance                   Decimal?                            @default(0) @db.Decimal
  status                            String?                             @default("unpaid")
  notes                             String?
  created_by                        String?
  date                              DateTime                            @default(now()) @map("created_at") @db.Timestamptz(6)
  updated_at                        DateTime?                           @default(now()) @db.Timestamptz(6)
  license_plate                     String?
  contact_name                      String?
  vat_invoice_received              Boolean?                            @default(false)
  vat_invoice_no                    String?
  vat_invoice_date                  DateTime?                           @db.Date
  vat_invoice_received_at           DateTime?                           @db.Timestamptz(6)
  purchase_source                   String?
  purchase_type                     String?
  discount_total                    Decimal?                            @default(0) @db.Decimal
  has_vat                           Boolean?                            @default(false)
  note                              String?
  updated_by                        String?
  has_wht                           Boolean?                            @default(false)
  wht_amount                        Decimal?                            @default(0) @db.Decimal
  total_cost                        Decimal?                            @db.Decimal
  swap_history                      Json?
  total_commission                  Decimal?                            @default(0) @db.Decimal
  commission_pct                    Decimal?                            @default(0) @db.Decimal
  deduction_qty                     Decimal?                            @default(0) @db.Decimal
  deduction_amount                  Decimal?                            @default(0) @db.Decimal
  version                           Int?                                @default(1)
  vat_rate_percent                  Decimal                             @default(7.00) @db.Decimal(5, 2)
  cancel_note                       String?
  cancelled_at                      DateTime?                           @db.Timestamptz(6)
  cancelled_by                      String?
  supplier_id                       BigInt?
  branch_id                         BigInt?
  warehouse_id                      BigInt?
  sales_id                          BigInt?
  id                                BigInt                              @id @default(autoincrement())
  po_buy_id                         BigInt?
  supplier_name_snapshot            String?
  supplier_tax_id_snapshot          String?
  supplier_address_snapshot         String?
  supplier_phone_snapshot           String?
  supplier_sales_rep_snapshot       String?
  po_buy_allocation_logs            po_buy_allocation_logs[]
  purchase_bill_items               purchase_bill_items[]
  purchase_bill_po_allocations      purchase_bill_po_allocations[]
  purchase_bill_receipt_allocations purchase_bill_receipt_allocations[]
  purchase_bill_status_logs         purchase_bill_status_logs[]
  branches                          branches?                           @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  suppliers                         suppliers?                          @relation(fields: [supplier_id], references: [id], onDelete: NoAction)
  warehouses                        warehouses?                         @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  supplier_advance_allocation_logs  supplier_advance_allocation_logs[]
  supplier_advance_allocations      supplier_advance_allocations[]
  trading_allocation_facts          trading_allocation_facts[]
  trading_deals                     trading_deals[]
  weight_ticket_usage_logs          weight_ticket_usage_logs[]

  @@index([branch_id], map: "idx_pb_branch")
  @@index([date(sort: Desc)], map: "idx_pb_created_at")
  @@index([branch_id, date(sort: Desc), doc_no(sort: Desc)], map: "idx_purchase_bills_branch_created_doc_no", where: raw("(branch_id IS NOT NULL)"))
  @@index([date(sort: Desc), doc_no(sort: Desc)], map: "idx_purchase_bills_created_doc_no")
  @@index([transaction_mode], map: "idx_pb_mode")
  @@index([transaction_mode, date(sort: Desc), doc_no(sort: Desc)], map: "idx_purchase_bills_mode_created_doc_no", where: raw("(transaction_mode IS NOT NULL)"))
  @@index([purchase_source, date(sort: Desc), doc_no(sort: Desc)], map: "idx_purchase_bills_source_created_doc_no", where: raw("(purchase_source IS NOT NULL)"))
  @@index([status], map: "idx_pb_status")
  @@index([status, date(sort: Desc), doc_no(sort: Desc)], map: "idx_purchase_bills_status_created_doc_no", where: raw("(status IS NOT NULL)"))
  @@index([supplier_id], map: "idx_pb_supplier")
  @@index([doc_no], map: "idx_purchase_bills_doc_no")
  @@index([date(sort: Desc), doc_no(sort: Desc)], map: "idx_purchase_bills_active_created_doc_no", where: raw("(status <> ALL (ARRAY['cancelled'::text, 'cancelled_supplier_swap'::text]))"))
  @@index([doc_no], map: "idx_purchase_bills_doc_no_pattern")
  @@schema("public")
}

model purchase_bill_status_logs {
  event_key            String         @unique(map: "idx_purchase_bill_status_logs_event_key")
  action               String
  from_status          String?
  to_status            String
  purchase_bill_doc_no String
  note                 String?
  meta                 Json?
  created_at           DateTime       @default(now()) @db.Timestamptz(6)
  created_by           String?
  id                   BigInt         @id @default(autoincrement())
  purchase_bill_id     BigInt
  purchase_bills       purchase_bills @relation(fields: [purchase_bill_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([purchase_bill_id, created_at(sort: Desc)], map: "idx_purchase_bill_status_logs_bill_created")
  @@schema("public")
}

model purchase_bill_items {
  line_no                           Int
  product_code                      String?
  product_name                      String?
  display_name                      String?
  unit                              String?
  lot_no                            String?
  gross_weight                      Decimal                            @default(0) @db.Decimal
  deduct_weight                     Decimal                            @default(0) @db.Decimal
  qty                               Decimal                            @default(0) @db.Decimal
  price                             Decimal                            @default(0) @db.Decimal
  sales_price                       Decimal                            @default(0) @db.Decimal
  discount                          Decimal                            @default(0) @db.Decimal
  amount                            Decimal                            @default(0) @db.Decimal
  note                              String?
  source_snapshot                   Json?
  created_at                        DateTime                           @default(now()) @db.Timestamptz(6)
  updated_at                        DateTime                           @default(now()) @db.Timestamptz(6)
  id                                BigInt                             @id @default(autoincrement())
  po_buy_id                         BigInt?
  product_id                        BigInt?
  purchase_bill_id                  BigInt
  item_status                       String                             @default("active")
  item_version                      Int                                @default(1)
  superseded_at                     DateTime?                          @db.Timestamptz(6)
  superseded_by                     String?
  superseded_reason                 String?
  po_buy_allocation_logs            po_buy_allocation_logs[]
  po_buys                           po_buys?                           @relation(fields: [po_buy_id], references: [id], onUpdate: NoAction)
  products                          products?                          @relation(fields: [product_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  purchase_bills                    purchase_bills                     @relation(fields: [purchase_bill_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  purchase_bill_po_allocations      purchase_bill_po_allocations?
  purchase_bill_receipt_allocations purchase_bill_receipt_allocations?
  weight_ticket_usage_logs          weight_ticket_usage_logs[]

  @@unique([purchase_bill_id, line_no], map: "uq_purchase_bill_items_active_bill_line", where: raw("(item_status = 'active'::text)"))
  @@index([purchase_bill_id, line_no], map: "idx_purchase_bill_items_active_bill", where: raw("(item_status = 'active'::text)"))
  @@index([purchase_bill_id, item_status, item_version(sort: Desc), line_no], map: "idx_purchase_bill_items_bill_status_version")
  @@index([po_buy_id], map: "idx_purchase_bill_items_po_buy")
  @@index([product_id], map: "idx_purchase_bill_items_product")
  @@index([purchase_bill_id], map: "idx_purchase_bill_items_bill")
  @@schema("public")
}

model purchase_bill_receipt_allocations {
  allocated_qty                    Decimal                         @default(0) @db.Decimal
  allocated_gross_weight           Decimal                         @default(0) @db.Decimal
  allocated_deduct_weight          Decimal                         @default(0) @db.Decimal
  created_at                       DateTime                        @default(now()) @db.Timestamptz(6)
  created_by                       String?
  id                               BigInt                          @id @default(autoincrement())
  purchase_bill_id                 BigInt
  purchase_bill_item_id            BigInt                          @unique
  weight_ticket_id                 BigInt
  weight_ticket_product_summary_id BigInt
  allocation_status                String                          @default("active")
  released_at                      DateTime?                       @db.Timestamptz(6)
  released_by                      String?
  release_reason                   String?
  weight_ticket_product_summaries  weight_ticket_product_summaries @relation(fields: [weight_ticket_product_summary_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "purchase_bill_receipt_allocat_weight_ticket_product_summar_fkey")
  purchase_bills                   purchase_bills                  @relation(fields: [purchase_bill_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  purchase_bill_items              purchase_bill_items             @relation(fields: [purchase_bill_item_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  weight_tickets                   weight_tickets                  @relation(fields: [weight_ticket_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([purchase_bill_id], map: "idx_purchase_bill_receipt_allocations_bill")
  @@index([purchase_bill_id, purchase_bill_item_id], map: "idx_purchase_bill_receipt_allocations_active_bill", where: raw("(allocation_status = 'active'::text)"))
  @@index([weight_ticket_id, weight_ticket_product_summary_id, purchase_bill_id], map: "idx_purchase_bill_receipt_allocations_active_ticket", where: raw("(allocation_status = 'active'::text)"))
  @@index([weight_ticket_id, weight_ticket_product_summary_id, purchase_bill_id], map: "idx_purchase_bill_receipt_allocations_active_ticket_summary", where: raw("(allocation_status = 'active'::text)"))
  @@index([weight_ticket_id], map: "idx_purchase_bill_receipt_allocations_ticket")
  @@index([weight_ticket_product_summary_id], map: "idx_purchase_bill_receipt_allocations_summary")
  @@schema("public")
}

model purchase_bill_po_allocations {
  allocated_qty         Decimal             @default(0) @db.Decimal
  allocated_amount      Decimal             @default(0) @db.Decimal
  unit_price_snapshot   Decimal             @default(0) @db.Decimal
  created_at            DateTime            @default(now()) @db.Timestamptz(6)
  created_by            String?
  id                    BigInt              @id @default(autoincrement())
  po_buy_id             BigInt
  purchase_bill_id      BigInt
  purchase_bill_item_id BigInt              @unique
  allocation_status     String              @default("active")
  released_at           DateTime?           @db.Timestamptz(6)
  released_by           String?
  release_reason        String?
  po_buys               po_buys             @relation(fields: [po_buy_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  purchase_bills        purchase_bills      @relation(fields: [purchase_bill_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  purchase_bill_items   purchase_bill_items @relation(fields: [purchase_bill_item_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([po_buy_id], map: "idx_purchase_bill_po_allocations_po")
  @@index([po_buy_id, purchase_bill_id, purchase_bill_item_id], map: "idx_purchase_bill_po_allocations_active_po", where: raw("(allocation_status = 'active'::text)"))
  @@index([purchase_bill_id, purchase_bill_item_id], map: "idx_purchase_bill_po_allocations_active_bill", where: raw("(allocation_status = 'active'::text)"))
  @@index([purchase_bill_id, purchase_bill_item_id], map: "idx_purchase_bill_po_allocations_active_bill_item", where: raw("(allocation_status = 'active'::text)"))
  @@index([purchase_bill_id], map: "idx_purchase_bill_po_allocations_bill")
  @@schema("public")
}

model purchase_channels {
  name   String
  active Boolean? @default(true)
  code   String   @unique
  id     BigInt   @id @default(autoincrement())

  @@schema("public")
}

model receipt_vouchers {
  doc_no                      String?                       @unique
  date                        DateTime?                     @db.Date
  purchase_bill_doc_no        String?
  seller_name                 String?
  seller_tax_id               String?
  seller_address              String?
  seller_phone                String?
  license_plate               String?
  sales_person                String?
  items                       Json?
  total_qty                   Decimal?                      @default(0) @db.Decimal
  total_amount                Decimal?                      @default(0) @db.Decimal
  amount_in_words             String?
  note                        String?
  payer_signer_name           String?
  receiver_signer_name        String?
  payment_method              String?
  created_by                  String?
  created_at                  DateTime?                     @default(now()) @db.Timestamptz(6)
  updated_by                  String?
  updated_at                  DateTime?                     @db.Timestamptz(6)
  id                          BigInt                        @id @default(autoincrement())
  purchase_bill_id            BigInt?
  status                      String                        @default("active")
  cancel_note                 String?
  cancelled_at                DateTime?                     @db.Timestamptz(6)
  cancelled_by                String?
  receipt_voucher_status_logs receipt_voucher_status_logs[]

  @@index([cancelled_at], map: "idx_receipt_vouchers_cancelled_at", where: raw("(cancelled_at IS NOT NULL)"))
  @@index([date(sort: Desc), doc_no(sort: Desc)], map: "idx_receipt_vouchers_date_doc_no")
  @@index([status], map: "idx_receipt_vouchers_status")
  @@index([doc_no], map: "idx_receipt_vouchers_doc_no_pattern", where: raw("(doc_no IS NOT NULL)"))
  @@schema("public")
}

model receipt_voucher_status_logs {
  event_key              String           @unique @default(dbgenerated("('RVLOG-'::text || (gen_random_uuid())::text)"))
  receipt_voucher_id     BigInt
  receipt_voucher_doc_no String
  action                 String
  from_status            String?
  to_status              String
  total_amount_snapshot  Decimal          @default(0) @db.Decimal
  note                   String?
  meta                   Json?
  created_at             DateTime         @default(now()) @db.Timestamptz(6)
  created_by             String?
  id                     BigInt           @id @default(autoincrement())
  receipt_vouchers       receipt_vouchers @relation(fields: [receipt_voucher_id], references: [id], onUpdate: NoAction)

  @@index([receipt_voucher_id, created_at(sort: Desc)], map: "idx_receipt_voucher_status_logs_voucher_created")
  @@index([receipt_voucher_id, created_at, id], map: "idx_receipt_voucher_status_logs_voucher_created_asc")
  @@index([receipt_voucher_doc_no], map: "idx_receipt_voucher_status_logs_doc_no")
  @@index([action], map: "idx_receipt_voucher_status_logs_action")
  @@schema("public")
}

model receipts {
  doc_no                       String
  date                         DateTime                      @db.Date
  amount                       Decimal                       @db.Decimal
  withholding_tax              Decimal?                      @default(0) @db.Decimal
  fee                          Decimal?                      @default(0) @db.Decimal
  net_amount                   Decimal?                      @default(0) @db.Decimal
  vat_type                     String?
  lines                        Json?
  notes                        String?
  status                       String?                       @default("active")
  created_by                   String?
  created_at                   DateTime?                     @default(now()) @db.Timestamptz(6)
  voucher_id                   String?
  bank_fee                     Decimal?                      @default(0) @db.Decimal
  discount                     Decimal?                      @default(0) @db.Decimal
  method                       String?
  updated_at                   DateTime?                     @db.Timestamptz(6)
  updated_by                   String?
  version                      Int?                          @default(1)
  idempotency_key              String?
  customer_id                  BigInt?
  branch_id                    BigInt?
  id                           BigInt                        @id @default(autoincrement())
  account_id                   BigInt?
  bill_id                      BigInt?
  customer_receipt_allocations customer_receipt_allocations?
  accounts                     accounts?                     @relation(fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  branches                     branches?                     @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customers                    customers?                    @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([bill_id], map: "idx_rcp_bill")
  @@index([date(sort: Desc)], map: "idx_rcp_date")
  @@index([doc_no], map: "idx_receipts_doc_no")
  @@index([idempotency_key], map: "idx_receipts_idempotency", where: raw("(idempotency_key IS NOT NULL)"))
  @@index([voucher_id], map: "idx_receipts_voucher", where: raw("(voucher_id IS NOT NULL)"))
  @@schema("public")
}

model customer_receipts {
  id                           BigInt                         @id @default(autoincrement())
  doc_no                       String                         @unique
  date                         DateTime                       @db.Date
  gross_amount                 Decimal                        @default(0) @db.Decimal
  discount_total               Decimal                        @default(0) @db.Decimal
  bank_fee_total               Decimal                        @default(0) @db.Decimal
  withholding_tax_total        Decimal                        @default(0) @db.Decimal
  net_cash_in                  Decimal                        @default(0) @db.Decimal
  status                       String                         @default("active")
  payment_method_code_snapshot String
  payment_method_name_snapshot String
  customer_code_snapshot       String
  customer_name_snapshot       String
  account_code_snapshot        String
  account_name_snapshot        String
  bank_statement_doc_no        String?
  notes                        String?
  cancel_reason                String?
  cancelled_at                 DateTime?                      @db.Timestamptz(6)
  cancelled_by                 String?
  created_at                   DateTime                       @default(now()) @db.Timestamptz(6)
  created_by                   String?
  updated_at                   DateTime                       @default(now()) @db.Timestamptz(6)
  updated_by                   String?
  version                      Int                            @default(1)
  customer_id                  BigInt
  account_id                   BigInt
  branch_id                    BigInt?
  payment_method_id            BigInt
  bank_statement_id            BigInt?                        @unique
  asset_disposals              asset_disposals[]
  customer_receipt_allocations customer_receipt_allocations[]
  customer_receipt_status_logs customer_receipt_status_logs[]
  accounts                     accounts                       @relation(fields: [account_id], references: [id], onUpdate: NoAction)
  bank_statement               bank_statement?                @relation(fields: [bank_statement_id], references: [id], onUpdate: NoAction)
  branches                     branches?                      @relation(fields: [branch_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  customers                    customers                      @relation(fields: [customer_id], references: [id], onUpdate: NoAction)
  payment_methods              payment_methods                @relation(fields: [payment_method_id], references: [id], onUpdate: NoAction)

  @@index([date(sort: Desc)], map: "idx_customer_receipts_date")
  @@index([customer_id, date(sort: Desc)], map: "idx_customer_receipts_customer_date")
  @@index([account_id, date(sort: Desc)], map: "idx_customer_receipts_account_date")
  @@index([branch_id, date(sort: Desc)], map: "idx_customer_receipts_branch_date", where: raw("(branch_id IS NOT NULL)"))
  @@index([status], map: "idx_customer_receipts_status")
  @@index([bank_statement_id], map: "idx_customer_receipts_bank_statement", where: raw("(bank_statement_id IS NOT NULL)"))
  @@index([date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_customer_receipts_history_order")
  @@schema("public")
}

model customer_receipt_allocations {
  id                         BigInt            @id @default(autoincrement())
  receipt_id                 BigInt
  sales_bill_id              BigInt
  receipt_line_id            BigInt?           @unique
  line_no                    Int
  sales_bill_doc_no_snapshot String
  customer_code_snapshot     String
  receipt_amount             Decimal           @default(0) @db.Decimal
  discount_amount            Decimal           @default(0) @db.Decimal
  withholding_tax_amount     Decimal           @default(0) @db.Decimal
  allocated_ar_amount        Decimal           @default(0) @db.Decimal
  outstanding_before         Decimal           @default(0) @db.Decimal
  outstanding_after          Decimal           @default(0) @db.Decimal
  status                     String            @default("active")
  created_at                 DateTime          @default(now()) @db.Timestamptz(6)
  created_by                 String?
  updated_at                 DateTime          @default(now()) @db.Timestamptz(6)
  updated_by                 String?
  version                    Int               @default(1)
  customer_receipts          customer_receipts @relation(fields: [receipt_id], references: [id], onUpdate: NoAction)
  receipts                   receipts?         @relation(fields: [receipt_line_id], references: [id], onUpdate: NoAction)
  sales_bills                sales_bills       @relation(fields: [sales_bill_id], references: [id], onUpdate: NoAction)

  @@unique([receipt_id, line_no], map: "customer_receipt_allocations_line_unique")
  @@index([receipt_id, line_no], map: "idx_customer_receipt_allocations_receipt")
  @@index([sales_bill_id], map: "idx_customer_receipt_allocations_sales_bill")
  @@index([status], map: "idx_customer_receipt_allocations_status")
  @@index([receipt_line_id], map: "idx_customer_receipt_allocations_receipt_line", where: raw("(receipt_line_id IS NOT NULL)"))
  @@index([sales_bill_id], map: "idx_customer_receipt_allocations_active_sales_bill", where: raw("(status = 'active'::text)"))
  @@schema("public")
}

model customer_receipt_status_logs {
  id                    BigInt            @id @default(autoincrement())
  event_key             String            @unique @default(dbgenerated("('RCPLOG-'::text || (gen_random_uuid())::text)"))
  receipt_id            BigInt
  receipt_doc_no        String
  action                String
  from_status           String?
  to_status             String
  gross_amount_snapshot Decimal           @default(0) @db.Decimal
  net_cash_in_snapshot  Decimal           @default(0) @db.Decimal
  note                  String?
  meta                  Json?
  created_at            DateTime          @default(now()) @db.Timestamptz(6)
  created_by            String?
  customer_receipts     customer_receipts @relation(fields: [receipt_id], references: [id], onUpdate: NoAction)

  @@index([receipt_id, created_at(sort: Desc)], map: "idx_customer_receipt_status_logs_receipt_created")
  @@index([receipt_doc_no], map: "idx_customer_receipt_status_logs_doc_no")
  @@index([action], map: "idx_customer_receipt_status_logs_action")
  @@schema("public")
}

model roles {
  name           String?
  desc           String?
  is_system      Boolean?  @default(false)
  menus          Json?
  actions        Json?
  see_cost       Boolean?  @default(false)
  see_profit     Boolean?  @default(false)
  see_cash       Boolean?  @default(false)
  see_financials Boolean?  @default(false)
  see_opening    Boolean?  @default(false)
  branch_scope   String?
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @db.Timestamptz(6)
  updated_by     String?
  version        Int?      @default(1)
  id             BigInt    @id @default(autoincrement())

  @@schema("public")
}

model roles_config {
  role        String  @id
  permissions Json?
  description String?

  @@schema("public")
}

model sales_bills {
  doc_no                                  String                                    @unique(map: "uq_sales_bills_doc_no")
  date                                    DateTime                                  @db.Date
  ref_no                                  String?
  transaction_mode                        String?                                   @default("STOCK")
  vat_type                                String?                                   @default("EXCLUDE")
  credit_term                             Int?                                      @default(0)
  subtotal                                Decimal?                                  @default(0) @db.Decimal
  discount                                Decimal?                                  @default(0) @db.Decimal
  vat_amount                              Decimal?                                  @default(0) @db.Decimal
  total_amount                            Decimal                                   @db.Decimal
  received_amount                         Decimal?                                  @default(0) @db.Decimal
  receivable_balance                      Decimal?                                  @default(0) @db.Decimal
  cogs_amount                             Decimal?                                  @default(0) @db.Decimal
  gross_profit                            Decimal?                                  @default(0) @db.Decimal
  status                                  String?                                   @default("open")
  items                                   Json?
  notes                                   String?
  created_by                              String?
  created_at                              DateTime?                                 @default(now()) @db.Timestamptz(6)
  updated_at                              DateTime?                                 @default(now()) @db.Timestamptz(6)
  license_plate                           String?
  contact_phone                           String?
  contact_name                            String?
  trading_from_purchase_ids               Json?
  sales_source                            String?
  vat_invoice_issued                      Boolean?                                  @default(false)
  vat_invoice_no                          String?
  vat_invoice_date                        DateTime?                                 @db.Date
  discount_total                          Decimal?                                  @default(0) @db.Decimal
  has_vat                                 Boolean?                                  @default(false)
  salesman                                String?
  note                                    String?
  from_p_sale_no                          String?
  has_wht                                 Boolean?                                  @default(false)
  wht_amount                              Decimal?                                  @default(0) @db.Decimal
  paid_amount                             Decimal?                                  @default(0) @db.Decimal
  total_cost                              Decimal?                                  @db.Decimal
  updated_by                              String?
  total_commission                        Decimal?                                  @default(0) @db.Decimal
  commission_pct                          Decimal?                                  @default(0) @db.Decimal
  version                                 Int?                                      @default(1)
  due_date                                DateTime?                                 @db.Date
  bill_date                               DateTime?                                 @db.Date
  payment_type                            String?
  is_opening                              Boolean?                                  @default(false)
  customer_id                             BigInt?
  branch_id                               BigInt?
  warehouse_id                            BigInt?
  sales_id                                BigInt?
  id                                      BigInt                                    @id @default(autoincrement())
  channel_id                              BigInt?
  from_p_sale_id                          BigInt?
  po_sell_id                              BigInt?
  trading_from_purchase_id                BigInt?
  cancel_note                             String?
  cancelled_at                            DateTime?                                 @db.Timestamptz(6)
  cancelled_by                            String?
  customer_receipt_allocations            customer_receipt_allocations[]
  sales_bill_customer_advance_allocations sales_bill_customer_advance_allocations[]
  sales_bill_lines             sales_bill_lines[]
  po_sell_allocation_logs      po_sell_allocation_logs[]
  sales_bill_po_sell_allocations sales_bill_po_sell_allocations[]
  sales_bill_source_allocations sales_bill_source_allocations[]
  sales_bill_status_logs       sales_bill_status_logs[]
  branches                     branches?                      @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  sales_channels               sales_channels?                @relation(fields: [channel_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customers                    customers?                     @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  warehouses                   warehouses?                    @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  trading_allocation_facts     trading_allocation_facts[]
  trading_deals                trading_deals[]

  @@index([doc_no], map: "idx_sales_bills_doc_no")
  @@index([branch_id], map: "idx_sb_branch")
  @@index([customer_id], map: "idx_sb_customer")
  @@index([date(sort: Desc)], map: "idx_sb_date")
  @@index([transaction_mode], map: "idx_sb_mode")
  @@index([status], map: "idx_sb_status")
  @@index([date(sort: Desc), id(sort: Desc)], map: "idx_sales_bills_customer_receipt_outstanding_queue", where: raw("((receivable_balance > (0)::numeric) AND (status <> ALL (ARRAY['cancelled'::text, 'canceled'::text])))"))
  @@schema("public")
}

model sales_bill_status_logs {
  event_key                   String      @unique(map: "idx_sales_bill_status_logs_event_key") @default(dbgenerated("('SBSTATUS-'::text || (gen_random_uuid())::text)"))
  action                      String
  sales_bill_doc_no           String
  total_amount_snapshot       Decimal     @default(0) @db.Decimal
  received_amount_snapshot    Decimal     @default(0) @db.Decimal
  receivable_balance_snapshot Decimal     @default(0) @db.Decimal
  from_status                 String?
  to_status                   String
  note                        String?
  meta                        Json?
  created_at                  DateTime    @default(now()) @db.Timestamptz(6)
  created_by                  String?
  id                          BigInt      @id @default(autoincrement())
  sales_bill_id               BigInt
  sales_bills                 sales_bills @relation(fields: [sales_bill_id], references: [id], onUpdate: NoAction)

  @@index([sales_bill_id, created_at(sort: Desc)], map: "idx_sales_bill_status_logs_bill_created")
  @@index([sales_bill_doc_no], map: "idx_sales_bill_status_logs_doc_no")
  @@index([action], map: "idx_sales_bill_status_logs_action")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model sales_bill_lines {
  id                                BigInt                            @id @default(autoincrement())
  sales_bill_id                     BigInt
  line_no                           Int
  product_id                        BigInt?
  product_code_snapshot             String
  product_name_snapshot             String
  unit_snapshot                     String?
  gross_weight                      Decimal                           @default(0) @db.Decimal
  deduct_weight                     Decimal                           @default(0) @db.Decimal
  net_weight                        Decimal                           @default(0) @db.Decimal
  qty                               Decimal                           @default(0) @db.Decimal
  unit_price                        Decimal                           @default(0) @db.Decimal
  discount_amount                   Decimal                           @default(0) @db.Decimal
  line_amount                       Decimal                           @default(0) @db.Decimal
  vat_amount                        Decimal                           @default(0) @db.Decimal
  status                            String                            @default("active")
  notes                             String?
  meta                              Json?
  created_at                        DateTime                          @default(now()) @db.Timestamptz(6)
  created_by                        String?
  updated_at                        DateTime                          @default(now()) @db.Timestamptz(6)
  updated_by                        String?
  version                           Int                               @default(1)
  products                          products?                         @relation(fields: [product_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  sales_bills                       sales_bills                       @relation(fields: [sales_bill_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  po_sell_allocation_logs           po_sell_allocation_logs[]
  sales_bill_po_sell_allocations    sales_bill_po_sell_allocations[]
  sales_bill_source_allocations     sales_bill_source_allocations[]

  @@unique([sales_bill_id, line_no], map: "uq_sales_bill_lines_bill_line")
  @@index([sales_bill_id, status], map: "idx_sales_bill_lines_bill_status")
  @@index([product_id, status], map: "idx_sales_bill_lines_product_status", where: raw("(product_id IS NOT NULL)"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model sales_bill_source_allocations {
  id                      BigInt            @id @default(autoincrement())
  sales_bill_id           BigInt
  sales_bill_line_id      BigInt?
  sales_line_no           Int
  source_type             String
  source_doc_no           String
  source_line_no          Int?
  source_id               BigInt?
  weight_ticket_id        BigInt?
  stock_issue_id          BigInt?
  product_id              BigInt?
  product_code_snapshot   String
  product_name_snapshot   String
  allocated_qty           Decimal           @default(0) @db.Decimal
  allocated_gross_weight  Decimal           @default(0) @db.Decimal
  allocated_deduct_weight Decimal           @default(0) @db.Decimal
  allocated_net_weight    Decimal           @default(0) @db.Decimal
  movement_owner          String
  stock_ledger_ref_type   String?
  status                  String            @default("active")
  notes                   String?
  meta                    Json?
  created_at              DateTime          @default(now()) @db.Timestamptz(6)
  created_by              String?
  updated_at              DateTime          @default(now()) @db.Timestamptz(6)
  updated_by              String?
  version                 Int               @default(1)
  products                products?         @relation(fields: [product_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  sales_bills             sales_bills       @relation(fields: [sales_bill_id], references: [id], onUpdate: NoAction)
  sales_bill_lines        sales_bill_lines? @relation(fields: [sales_bill_line_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  stock_issues            stock_issues?     @relation(fields: [stock_issue_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  weight_tickets          weight_tickets?   @relation(fields: [weight_ticket_id], references: [id], onDelete: Restrict, onUpdate: NoAction)

  @@index([sales_bill_id, sales_line_no, status], map: "idx_sales_bill_source_allocations_bill_line_status")
  @@index([source_type, source_doc_no, status], map: "idx_sales_bill_source_allocations_source_status")
  @@index([weight_ticket_id, status], map: "idx_sales_bill_source_allocations_wto", where: raw("(weight_ticket_id IS NOT NULL)"))
  @@index([stock_issue_id, status], map: "idx_sales_bill_source_allocations_psale", where: raw("(stock_issue_id IS NOT NULL)"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model sales_bill_po_sell_allocations {
  id                    BigInt            @id @default(autoincrement())
  sales_bill_id         BigInt
  sales_bill_line_id    BigInt?
  sales_line_no         Int
  allocation_type       String
  po_sell_id            BigInt?
  po_sell_doc_no        String?
  po_sell_line_no       Int?
  product_id            BigInt?
  product_code_snapshot String
  product_name_snapshot String
  allocated_qty         Decimal           @default(0) @db.Decimal
  unit_price            Decimal           @default(0) @db.Decimal
  allocated_amount      Decimal           @default(0) @db.Decimal
  status                String            @default("active")
  notes                 String?
  meta                  Json?
  created_at            DateTime          @default(now()) @db.Timestamptz(6)
  created_by            String?
  updated_at            DateTime          @default(now()) @db.Timestamptz(6)
  updated_by            String?
  version               Int               @default(1)
  po_sells              po_sells?         @relation(fields: [po_sell_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  products              products?         @relation(fields: [product_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  sales_bills           sales_bills       @relation(fields: [sales_bill_id], references: [id], onUpdate: NoAction)
  sales_bill_lines      sales_bill_lines? @relation(fields: [sales_bill_line_id], references: [id], onDelete: Restrict, onUpdate: NoAction)

  @@index([sales_bill_id, sales_line_no, status], map: "idx_sales_bill_po_sell_allocations_bill_line_status")
  @@index([po_sell_id, status], map: "idx_sales_bill_po_sell_allocations_po_status", where: raw("(po_sell_id IS NOT NULL)"))
  @@index([allocation_type, status], map: "idx_sales_bill_po_sell_allocations_type_status")
  @@schema("public")
}

model po_sell_allocation_logs {
  event_key              String            @unique(map: "idx_po_sell_allocation_logs_event_key") @default(dbgenerated("('POSALLOC-'::text || (gen_random_uuid())::text)"))
  action                 String
  po_sell_doc_no         String
  sales_bill_doc_no      String?
  sales_bill_line_no     Int?
  product_code_snapshot  String?
  product_name_snapshot  String?
  allocated_qty          Decimal           @default(0) @db.Decimal
  allocated_amount       Decimal           @default(0) @db.Decimal
  unit_price_snapshot    Decimal           @default(0) @db.Decimal
  from_remaining_qty     Decimal?          @db.Decimal
  to_remaining_qty       Decimal?          @db.Decimal
  note                   String?
  meta                   Json?
  created_at             DateTime          @default(now()) @db.Timestamptz(6)
  created_by             String?
  id                     BigInt            @id @default(autoincrement())
  po_sell_id             BigInt
  sales_bill_id          BigInt?
  sales_bill_line_id     BigInt?
  product_id             BigInt?
  po_sells               po_sells          @relation(fields: [po_sell_id], references: [id], onUpdate: NoAction)
  products               products?         @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  sales_bills            sales_bills?      @relation(fields: [sales_bill_id], references: [id], onUpdate: NoAction)
  sales_bill_lines       sales_bill_lines? @relation(fields: [sales_bill_line_id], references: [id], onUpdate: NoAction)

  @@index([po_sell_id, created_at(sort: Desc)], map: "idx_po_sell_allocation_logs_po_created")
  @@index([product_id], map: "idx_po_sell_allocation_logs_product")
  @@index([sales_bill_id], map: "idx_po_sell_allocation_logs_sales_bill")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model sales_bill_customer_advance_allocations {
  id                      BigInt      @id @default(autoincrement())
  sales_bill_id           BigInt
  customer_advance_doc_no String
  customer_id             BigInt?
  customer_code_snapshot  String
  customer_name_snapshot  String
  allocated_amount        Decimal     @default(0) @db.Decimal
  outstanding_before      Decimal     @default(0) @db.Decimal
  outstanding_after       Decimal     @default(0) @db.Decimal
  status                  String      @default("active")
  notes                   String?
  meta                    Json?
  created_at              DateTime    @default(now()) @db.Timestamptz(6)
  created_by              String?
  updated_at              DateTime    @default(now()) @db.Timestamptz(6)
  updated_by              String?
  version                 Int         @default(1)
  customers               customers?  @relation(fields: [customer_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  sales_bills             sales_bills @relation(fields: [sales_bill_id], references: [id], onUpdate: NoAction)

  @@index([sales_bill_id, status], map: "idx_sales_bill_customer_advance_allocations_bill_status")
  @@index([customer_advance_doc_no, status], map: "idx_sales_bill_customer_advance_allocations_advance_status")
  @@index([customer_id, status], map: "idx_sales_bill_customer_advance_allocations_customer_status", where: raw("(customer_id IS NOT NULL)"))
  @@schema("public")
}

model sales_channels {
  name        String
  active      Boolean?      @default(true)
  code        String        @unique
  id          BigInt        @id @default(autoincrement())
  sales_bills sales_bills[]

  @@schema("public")
}

model salespersons {
  code       String    @unique(map: "uq_salespersons_code")
  name       String
  phone      String?
  email      String?
  active     Boolean?  @default(true)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
  updated_by String?
  version    Int?      @default(1)
  id         BigInt    @id @default(autoincrement())

  @@schema("public")
}

model weight_tickets {
  doc_no                            String                              @unique
  doc_type                          String
  status                            String                              @default("received")
  document_date                     DateTime                            @db.Date
  party_name                        String
  vehicle_no                        String
  remark                            String?
  entered_by                        String?
  image_count                       Int                                 @default(0)
  vehicle_image_count               Int                                 @default(0)
  vehicle_image_names               String[]                            @default([])
  gross_weight                      Decimal                             @default(0) @db.Decimal
  deduct_weight                     Decimal                             @default(0) @db.Decimal
  net_weight                        Decimal                             @default(0) @db.Decimal
  cancel_note                       String?
  cancelled_at                      DateTime?                           @db.Timestamptz(6)
  cancelled_by                      String?
  created_at                        DateTime                            @default(now()) @db.Timestamptz(6)
  created_by                        String?
  updated_at                        DateTime                            @default(now()) @db.Timestamptz(6)
  updated_by                        String?
  branch_id                         BigInt
  supplier_id                       BigInt?
  customer_id                       BigInt?
  id                                BigInt                              @id @default(autoincrement())
  container_deduction_weight        Decimal                             @default(0) @db.Decimal
  warehouse_name                    String?
  purchase_bill_receipt_allocations purchase_bill_receipt_allocations[]
  sales_bill_source_allocations     sales_bill_source_allocations[]
  stock_holds                       stock_holds[]
  weight_ticket_lines               weight_ticket_lines[]
  weight_ticket_notification_logs   weight_ticket_notification_logs[]
  weight_ticket_product_summaries   weight_ticket_product_summaries[]
  weight_ticket_status_logs         weight_ticket_status_logs[]
  weight_ticket_usage_logs          weight_ticket_usage_logs[]
  branches                          branches                            @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customers                         customers?                          @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  suppliers                         suppliers?                          @relation(fields: [supplier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([branch_id], map: "idx_weight_tickets_branch")
  @@index([branch_id, document_date(sort: Desc), doc_no(sort: Desc)], map: "idx_weight_tickets_wti_branch_bill_options", where: raw("((cancelled_at IS NULL) AND (doc_type = 'WTI'::text) AND (status = ANY (ARRAY['received'::text, 'partially_billed'::text])))"))
  @@index([customer_id], map: "idx_weight_tickets_customer")
  @@index([doc_no], map: "idx_weight_tickets_doc_no")
  @@index([doc_type], map: "idx_weight_tickets_doc_type")
  @@index([document_date(sort: Desc)], map: "idx_weight_tickets_date")
  @@index([document_date(sort: Desc), doc_no(sort: Desc)], map: "idx_weight_tickets_wti_bill_options", where: raw("((cancelled_at IS NULL) AND (doc_type = 'WTI'::text) AND (status = ANY (ARRAY['received'::text, 'partially_billed'::text])))"))
  @@index([status], map: "idx_weight_tickets_status")
  @@index([supplier_id], map: "idx_weight_tickets_supplier")
  @@schema("public")
}

model weight_ticket_lines {
  line_no                             Int
  product_name                        String
  gross_weight                        Decimal                              @default(0) @db.Decimal
  deduct_weight                       Decimal                              @default(0) @db.Decimal
  net_weight                          Decimal                              @default(0) @db.Decimal
  deduction_mode                      String
  deduction_value                     Decimal?                             @default(0) @db.Decimal
  impurity_name                       String?
  note                                String?
  image_count                         Int                                  @default(0)
  image_names                         String[]                             @default([])
  created_at                          DateTime                             @default(now()) @db.Timestamptz(6)
  id                                  BigInt                               @id @default(autoincrement())
  impurity_id                         BigInt?
  product_id                          BigInt
  weight_ticket_id                    BigInt
  warehouse_id                        BigInt?
  container_deduction_weight          Decimal                              @default(0) @db.Decimal
  parent_line_no                      Int?
  impurity_source_line_no             Int?
  stock_holds                         stock_holds[]
  impurities                          impurities?                          @relation(fields: [impurity_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products                            products                             @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  warehouses                          warehouses?                          @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  weight_tickets                      weight_tickets                       @relation(fields: [weight_ticket_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  weight_ticket_product_summary_lines weight_ticket_product_summary_lines?

  @@unique([weight_ticket_id, line_no], map: "uq_weight_ticket_lines_ticket_line")
  @@index([weight_ticket_id, impurity_source_line_no], map: "idx_weight_ticket_lines_impurity_source_line")
  @@index([weight_ticket_id, parent_line_no], map: "idx_weight_ticket_lines_parent_line")
  @@index([impurity_id], map: "idx_weight_ticket_lines_impurity")
  @@index([product_id], map: "idx_weight_ticket_lines_product")
  @@index([warehouse_id], map: "idx_weight_ticket_lines_warehouse")
  @@index([weight_ticket_id], map: "idx_weight_ticket_lines_ticket")
  @@schema("public")
}

model weight_ticket_product_summaries {
  product_name                        String
  line_count                          Int                                   @default(0)
  gross_weight                        Decimal                               @default(0) @db.Decimal
  deduct_weight                       Decimal                               @default(0) @db.Decimal
  net_weight                          Decimal                               @default(0) @db.Decimal
  billed_weight                       Decimal                               @default(0) @db.Decimal
  remaining_weight                    Decimal                               @default(0) @db.Decimal
  has_mixed_deduction_profiles        Boolean                               @default(false)
  created_at                          DateTime                              @default(now()) @db.Timestamptz(6)
  updated_at                          DateTime                              @default(now()) @db.Timestamptz(6)
  id                                  BigInt                                @id @default(autoincrement())
  product_id                          BigInt
  weight_ticket_id                    BigInt
  container_deduction_weight          Decimal                               @default(0) @db.Decimal
  purchase_bill_receipt_allocations   purchase_bill_receipt_allocations[]
  products                            products                              @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  weight_tickets                      weight_tickets                        @relation(fields: [weight_ticket_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  weight_ticket_product_summary_lines weight_ticket_product_summary_lines[]
  weight_ticket_usage_logs            weight_ticket_usage_logs[]

  @@unique([weight_ticket_id, product_id], map: "uq_weight_ticket_product_summaries_ticket_product")
  @@index([product_id], map: "idx_weight_ticket_product_summaries_product")
  @@index([weight_ticket_id], map: "idx_weight_ticket_product_summaries_ticket")
  @@schema("public")
}

model weight_ticket_status_logs {
  event_key              String         @unique(map: "idx_weight_ticket_status_logs_event_key") @default(dbgenerated("('WTSTATUS-'::text || (gen_random_uuid())::text)"))
  action                 String
  weight_ticket_doc_no   String
  doc_type               String
  from_status            String?
  to_status              String
  gross_weight_snapshot  Decimal        @default(0) @db.Decimal
  deduct_weight_snapshot Decimal        @default(0) @db.Decimal
  net_weight_snapshot    Decimal        @default(0) @db.Decimal
  note                   String?
  meta                   Json?
  created_at             DateTime       @default(now()) @db.Timestamptz(6)
  created_by             String?
  id                     BigInt         @id @default(autoincrement())
  weight_ticket_id       BigInt
  weight_tickets         weight_tickets @relation(fields: [weight_ticket_id], references: [id], onUpdate: NoAction)

  @@index([weight_ticket_id, created_at(sort: Desc)], map: "idx_weight_ticket_status_logs_ticket_created")
  @@index([weight_ticket_doc_no], map: "idx_weight_ticket_status_logs_doc_no")
  @@schema("public")
}

model weight_ticket_usage_logs {
  event_key                        String                           @unique(map: "idx_weight_ticket_usage_logs_event_key") @default(dbgenerated("('WTUSE-'::text || (gen_random_uuid())::text)"))
  action                           String
  weight_ticket_doc_no             String
  doc_type                         String                           @default("WTI")
  target_type                      String
  target_id                        BigInt?
  target_doc_no                    String?
  target_line_no                   Int?
  product_code_snapshot            String?
  product_name_snapshot            String?
  allocated_qty                    Decimal                          @default(0) @db.Decimal
  allocated_gross_weight           Decimal                          @default(0) @db.Decimal
  allocated_deduct_weight          Decimal                          @default(0) @db.Decimal
  allocated_net_weight             Decimal                          @default(0) @db.Decimal
  from_remaining_weight            Decimal?                         @db.Decimal
  to_remaining_weight              Decimal?                         @db.Decimal
  note                             String?
  meta                             Json?
  created_at                       DateTime                         @default(now()) @db.Timestamptz(6)
  created_by                       String?
  id                               BigInt                           @id @default(autoincrement())
  weight_ticket_id                 BigInt
  weight_ticket_product_summary_id BigInt?
  product_id                       BigInt?
  purchase_bill_id                 BigInt?
  purchase_bill_item_id            BigInt?
  products                         products?                        @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  purchase_bills                   purchase_bills?                  @relation(fields: [purchase_bill_id], references: [id], onUpdate: NoAction)
  purchase_bill_items              purchase_bill_items?             @relation(fields: [purchase_bill_item_id], references: [id], onUpdate: NoAction)
  weight_tickets                   weight_tickets                   @relation(fields: [weight_ticket_id], references: [id], onUpdate: NoAction)
  weight_ticket_product_summaries  weight_ticket_product_summaries? @relation(fields: [weight_ticket_product_summary_id], references: [id], onUpdate: NoAction)

  @@index([weight_ticket_id, created_at(sort: Desc)], map: "idx_weight_ticket_usage_logs_ticket_created")
  @@index([weight_ticket_product_summary_id], map: "idx_weight_ticket_usage_logs_summary")
  @@index([target_type, target_id], map: "idx_weight_ticket_usage_logs_target")
  @@index([target_type, target_doc_no, action], map: "idx_weight_ticket_usage_logs_sales_bill_doc", where: raw("(target_type = 'SALES_BILL'::text)"))
  @@index([purchase_bill_id], map: "idx_weight_ticket_usage_logs_purchase_bill")
  @@index([product_id], map: "idx_weight_ticket_usage_logs_product")
  @@schema("public")
}

model weight_ticket_product_summary_lines {
  created_at                      DateTime                        @default(now()) @db.Timestamptz(6)
  id                              BigInt                          @id @default(autoincrement())
  summary_id                      BigInt
  weight_ticket_line_id           BigInt                          @unique(map: "uq_weight_ticket_product_summary_lines_line")
  weight_ticket_product_summaries weight_ticket_product_summaries @relation(fields: [summary_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  weight_ticket_lines             weight_ticket_lines             @relation(fields: [weight_ticket_line_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([summary_id, weight_ticket_line_id], map: "uq_weight_ticket_product_summary_lines_summary_line")
  @@index([summary_id], map: "idx_weight_ticket_product_summary_lines_summary")
  @@schema("public")
}

model stock_adjustments {
  doc_no                   String?   @unique(map: "uq_stock_adjustments_doc_no", where: raw("(doc_no IS NOT NULL)"))
  date                     DateTime? @db.Date
  lot_no                   String?
  system_qty               Decimal?  @default(0) @db.Decimal
  counted_qty              Decimal?  @default(0) @db.Decimal
  diff_qty                 Decimal?  @default(0) @db.Decimal
  adjust_type              String?
  reason                   String?
  remark                   String?
  unit_cost_used           Decimal?  @default(0) @db.Decimal
  value_note               Decimal?  @default(0) @db.Decimal
  status                   String?   @default("posted")
  created_by               String?
  created_at               DateTime? @default(now()) @db.Timestamptz(6)
  reversed_at              DateTime? @db.Timestamptz(6)
  reversed_by              String?
  updated_at               DateTime? @db.Timestamptz(6)
  updated_by               String?
  version                  Int?      @default(1)
  branch_id                BigInt?
  warehouse_id             BigInt?
  id                       BigInt    @id @default(autoincrement())
  product_id               BigInt?
  stock_ledger_id          BigInt?
  output_category          String?
  on_hold_qty              Decimal?  @default(0) @db.Decimal
  ready_qty_snapshot       Decimal?  @default(0) @db.Decimal
  accounting_impact_policy String?   @default("NOTE_ONLY")

  @@index([branch_id], map: "idx_stock_adjustments_branch")
  @@index([branch_id, date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_adjustments_branch_date_created", where: raw("(branch_id IS NOT NULL)"))
  @@index([date(sort: Desc)], map: "idx_stock_adjustments_date")
  @@index([date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_adjustments_list_date_created")
  @@index([adjust_type, date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_adjustments_type_date_created", where: raw("(adjust_type IS NOT NULL)"))
  @@index([product_id], map: "idx_stock_adjustments_product")
  @@index([product_id, date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_adjustments_product_date_created", where: raw("(product_id IS NOT NULL)"))
  @@index([output_category], map: "idx_stock_adjustments_output_category")
  @@index([status], map: "idx_stock_adjustments_status")
  @@index([doc_no], map: "idx_stock_adjustments_doc_no_pattern", where: raw("(doc_no IS NOT NULL)"))
  @@schema("public")
}

model stock_issues {
  doc_no                        String
  date                          DateTime                        @db.Date
  total_cost                    Decimal?                        @default(0) @db.Decimal
  total_est_amount              Decimal?                        @default(0) @db.Decimal
  status                        String?                         @default("pending")
  items                         Json?
  notes                         String?
  created_by                    String?
  created_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  customer_id                   BigInt?
  branch_id                     BigInt?
  warehouse_id                  BigInt?
  id                            BigInt                          @id @default(autoincrement())
  converted_to_bill_id          BigInt?
  sales_bill_source_allocations sales_bill_source_allocations[]
  stock_issue_status_logs       stock_issue_status_logs[]
  branches                      branches?                       @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customers                     customers?                      @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  warehouses                    warehouses?                     @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([converted_to_bill_id, status], map: "idx_stock_issues_converted_bill", where: raw("(converted_to_bill_id IS NOT NULL)"))
  @@index([date(sort: Desc), doc_no(sort: Desc)], map: "idx_stock_issues_date_doc")
  @@index([doc_no], map: "idx_stock_issues_doc_no")
  @@index([status, date(sort: Desc), doc_no(sort: Desc)], map: "idx_stock_issues_status_date_doc")
  @@schema("public")
}

model stock_issue_status_logs {
  id                        BigInt       @id @default(autoincrement())
  event_key                 String       @unique
  stock_issue_id            BigInt
  stock_issue_doc_no        String
  action                    String
  from_status               String?
  to_status                 String
  total_cost_snapshot       Decimal      @default(0) @db.Decimal
  total_est_amount_snapshot Decimal      @default(0) @db.Decimal
  note                      String?
  meta                      Json?
  created_at                DateTime     @default(now()) @db.Timestamptz(6)
  created_by                String?
  stock_issues              stock_issues @relation(fields: [stock_issue_id], references: [id], onUpdate: NoAction)

  @@index([stock_issue_id, created_at(sort: Desc)], map: "idx_stock_issue_status_logs_issue_created")
  @@index([stock_issue_doc_no], map: "idx_stock_issue_status_logs_doc_no")
  @@index([action], map: "idx_stock_issue_status_logs_action")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model stock_holds {
  hold_key               String               @unique @default(dbgenerated("('STH-'::text || (gen_random_uuid())::text)"))
  source_type            String               @default("WTO")
  source_doc_no          String
  source_line_no         Int?
  qty                    Decimal              @default(0) @db.Decimal
  status                 String               @default("active")
  held_at                DateTime             @default(now()) @db.Timestamptz(6)
  released_at            DateTime?            @db.Timestamptz(6)
  consumed_at            DateTime?            @db.Timestamptz(6)
  cancelled_at           DateTime?            @db.Timestamptz(6)
  released_by            String?
  consumed_by            String?
  cancelled_by           String?
  release_reason         String?
  consumed_by_ref_type   String?
  consumed_by_ref_no     String?
  note                   String?
  meta                   Json?
  created_at             DateTime             @default(now()) @db.Timestamptz(6)
  created_by             String?
  updated_at             DateTime?            @db.Timestamptz(6)
  updated_by             String?
  id                     BigInt               @id @default(autoincrement())
  weight_ticket_id       BigInt
  weight_ticket_line_id  BigInt?
  product_id             BigInt
  branch_id              BigInt
  warehouse_id           BigInt
  output_category        String?
  lot_no                 String?
  not_available_for_sale Boolean              @default(false)
  branches               branches             @relation(fields: [branch_id], references: [id], onUpdate: NoAction)
  products               products             @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  warehouses             warehouses           @relation(fields: [warehouse_id], references: [id], onUpdate: NoAction)
  weight_tickets         weight_tickets       @relation(fields: [weight_ticket_id], references: [id], onUpdate: NoAction)
  weight_ticket_lines    weight_ticket_lines? @relation(fields: [weight_ticket_line_id], references: [id], onUpdate: NoAction)

  @@index([branch_id, product_id, warehouse_id, output_category], map: "idx_stock_holds_active_lookup")
  @@index([product_id, branch_id, warehouse_id, output_category, lot_no, not_available_for_sale, held_at(sort: Desc), id(sort: Desc)], map: "idx_stock_holds_active_bucket_detail", where: raw("(status = 'active'::text)"))
  @@index([consumed_by_ref_type, consumed_by_ref_no, status], map: "idx_stock_holds_consumed_ref")
  @@index([weight_ticket_line_id], map: "idx_stock_holds_line")
  @@index([source_type, source_doc_no], map: "idx_stock_holds_source_doc")
  @@index([weight_ticket_id, status], map: "idx_stock_holds_ticket_status")
  @@schema("public")
}

model stock_transfer_items {
  id               BigInt          @id @default(autoincrement())
  transfer_id      BigInt
  product_id       BigInt
  qty              Decimal         @db.Decimal
  source_unit_cost Decimal         @default(0) @db.Decimal
  line_value       Decimal         @default(0) @db.Decimal
  created_at       DateTime        @default(now()) @db.Timestamptz(6)
  created_by       String?
  updated_at       DateTime?       @db.Timestamptz(6)
  updated_by       String?
  products         products        @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  stock_transfers  stock_transfers @relation(fields: [transfer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([product_id], map: "idx_stock_transfer_items_product")
  @@index([transfer_id, product_id], map: "idx_stock_transfer_items_transfer_product")
  @@schema("public")
}

model stock_transfers {
  id                   BigInt                 @id @default(autoincrement())
  doc_no               String                 @unique
  date                 DateTime               @db.Date
  transfer_date        DateTime?              @db.Date
  from_branch_id       BigInt
  from_warehouse_id    BigInt
  to_branch_id         BigInt
  to_warehouse_id      BigInt
  status               String                 @default("draft")
  notes                String?
  total_qty            Decimal                @default(0) @db.Decimal
  total_value          Decimal                @default(0) @db.Decimal
  posted_at            DateTime?              @db.Timestamptz(6)
  posted_by            String?
  cancelled_at         DateTime?              @db.Timestamptz(6)
  cancelled_by         String?
  cancel_reason        String?
  created_at           DateTime               @default(now()) @db.Timestamptz(6)
  created_by           String?
  updated_at           DateTime?              @db.Timestamptz(6)
  updated_by           String?
  version              Int                    @default(1)
  stock_transfer_items stock_transfer_items[]
  branches_from        branches               @relation("stock_transfers_from_branch_idTobranches", fields: [from_branch_id], references: [id], onUpdate: NoAction)
  warehouses_from      warehouses             @relation("stock_transfers_from_warehouse_idTowarehouses", fields: [from_warehouse_id], references: [id], onUpdate: NoAction)
  branches_to          branches               @relation("stock_transfers_to_branch_idTobranches", fields: [to_branch_id], references: [id], onUpdate: NoAction)
  warehouses_to        warehouses             @relation("stock_transfers_to_warehouse_idTowarehouses", fields: [to_warehouse_id], references: [id], onUpdate: NoAction)

  @@index([date(sort: Desc), doc_no(sort: Desc)], map: "idx_stock_transfers_date_doc")
  @@index([doc_no], map: "idx_stock_transfers_doc_no")
  @@index([status, date(sort: Desc), doc_no(sort: Desc)], map: "idx_stock_transfers_status_date_doc")
  @@index([total_qty], map: "idx_stock_transfers_total_qty")
  @@index([doc_no], map: "idx_stock_transfers_doc_no_pattern")
  @@schema("public")
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model stock_ledger {
  date                    DateTime                  @db.Date
  movement_type           String
  ref_id                  String?
  ref_no                  String?
  qty_in                  Decimal?                  @default(0) @db.Decimal
  qty_out                 Decimal?                  @default(0) @db.Decimal
  value_in                Decimal?                  @default(0) @db.Decimal
  value_out               Decimal?                  @default(0) @db.Decimal
  unit_cost               Decimal?                  @default(0) @db.Decimal
  notes                   String?
  created_at              DateTime?                 @default(now()) @db.Timestamptz(6)
  lot_no                  String?
  created_by              String?
  not_available_for_sale  Boolean?
  output_category         String?
  ref_type                String?
  return_customer_id      String?
  return_reason           String?
  source_input_id         String?
  note                    String?
  paid                    Boolean?                  @default(false)
  updated_at              DateTime?                 @db.Timestamptz(6)
  updated_by              String?
  version                 Int?                      @default(1)
  is_opening              Boolean?                  @default(false)
  warehouse_id            BigInt?
  branch_id               BigInt?
  id                      BigInt                    @id @default(autoincrement())
  output_product_id       BigInt?
  product_id              BigInt?
  purchase_channel_id     BigInt?
  sales_channel_id        BigInt?
  source_input_product_id BigInt?
  ledger_key              String                    @unique @default(dbgenerated("('SLG-'::text || (gen_random_uuid())::text)"))
  stock_cost_pool_entries stock_cost_pool_entries[]
  branches                branches?                 @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products                products?                 @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  warehouses              warehouses?               @relation(fields: [warehouse_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([date(sort: Desc)], map: "idx_stock_date")
  @@index([ref_type, ref_no, product_id], map: "idx_stock_ledger_ref_lookup", where: raw("(ref_type = ANY (ARRAY['PB'::text, 'PB-CANCEL'::text, 'PB-EDIT-REV'::text, 'SB'::text, 'SB-CANCEL'::text, 'PSALE'::text, 'PSALE-CANCEL'::text]))"))
  @@index([ref_no, ref_type, branch_id, warehouse_id, product_id], map: "idx_stock_ledger_purchase_reversal_ref", where: raw("(ref_type = ANY (ARRAY['PB'::text, 'PB-CANCEL'::text, 'PB-EDIT-REV'::text]))"))
  @@index([ref_no, ref_type, branch_id, warehouse_id, product_id], map: "idx_stock_ledger_sales_reversal_ref", where: raw("(ref_type = ANY (ARRAY['SB'::text, 'SB-CANCEL'::text, 'PSALE'::text, 'PSALE-CANCEL'::text]))"))
  @@index([ref_type, ref_no, ref_id], map: "idx_stock_ledger_pending_sale_reversal_ref", where: raw("(ref_type = ANY (ARRAY['PSALE'::text, 'PSALE-CANCEL'::text]))"))
  @@index([ref_type, ref_no], map: "idx_stock_ledger_production_ref", where: raw("(ref_type = ANY (ARRAY['PI'::text, 'PI-REV'::text, 'PO2'::text, 'PO2-REV'::text]))"))
  @@index([ref_type, ref_no, movement_type], map: "idx_stock_ledger_production_ref_movement", where: raw("(ref_type = ANY (ARRAY['PI'::text, 'PI-REV'::text, 'PO2'::text, 'PO2-REV'::text]))"))
  @@index([ref_type, ref_id, movement_type], map: "idx_stock_ledger_production_source_movement", where: raw("((ref_id IS NOT NULL) AND (ref_type = ANY (ARRAY['PI'::text, 'PO2'::text])))"))
  @@index([ref_type, ref_id], map: "idx_stock_ledger_source", where: raw("((ref_type IS NOT NULL) AND (ref_id IS NOT NULL))"))
  @@index([ref_type, ref_no, ref_id], map: "idx_stock_ledger_st_ref", where: raw("(ref_type = 'ST'::text)"))
  @@index([branch_id, warehouse_id, product_id, date], map: "idx_stock_ledger_st_source_lookup", where: raw("(ref_type = 'ST'::text)"))
  @@index([date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_ledger_sc_list", where: raw("((ref_type = 'SC'::text) AND (qty_out > (0)::numeric))"))
  @@index([output_category, note, branch_id, warehouse_id, product_id, date(sort: Desc)], map: "idx_stock_ledger_sc_filter", where: raw("((ref_type = 'SC'::text) AND (qty_out > (0)::numeric))"))
  @@index([output_category, note, date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_ledger_sc_transition_list", where: raw("((ref_type = 'SC'::text) AND (qty_out > (0)::numeric))"))
  @@index([ref_type, ref_no, created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_ledger_adj_ref_lookup", where: raw("(ref_type = ANY (ARRAY['ADJ'::text, 'ADJ-REV'::text]))"))
  @@index([branch_id, warehouse_id, product_id, date], map: "idx_stock_ledger_stock_dimension")
  @@index([product_id, branch_id, warehouse_id, output_category, lot_no, not_available_for_sale, date], map: "idx_stock_ledger_balance_bucket")
  @@index([product_id, branch_id, warehouse_id, output_category, lot_no, not_available_for_sale, date(sort: Desc), created_at(sort: Desc), id(sort: Desc)], map: "idx_stock_ledger_bucket_detail")
  @@index([product_id], map: "idx_stock_product")
  @@index([ref_id], map: "idx_stock_ref")
  @@index([movement_type], map: "idx_stock_type")
  @@schema("public")
}

model supplier_advance_allocations {
  allocated_amount                 Decimal                            @db.Decimal
  status                           String                             @default("active")
  allocated_at                     DateTime                           @default(now()) @db.Timestamptz(6)
  allocated_by                     String?
  voided_at                        DateTime?                          @db.Timestamptz(6)
  voided_by                        String?
  void_reason                      String?
  created_at                       DateTime                           @default(now()) @db.Timestamptz(6)
  updated_at                       DateTime                           @default(now()) @db.Timestamptz(6)
  version                          Int                                @default(1)
  id                               BigInt                             @id @default(autoincrement())
  advance_payment_id               BigInt
  purchase_bill_id                 BigInt
  allocation_key                   String                             @unique @default(dbgenerated("('SAA-'::text || (gen_random_uuid())::text)"))
  supplier_advance_allocation_logs supplier_advance_allocation_logs[]
  supplier_advance_payments        supplier_advance_payments          @relation(fields: [advance_payment_id], references: [id], onUpdate: NoAction)
  purchase_bills                   purchase_bills                     @relation(fields: [purchase_bill_id], references: [id], onUpdate: NoAction)

  @@index([advance_payment_id], map: "idx_supplier_advance_allocations_advance")
  @@index([purchase_bill_id], map: "idx_supplier_advance_allocations_bill")
  @@index([status], map: "idx_supplier_advance_allocations_status")
  @@schema("public")
}

model supplier_advance_allocation_logs {
  event_key                    String                        @unique(map: "idx_supplier_advance_allocation_logs_event_key") @default(dbgenerated("('ADVALLOC-'::text || (gen_random_uuid())::text)"))
  action                       String
  advance_doc_no               String
  purchase_bill_doc_no         String?
  allocation_key               String?
  allocated_amount             Decimal                       @default(0) @db.Decimal
  from_remaining_amount        Decimal?                      @db.Decimal
  to_remaining_amount          Decimal?                      @db.Decimal
  note                         String?
  meta                         Json?
  created_at                   DateTime                      @default(now()) @db.Timestamptz(6)
  created_by                   String?
  id                           BigInt                        @id @default(autoincrement())
  advance_payment_id           BigInt
  purchase_bill_id             BigInt?
  allocation_id                BigInt?
  supplier_advance_payments    supplier_advance_payments     @relation(fields: [advance_payment_id], references: [id], onUpdate: NoAction)
  supplier_advance_allocations supplier_advance_allocations? @relation(fields: [allocation_id], references: [id], onUpdate: NoAction)
  purchase_bills               purchase_bills?               @relation(fields: [purchase_bill_id], references: [id], onUpdate: NoAction)

  @@index([advance_payment_id, created_at(sort: Desc)], map: "idx_supplier_advance_allocation_logs_advance_created")
  @@index([allocation_id], map: "idx_supplier_advance_allocation_logs_allocation")
  @@index([allocation_key], map: "idx_supplier_advance_allocation_logs_allocation_key")
  @@index([purchase_bill_id], map: "idx_supplier_advance_allocation_logs_purchase_bill")
  @@schema("public")
}

model supplier_advance_payments {
  doc_no                           String                             @unique
  advance_date                     DateTime                           @db.Date
  amount                           Decimal                            @default(0) @db.Decimal
  allocated_amount                 Decimal                            @default(0) @db.Decimal
  remaining_amount                 Decimal                            @default(0) @db.Decimal
  payment_method                   String?
  status                           String                             @default("pending_approval")
  large_scale_doc_no               String?
  in_date                          DateTime?                          @db.Date
  out_date                         DateTime?                          @db.Date
  plate_no                         String?
  vehicle_photo_url                String?
  customer_name                    String?
  product_name                     String?
  weight_in                        Decimal                            @default(0) @db.Decimal
  weight_out                       Decimal                            @default(0) @db.Decimal
  net_weight                       Decimal                            @default(0) @db.Decimal
  price_per_kg                     Decimal                            @default(0) @db.Decimal
  scale_operator                   String?
  sender_name                      String?
  driver_name                      String?
  remark                           String?
  created_by                       String?
  created_at                       DateTime                           @default(now()) @db.Timestamptz(6)
  updated_by                       String?
  updated_at                       DateTime                           @default(now()) @db.Timestamptz(6)
  cancelled_at                     DateTime?                          @db.Timestamptz(6)
  cancelled_by                     String?
  cancel_reason                    String?
  version                          Int                                @default(1)
  vehicle_photo_names              String[]                           @default([])
  branch_id                        BigInt
  supplier_id                      BigInt
  id                               BigInt                             @id @default(autoincrement())
  funding_account_id               BigInt?
  supplier_advance_allocation_logs supplier_advance_allocation_logs[]
  supplier_advance_allocations     supplier_advance_allocations[]
  branches                         branches                           @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  accounts                         accounts?                          @relation(fields: [funding_account_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  suppliers                        suppliers                          @relation(fields: [supplier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  supplier_advance_status_logs     supplier_advance_status_logs[]

  @@index([funding_account_id], map: "idx_supplier_advance_payments_account")
  @@index([branch_id], map: "idx_supplier_advance_payments_branch")
  @@index([branch_id, advance_date(sort: Desc), doc_no(sort: Desc)], map: "idx_supplier_advances_active_branch_advance_date_doc", where: raw("((cancelled_at IS NULL) AND (branch_id IS NOT NULL))"))
  @@index([advance_date(sort: Desc)], map: "idx_supplier_advance_payments_date")
  @@index([advance_date(sort: Desc), doc_no(sort: Desc)], map: "idx_supplier_advances_active_advance_date_doc", where: raw("(cancelled_at IS NULL)"))
  @@index([status], map: "idx_supplier_advance_payments_status")
  @@index([supplier_id], map: "idx_supplier_advance_payments_supplier")
  @@schema("public")
}

model supplier_advance_status_logs {
  event_key                 String                    @unique(map: "idx_supplier_advance_status_logs_event_key") @default(dbgenerated("('ADVLOG-'::text || (gen_random_uuid())::text)"))
  action                    String
  advance_doc_no            String
  amount_snapshot           Decimal                   @default(0) @db.Decimal
  allocated_amount_snapshot Decimal                   @default(0) @db.Decimal
  remaining_amount_snapshot Decimal                   @default(0) @db.Decimal
  from_status               String?
  to_status                 String
  note                      String?
  meta                      Json?
  created_at                DateTime                  @default(now()) @db.Timestamptz(6)
  created_by                String?
  id                        BigInt                    @id @default(autoincrement())
  advance_payment_id        BigInt
  supplier_advance_payments supplier_advance_payments @relation(fields: [advance_payment_id], references: [id], onUpdate: NoAction)

  @@index([action], map: "idx_supplier_advance_status_logs_action")
  @@index([advance_payment_id, created_at(sort: Desc)], map: "idx_supplier_advance_status_logs_advance_created")
  @@schema("public")
}

model suppliers {
  code                      String                      @unique(map: "uq_suppliers_code")
  name                      String
  tax_id                    String?
  phone                     String?
  address                   String?
  sales_rep                 String?
  active                    Boolean?                    @default(true)
  created_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_by                String?
  type                      String?
  market_scope              String                      @default("ในประเทศ")
  name_title                String?
  first_name                String?
  last_name                 String?
  address_no                String?
  address_moo               String?
  address_village           String?
  address_road              String?
  address_subdistrict       String?
  address_district          String?
  address_province          String?
  address_postal_code       String?
  address_country           String?                     @default("ไทย")
  country_code              String?                     @default("TH")
  address_line1             String?
  address_line2             String?
  address_city              String?
  address_state_region      String?
  address_postal_code_intl  String?
  id                        BigInt                      @id @default(autoincrement())
  branch_id                 BigInt?
  sales_id                  BigInt?
  assets                    assets[]
  expenses                  expenses[]
  payment_status_logs       payment_status_logs[]
  payments                  payments[]
  po_buys                   po_buys[]
  purchase_bills            purchase_bills[]
  supplier_advance_payments supplier_advance_payments[]
  supplier_bank_accounts    supplier_bank_accounts[]
  supplier_branches         supplier_branches[]
  branches                  branches?                   @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  trading_allocation_facts  trading_allocation_facts[]
  trading_cost_sources      trading_cost_sources[]
  trading_deals             trading_deals[]
  weight_tickets            weight_tickets[]

  @@index([branch_id], map: "idx_suppliers_branch")
  @@index([name], map: "idx_suppliers_name")
  @@index([active], map: "idx_suppliers_active")
  @@index([market_scope], map: "idx_suppliers_market_scope")
  @@index([type], map: "idx_suppliers_type")
  @@index([country_code], map: "idx_suppliers_country_code")
  @@index([code, name], map: "idx_suppliers_active_code_name", where: raw("(active IS TRUE)"))
  @@index([active(sort: Desc), name, code, id], map: "idx_suppliers_active_name_cover")
  @@schema("public")
}

model supplier_branches {
  id          BigInt    @id @default(autoincrement())
  supplier_id BigInt
  branch_id   BigInt
  active      Boolean   @default(true)
  is_primary  Boolean   @default(false)
  created_at  DateTime  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime  @default(now()) @db.Timestamptz(6)
  created_by  String?
  updated_by  String?
  branches    branches  @relation(fields: [branch_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  suppliers   suppliers @relation(fields: [supplier_id], references: [id], onDelete: Restrict, onUpdate: NoAction)

  @@unique([supplier_id, branch_id], map: "supplier_branches_supplier_branch_unique")
  @@unique([supplier_id], map: "uq_supplier_branches_primary_active", where: raw("((active IS TRUE) AND (is_primary IS TRUE))"))
  @@index([branch_id, supplier_id], map: "idx_supplier_branches_branch_active_supplier", where: raw("(active IS TRUE)"))
  @@index([supplier_id, branch_id], map: "idx_supplier_branches_supplier_active_branch", where: raw("(active IS TRUE)"))
  @@schema("public")
}

model supplier_bank_accounts {
  payment_method String?     @default("เงินโอน")
  account_no     String?
  account_name   String?
  is_primary     Boolean?    @default(false)
  active         Boolean?    @default(true)
  created_at     DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at     DateTime?   @default(now()) @db.Timestamptz(6)
  branch_code    String?
  supplier_id    BigInt
  id             BigInt      @id @default(autoincrement())
  bank_name_id   BigInt?
  code           String      @unique(map: "uq_supplier_bank_accounts_code")
  bank_names     bank_names? @relation(fields: [bank_name_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_supplier_bank_accounts_bank_name")
  suppliers      suppliers   @relation(fields: [supplier_id], references: [id], onDelete: Cascade)

  @@unique([supplier_id, account_no], map: "uq_supplier_bank_accounts_supplier_account")
  @@index([account_no], map: "idx_supplier_bank_accounts_account")
  @@index([bank_name_id], map: "idx_supplier_bank_accounts_bank_name")
  @@index([code], map: "idx_supplier_bank_accounts_code")
  @@index([supplier_id], map: "idx_supplier_bank_accounts_supplier")
  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model thai_districts {
  district_code     String              @id
  province_code     String
  name_th           String
  name_en           String?
  postal_code       String?
  active            Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  thai_provinces    thai_provinces      @relation(fields: [province_code], references: [province_code], onDelete: NoAction, onUpdate: NoAction)
  thai_subdistricts thai_subdistricts[]

  @@index([province_code], map: "idx_thai_districts_province_code")
  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model thai_provinces {
  province_code     String              @id
  name_th           String
  name_en           String?
  active            Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  thai_districts    thai_districts[]
  thai_subdistricts thai_subdistricts[]

  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model thai_subdistricts {
  subdistrict_code String         @id
  district_code    String
  province_code    String
  name_th          String
  name_en          String?
  postal_code      String
  active           Boolean        @default(true)
  created_at       DateTime       @default(now()) @db.Timestamptz(6)
  thai_districts   thai_districts @relation(fields: [district_code], references: [district_code], onDelete: NoAction, onUpdate: NoAction)
  thai_provinces   thai_provinces @relation(fields: [province_code], references: [province_code], onDelete: NoAction, onUpdate: NoAction)

  @@index([district_code], map: "idx_thai_subdistricts_district_code")
  @@index([postal_code], map: "idx_thai_subdistricts_postal_code")
  @@index([province_code], map: "idx_thai_subdistricts_province_code")
  @@schema("public")
}

model trading_deals {
  deal_no                  String
  date                     DateTime                   @db.Date
  purchase_bill_no         String?
  sales_bill_no            String?
  matched_qty              Decimal?                   @default(0) @db.Decimal
  matched_purchase_amount  Decimal?                   @default(0) @db.Decimal
  matched_sales_amount     Decimal?                   @default(0) @db.Decimal
  ex_vat                   Boolean?                   @default(true)
  status                   String?                    @default("Partially Matched")
  notes                    String?
  created_by               String?
  cancelled_at             DateTime?                  @db.Timestamptz(6)
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  note                     String?
  updated_at               DateTime?                  @db.Timestamptz(6)
  updated_by               String?
  version                  Int?                       @default(1)
  cancelled_by             String?
  cancelled_reason         String?
  auto_created             Boolean?                   @default(false)
  supplier_id              BigInt?
  customer_id              BigInt?
  id                       BigInt                     @id @default(autoincrement())
  product_id               BigInt?
  purchase_bill_id         BigInt?
  sales_bill_id            BigInt?
  trading_allocation_facts trading_allocation_facts[]
  customers                customers?                 @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products                 products?                  @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchase_bills           purchase_bills?            @relation(fields: [purchase_bill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  sales_bills              sales_bills?               @relation(fields: [sales_bill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  suppliers                suppliers?                 @relation(fields: [supplier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([purchase_bill_id], map: "idx_deal_pb")
  @@index([sales_bill_id], map: "idx_deal_sb")
  @@index([purchase_bill_id, sales_bill_id], map: "idx_trading_deals_bills", where: raw("(status <> 'Cancelled'::text)"))
  @@index([deal_no], map: "idx_trading_deals_deal_no")
  @@schema("public")
}

model transfers {
  doc_no                                       String
  date                                         DateTime  @db.Date
  amount                                       Decimal   @db.Decimal
  fee                                          Decimal?  @default(0) @db.Decimal
  notes                                        String?
  status                                       String?   @default("active")
  created_by                                   String?
  created_at                                   DateTime? @default(now()) @db.Timestamptz(6)
  bank_fee                                     Decimal?  @default(0) @db.Decimal
  updated_at                                   DateTime? @db.Timestamptz(6)
  updated_by                                   String?
  version                                      Int?      @default(1)
  idempotency_key                              String?
  id                                           BigInt    @id @default(autoincrement())
  from_account_id                              BigInt?
  to_account_id                                BigInt?
  accounts_transfers_from_account_idToaccounts accounts? @relation("transfers_from_account_idToaccounts", fields: [from_account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  accounts_transfers_to_account_idToaccounts   accounts? @relation("transfers_to_account_idToaccounts", fields: [to_account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([doc_no], map: "idx_transfers_doc_no")
  @@index([idempotency_key], map: "idx_transfers_idempotency", where: raw("(idempotency_key IS NOT NULL)"))
  @@schema("public")
}

model user_profiles {
  user_id      String?   @unique @db.Uuid
  username     String    @unique
  display_name String?
  email        String?
  role         String
  active       Boolean?  @default(true)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  id           BigInt    @id @default(autoincrement())

  @@schema("public")
}

model public_users {
  code                String?
  username            String?
  email               String?
  name                String?
  active              Boolean?  @default(true)
  created_at          DateTime? @default(now()) @db.Timestamptz(6)
  updated_at          DateTime? @db.Timestamptz(6)
  password_changed_at DateTime? @db.Timestamptz(6)
  must_change_pwd     Boolean?  @default(false)
  updated_by          String?
  version             Int?      @default(1)
  branch_id           BigInt?
  id                  BigInt    @id @default(autoincrement())
  role_id             BigInt?

  @@map("users")
  @@schema("public")
}

model warehouses {
  code                                                                String                    @unique
  name                                                                String
  active                                                              Boolean?                  @default(true)
  created_at                                                          DateTime?                 @default(now()) @db.Timestamptz(6)
  type                                                                String?
  id                                                                  BigInt                    @id @default(autoincrement())
  branch_id                                                           BigInt?
  grade_adjustments                                                   grade_adjustments[]
  production_inputs_production_inputs_source_warehouse_idTowarehouses production_inputs[]       @relation("production_inputs_source_warehouse_idTowarehouses")
  production_inputs_production_inputs_wip_warehouse_idTowarehouses    production_inputs[]       @relation("production_inputs_wip_warehouse_idTowarehouses")
  production_orders                                                   production_orders[]
  production_outputs                                                  production_outputs[]
  purchase_bills                                                      purchase_bills[]
  sales_bills                                                         sales_bills[]
  stock_cost_pool_entries                                             stock_cost_pool_entries[]
  stock_holds                                                         stock_holds[]
  stock_issues                                                        stock_issues[]
  stock_ledger                                                        stock_ledger[]
  stock_transfers_from                                                stock_transfers[]         @relation("stock_transfers_from_warehouse_idTowarehouses")
  stock_transfers_to                                                  stock_transfers[]         @relation("stock_transfers_to_warehouse_idTowarehouses")
  branches                                                            branches?                 @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  weight_ticket_lines                                                 weight_ticket_lines[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_activity_logs {
  occurred_at        DateTime    @default(now()) @db.Timestamptz(6)
  activity_key       String
  activity_type      String
  title              String?
  description        String?
  actor_auth_user_id String?     @db.Uuid
  actor_username     String?
  actor_display_name String?
  route_path         String?
  referrer           String?
  http_method        String?
  request_path       String?
  request_id         String?
  target_type        String?
  target_id          String?
  target_label       String?
  status             String      @default("success")
  ip_address         String?     @db.Inet
  user_agent         String?
  metadata           Json        @default("{}")
  created_at         DateTime    @default(now()) @db.Timestamptz(6)
  id                 BigInt      @id @default(autoincrement())
  actor_app_user_id  BigInt?
  app_users          app_users?  @relation(fields: [actor_app_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users              auth_users? @relation(fields: [actor_auth_user_id], references: [id], onUpdate: NoAction)

  @@index([actor_app_user_id], map: "idx_app_activity_logs_actor_app_user_id")
  @@index([actor_auth_user_id], map: "idx_app_activity_logs_actor_auth_user_id")
  @@index([activity_key], map: "idx_app_activity_logs_activity_key")
  @@index([activity_type], map: "idx_app_activity_logs_activity_type")
  @@index([metadata], map: "idx_app_activity_logs_metadata_gin", type: Gin)
  @@index([occurred_at(sort: Desc)], map: "idx_app_activity_logs_occurred_at")
  @@index([route_path], map: "idx_app_activity_logs_route_path")
  @@index([target_type, target_id], map: "idx_app_activity_logs_target")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_audit_logs {
  occurred_at        DateTime    @default(now()) @db.Timestamptz(6)
  event_key          String
  action             String
  outcome            String      @default("success")
  severity           String      @default("info")
  actor_auth_user_id String?     @db.Uuid
  actor_username     String?
  actor_display_name String?
  target_type        String?
  target_id          String?
  target_label       String?
  entity_schema      String?
  entity_table       String?
  entity_id          String?
  entity_label       String?
  http_method        String?
  request_path       String?
  request_id         String?
  ip_address         String?     @db.Inet
  user_agent         String?
  before_data        Json?
  after_data         Json?
  diff               Json?
  metadata           Json        @default("{}")
  created_at         DateTime    @default(now()) @db.Timestamptz(6)
  id                 BigInt      @id @default(autoincrement())
  actor_app_user_id  BigInt?
  app_users          app_users?  @relation(fields: [actor_app_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users              auth_users? @relation(fields: [actor_auth_user_id], references: [id], onUpdate: NoAction)

  @@index([action], map: "idx_app_audit_logs_action")
  @@index([actor_app_user_id], map: "idx_app_audit_logs_actor_app_user_id")
  @@index([actor_auth_user_id], map: "idx_app_audit_logs_actor_auth_user_id")
  @@index([entity_table, entity_id], map: "idx_app_audit_logs_entity")
  @@index([event_key], map: "idx_app_audit_logs_event_key")
  @@index([metadata], map: "idx_app_audit_logs_metadata_gin", type: Gin)
  @@index([occurred_at(sort: Desc)], map: "idx_app_audit_logs_occurred_at")
  @@index([target_type, target_id], map: "idx_app_audit_logs_target")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_permissions {
  code                 String                 @unique
  module               String
  resource             String
  action               String
  description          String?
  active               Boolean                @default(true)
  created_at           DateTime               @default(now()) @db.Timestamptz(6)
  updated_at           DateTime               @default(now()) @db.Timestamptz(6)
  id                   BigInt                 @id @default(autoincrement())
  app_role_permissions app_role_permissions[]

  @@index([active], map: "idx_app_permissions_active")
  @@index([module], map: "idx_app_permissions_module")
  @@index([resource], map: "idx_app_permissions_resource")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_role_permissions {
  created_at      DateTime        @default(now()) @db.Timestamptz(6)
  created_by      String?
  role_id         BigInt
  permission_id   BigInt
  app_permissions app_permissions @relation(fields: [permission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  app_roles       app_roles       @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([role_id, permission_id])
  @@index([permission_id], map: "idx_app_role_permissions_permission_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_roles {
  code                     String                 @unique
  name                     String
  description              String?
  is_system                Boolean                @default(false)
  branch_scope             String                 @default("all")
  can_see_cost             Boolean                @default(false)
  can_see_profit           Boolean                @default(false)
  can_see_cash             Boolean                @default(false)
  can_see_financials       Boolean                @default(false)
  can_edit_opening_balance Boolean                @default(false)
  active                   Boolean                @default(true)
  created_at               DateTime               @default(now()) @db.Timestamptz(6)
  updated_at               DateTime               @default(now()) @db.Timestamptz(6)
  created_by               String?
  updated_by               String?
  id                       BigInt                 @id @default(autoincrement())
  app_role_permissions     app_role_permissions[]
  app_user_roles           app_user_roles[]

  @@index([active], map: "idx_app_roles_active")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_user_branch_access {
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  created_by String?
  branch_id  BigInt
  user_id    BigInt
  branches   branches  @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  app_users  app_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([user_id, branch_id])
  @@index([branch_id], map: "idx_app_user_branch_access_branch_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_user_roles {
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  created_by String?
  user_id    BigInt
  role_id    BigInt
  app_roles  app_roles @relation(fields: [role_id], references: [id], onUpdate: NoAction)
  app_users  app_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([user_id, role_id])
  @@index([role_id], map: "idx_app_user_roles_role_id")
  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model app_users {
  auth_user_id                                                  String?                  @unique @db.Uuid
  username                                                      String
  display_name                                                  String?
  email                                                         String?
  active                                                        Boolean                  @default(true)
  must_change_password                                          Boolean                  @default(false)
  last_login_at                                                 DateTime?                @db.Timestamptz(6)
  created_at                                                    DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at                                                    DateTime                 @default(now()) @db.Timestamptz(6)
  created_by                                                    String?
  updated_by                                                    String?
  id                                                            BigInt                   @id @default(autoincrement())
  name_prefix                                                   String?
  first_name                                                    String?
  last_name                                                     String?
  profile_image_url                                             String?
  contact_phone                                                 String?
  contact_line_id                                               String?
  contact_note                                                  String?
  app_activity_logs                                             app_activity_logs[]
  app_audit_logs                                                app_audit_logs[]
  app_auth_events_app_auth_events_actor_app_user_idToapp_users  app_auth_events[]        @relation("app_auth_events_actor_app_user_idToapp_users")
  app_auth_events_app_auth_events_target_app_user_idToapp_users app_auth_events[]        @relation("app_auth_events_target_app_user_idToapp_users")
  app_user_branch_access                                        app_user_branch_access[]
  app_user_roles                                                app_user_roles[]
  users                                                         auth_users?              @relation(fields: [auth_user_id], references: [id], onUpdate: NoAction)

  @@index([active], map: "idx_app_users_active")
  @@index([auth_user_id], map: "idx_app_users_auth_user_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_auth_events {
  actor_auth_user_id                                      String?     @db.Uuid
  event_type                                              String
  metadata                                                Json        @default("{}")
  ip_address                                              String?     @db.Inet
  user_agent                                              String?
  created_at                                              DateTime    @default(now()) @db.Timestamptz(6)
  id                                                      BigInt      @id @default(autoincrement())
  actor_app_user_id                                       BigInt?
  target_app_user_id                                      BigInt?
  app_users_app_auth_events_actor_app_user_idToapp_users  app_users?  @relation("app_auth_events_actor_app_user_idToapp_users", fields: [actor_app_user_id], references: [id], onUpdate: NoAction)
  users                                                   auth_users? @relation(fields: [actor_auth_user_id], references: [id], onUpdate: NoAction)
  app_users_app_auth_events_target_app_user_idToapp_users app_users?  @relation("app_auth_events_target_app_user_idToapp_users", fields: [target_app_user_id], references: [id], onUpdate: NoAction)

  @@index([actor_app_user_id], map: "idx_app_auth_events_actor_app_user_id")
  @@index([actor_auth_user_id], map: "idx_app_auth_events_actor_auth_user_id")
  @@index([created_at(sort: Desc)], map: "idx_app_auth_events_created_at")
  @@index([event_type], map: "idx_app_auth_events_event_type")
  @@index([target_app_user_id], map: "idx_app_auth_events_target_app_user_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trading_allocation_facts {
  id                     BigInt                @id @default(autoincrement())
  allocation_no          String                @unique(map: "uq_trading_allocation_facts_allocation_no")
  date                   DateTime              @db.Date
  trading_deal_id        BigInt?
  purchase_bill_id       BigInt?
  sales_bill_id          BigInt?
  supplier_id            BigInt?
  customer_id            BigInt?
  product_id             BigInt?
  source_type            String                @default("TRADING_PURCHASE_BILL")
  source_doc_no          String?
  source_line_no         Int?
  sales_doc_no           String?
  sales_line_no          Int?
  product_code_snapshot  String?
  product_name_snapshot  String?
  supplier_name_snapshot String?
  customer_name_snapshot String?
  qty                    Decimal               @default(0) @db.Decimal
  sales_amount           Decimal               @default(0) @db.Decimal
  matched_cogs           Decimal               @default(0) @db.Decimal
  allocation_method      String                @default("RECORDED_LINE")
  status                 String                @default("active")
  notes                  String?
  created_at             DateTime              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime              @default(now()) @db.Timestamptz(6)
  created_by             String?
  updated_by             String?
  trading_cost_source_id BigInt?
  customers              customers?            @relation(fields: [customer_id], references: [id], onUpdate: NoAction)
  products               products?             @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  purchase_bills         purchase_bills?       @relation(fields: [purchase_bill_id], references: [id], onUpdate: NoAction)
  sales_bills            sales_bills?          @relation(fields: [sales_bill_id], references: [id], onUpdate: NoAction)
  suppliers              suppliers?            @relation(fields: [supplier_id], references: [id], onUpdate: NoAction)
  trading_cost_sources   trading_cost_sources? @relation(fields: [trading_cost_source_id], references: [id], onUpdate: NoAction)
  trading_deals          trading_deals?        @relation(fields: [trading_deal_id], references: [id], onUpdate: NoAction)

  @@index([trading_cost_source_id, product_id], map: "idx_trading_allocation_facts_cost_source")
  @@index([customer_id, date(sort: Desc), id(sort: Desc)], map: "idx_trading_allocation_facts_customer_date", where: raw("((status = 'active'::text) AND (customer_id IS NOT NULL))"))
  @@index([date(sort: Desc), id(sort: Desc)], map: "idx_trading_allocation_facts_date", where: raw("(status = 'active'::text)"))
  @@index([product_id, date(sort: Desc), id(sort: Desc)], map: "idx_trading_allocation_facts_product_date", where: raw("((status = 'active'::text) AND (product_id IS NOT NULL))"))
  @@index([purchase_bill_id, product_id], map: "idx_trading_allocation_facts_purchase", where: raw("(status = 'active'::text)"))
  @@index([sales_bill_id, sales_line_no, product_id], map: "idx_trading_allocation_facts_sales", where: raw("(status = 'active'::text)"))
  @@index([sales_doc_no], map: "idx_trading_allocation_facts_sales_doc", where: raw("(sales_doc_no IS NOT NULL)"))
  @@index([source_doc_no], map: "idx_trading_allocation_facts_source_doc", where: raw("(source_doc_no IS NOT NULL)"))
  @@index([supplier_id, date(sort: Desc), id(sort: Desc)], map: "idx_trading_allocation_facts_supplier_date", where: raw("((status = 'active'::text) AND (supplier_id IS NOT NULL))"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trading_cost_sources {
  id                       BigInt                     @id @default(autoincrement())
  source_no                String                     @unique(map: "uq_trading_cost_sources_source_no")
  source_type              String                     @default("SPOT_MANUAL")
  date                     DateTime                   @db.Date
  supplier_id              BigInt?
  product_id               BigInt?
  product_code_snapshot    String?
  product_name_snapshot    String?
  supplier_name_snapshot   String?
  qty                      Decimal                    @default(0) @db.Decimal
  unit_cost                Decimal                    @default(0) @db.Decimal
  total_amount             Decimal                    @default(0) @db.Decimal
  status                   String                     @default("active")
  notes                    String?
  created_at               DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at               DateTime                   @default(now()) @db.Timestamptz(6)
  created_by               String?
  updated_by               String?
  trading_allocation_facts trading_allocation_facts[]
  products                 products?                  @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  suppliers                suppliers?                 @relation(fields: [supplier_id], references: [id], onUpdate: NoAction)

  @@index([date(sort: Desc), id(sort: Desc)], map: "idx_trading_cost_sources_date")
  @@index([product_id, status, date(sort: Desc)], map: "idx_trading_cost_sources_product_status")
  @@index([supplier_id, status, date(sort: Desc)], map: "idx_trading_cost_sources_supplier_status")
  @@schema("public")
}

model system_settings {
  id          BigInt   @id @default(autoincrement())
  key         String   @unique
  value       String?
  description String?
  updated_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_by  String?

  @@schema("public")
}

model line_groups {
  id          BigInt   @id @default(autoincrement())
  group_id    String   @unique
  name        String
  picture_url String?
  updated_at  DateTime @default(now()) @db.Timestamptz(6)
  branch_code String?
  notify_wti  Boolean  @default(true)
  notify_wto  Boolean  @default(true)
  is_active   Boolean  @default(true)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model line_command_permissions {
  id         BigInt   @id @default(autoincrement())
  target_id  String
  user_id    String?
  command    String
  is_allowed Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model line_message_templates {
  id             BigInt   @id @default(autoincrement())
  name           String
  template_type  String   @default("weight_ticket")
  is_default_wti Boolean  @default(false)
  is_default_wto Boolean  @default(false)
  is_active      Boolean  @default(true)
  config         Json
  created_by     String?
  updated_by     String?
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  updated_at     DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model line_notification_attempts {
  id                     BigInt                 @id @default(autoincrement())
  job_id                 BigInt
  attempt_no             Int
  status                 String
  http_status            Int?
  line_request_id        String?
  accepted_request_id    String?
  error_code             String?
  error_message          String?
  duration_ms            Int?
  created_at             DateTime               @default(now()) @db.Timestamptz(6)
  line_notification_jobs line_notification_jobs @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model line_notification_jobs {
  id                         BigInt                       @id @default(autoincrement())
  source_type                String                       @default("weight_ticket")
  source_id                  BigInt
  document_no                String
  document_type              String
  target_id                  String
  target_type                String                       @default("unknown")
  template_id                BigInt?
  custom_message             String?
  status                     String                       @default("pending")
  priority                   Int                          @default(100)
  attempt_count              Int                          @default(0)
  max_attempts               Int                          @default(5)
  next_retry_at              DateTime                     @default(now()) @db.Timestamptz(6)
  locked_at                  DateTime?                    @db.Timestamptz(6)
  locked_by                  String?
  retry_key                  String                       @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  pdf_storage_bucket         String?
  pdf_storage_key            String?
  pdf_url                    String?
  line_request_id            String?
  accepted_request_id        String?
  last_error_code            String?
  last_error_message         String?
  requested_by               String?
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  sent_at                    DateTime?                    @db.Timestamptz(6)
  line_notification_attempts line_notification_attempts[]

  @@unique([source_type, source_id, target_id], map: "line_notification_jobs_uniq_pending_idx", where: raw("(status IN ('pending'::text, 'processing'::text))"))
  @@index([source_type, source_id], map: "line_notification_jobs_source_idx")
  @@index([status, next_retry_at], map: "line_notification_jobs_status_retry_idx")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model line_notification_rules {
  id               BigInt   @id @default(autoincrement())
  name             String
  description      String?
  priority         Int      @default(100)
  is_active        Boolean  @default(true)
  target_id        String
  template_id      BigInt?
  stop_after_match Boolean  @default(false)
  conditions       Json     @default("{}")
  created_by       String?
  updated_by       String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@index([is_active, priority], map: "line_notification_rules_active_priority_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model line_targets {
  id              BigInt    @id @default(autoincrement())
  target_id       String    @unique
  target_type     String
  display_name    String
  picture_url     String?
  branch_code     String?
  is_default      Boolean   @unique(map: "line_targets_one_default_idx", where: raw("(is_default = true)")) @default(false)
  is_active       Boolean   @default(true)
  notify_wti      Boolean   @default(true)
  notify_wto      Boolean   @default(true)
  last_seen_at    DateTime? @db.Timestamptz(6)
  last_event_type String?
  registered_by   String?
  created_at      DateTime  @default(now()) @db.Timestamptz(6)
  updated_at      DateTime  @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model weight_ticket_notification_logs {
  id                 BigInt         @id @default(autoincrement())
  weight_ticket_id   BigInt
  delivery_channel   String         @default("line")
  target_id          String?
  status             String
  pdf_storage_bucket String?
  pdf_storage_key    String?
  pdf_url            String?
  line_request_id    String?
  custom_message     String?
  error_message      String?
  requested_by       String?
  sent_at            DateTime?      @db.Timestamptz(6)
  created_at         DateTime       @default(now()) @db.Timestamptz(6)
  weight_tickets     weight_tickets @relation(fields: [weight_ticket_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([status, created_at(sort: Desc)], map: "idx_weight_ticket_notification_logs_status")
  @@index([weight_ticket_id, created_at(sort: Desc)], map: "idx_weight_ticket_notification_logs_ticket")
  @@schema("public")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}
