# Task Checklist 2026-06-27

## ภารกิจ: แก้ปัญหา Playwright Chromium binary หายใน runtime container (ns-dev.devkub.com)

### บริบทปัญหา
- LINE notification (`/api/daily/weight-tickets/[id]/notify-line`) คืน 502 บน devkub
- error: `browserType.launch: Executable doesn't exist at /root/.cache/ms-playwright/chromium_headless_shell-1223/...`
- สาเหตุ: `.playwright-browsers/` (build artifact จาก `scripts/install-playwright.js`) ไม่อยู่ใน runtime container
  เพราะ Docker multi-stage ไม่ copy ข้าม stage, และ runtime path logic ปัจจุบันเป็น best-effort guess
  (commit 70270279, b98a6c44) ที่ไม่ throw เมื่อหาไม่เจอ → Playwright fallback ไป default path ที่ว่างเปล่า
- localhost ใช้ได้เพราะ `.playwright-browsers/` อยู่ในเครื่องจากการรัน build ครั้งล่าสุด

### Checklist (แผน A — แก้ฝั่งโค้ดให้ resolve path แข็งแรง + health endpoint)
- [ ] Sync upstream `new-origin/dev` (0/0 clean แล้ว)
- [ ] แก้ `apps/next/src/lib/server/weight-ticket-line-notification.ts` (~L435-453):
      resolve PLAYWRIGHT_BROWSERS_PATH ตามลำดับ env var → candidates → throw ชัดเจนเมื่อหาไม่เจอ
- [ ] แก้ `scripts/install-playwright.js`: เขียน marker file `.playwright-browsers/.installed` + log path ชัดเจน
- [ ] แก้ `apps/next/src/app/api/health/route.ts`: เพิ่ม playwright binary check
- [ ] รัน `npm run type-check --workspace @ns-scrap-erp/next` ผ่าน
- [ ] รัน `npm run lint --workspace @ns-scrap-erp/next` ผ่าน
- [ ] รัน `npm run build --workspace @ns-scrap-erp/next` ผ่าน
- [ ] บันทึก Flow summary ลง `docs/notes/`
- [ ] รายงานผล + ขออนุมัติ commit/push ขึ้น `new-origin/dev`

### ขอบเขตที่ทำ (Scope)
- ✅ แก้โค้ดฝั่ง runtime path resolution + health endpoint
- ❌ ไม่รัน browser UAT (รอ Dockerfile แก้ฝั่ง infra)
- ❌ ไม่ commit/push เอง (รออนุมัติ)
