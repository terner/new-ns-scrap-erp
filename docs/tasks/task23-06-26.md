# Task 23-06-26

## Sorting Dropdown Sync & Stable Sorting (NSERP-53)

- [x] Refactor `sortPool` in `apps/next/src/app/api/dual-costing/cost-allocator/route.ts` to implement stable sorting with secondary/tertiary tie-breakers.
- [x] Refactor `sortRows` in `apps/next/src/app/api/dual-costing/cost-pool/route.ts` to implement identical stable sorting tie-breakers.
- [x] Modify `GET` handler in `apps/next/src/app/api/dual-costing/cost-allocator/route.ts` to return `pool: selectedPool` instead of `filteredPool` so Section ③ updates automatically on sorting mode change.

## Match Confirmation API & UI Hook (NSERP-54)

- [x] Implement `POST` handler in `apps/next/src/app/api/dual-costing/cost-allocator/route.ts` to save matches in a Prisma transaction.
  - [x] Lookup target document (sales bill, PO sell, or production order).
  - [x] Lookup candidate documents (PO buy, purchase bill, or stock cost pool entry).
  - [x] Deduct quantity from `stock_cost_pool_entries.allocated_qty` if candidate is production/regrade.
  - [x] Create `trading_deals` record for each match candidate.
  - [x] Create `trading_allocation_facts` record for each match candidate.
- [x] Update `CostAllocatorPageClient.tsx` to enable the "ยืนยัน Match" button.
  - [x] Hook up `onClick` to call the `POST` API.
  - [x] Handle loading state, errors, success toast, and state cleanup.

## Verification & Validation

- [x] Run `npm run type-check --workspace @ns-scrap-erp/next`
- [x] Run `npm run lint --workspace @ns-scrap-erp/next`
- [x] Propose build validation check

## Fix Waiting Allocations Deduct / Update Quantity Bug

- [x] Modify `apps/next/src/lib/server/daily-costing-management.ts` to link matches to PO Sells and Sales Bills using document number mapping as a fallback when `sales_bill_id` is null.
- [x] Modify `apps/next/src/app/api/dual-costing/cost-allocator/route.ts` to implement matching logic by product and document number fallbacks so remaining quantity is calculated correctly when rendering the matcher screen.
- [x] Run validation commands (type-check, lint, build) to verify changes.

## WTO pending_out / Remove Pending Sale Runtime

- [x] อ่าน startup rules และตรวจ worktree ก่อนแก้
- [x] ตรวจ code path ที่ยังใช้ `PSALE`, `pendingStockIssueId`, `/sales/stock-issue`
- [x] ปิด navigation และ write API ของ Pending Sale
- [x] ปรับ Sales Bill schema/API/UI ให้รับ WTO direct เท่านั้นสำหรับ stock source
- [x] ปรับ wording/error/helper alias ให้ใช้ `pending_out` ใน business contract
- [x] อัปเดต flow docs/current-work หลัง runtime change
- [x] รัน type-check/lint/build หรือ validation ที่เหมาะสม

## Set Default Sort on WTI/WTO List Page (NSERP-57)

- [x] Modify `WeightTicketListPageClient.tsx` to set default sorting state to `documentNo` / `asc` and update `clearFilters`.
- [x] Modify `weight-tickets.ts` backend query parser to default to `documentNo` / `asc` when `sortBy` / `sortDir` parameters are empty.
- [x] Run validation commands (type-check, lint, build) to verify changes.

## Mobile Responsive Sizing & Manage Action Drawer (NSERP-58)

- [x] Declare `activeActionsTicket` state in `WeightTicketListPageClient.tsx`.
- [x] Implement larger text-sm (14px) and high-contrast styling on mobile cards.
- [x] Replace bottom individual action buttons with a single "จัดการรายการ" button opening a bottom-sheet like action modal.
- [x] Increase pagination button size from `xs` to `sm` for better tap target size.
- [x] Run validation commands (type-check, lint, build) to verify compilation.

## Fix Production Report Interactivity Crash (Maximum update depth exceeded)

- [x] Identify React render loop caused by inline array literal `[]` as columns parameter inside `ProductionReportPageClient.tsx`.
- [x] Refactor `dummyResize` to pass a static `emptyColumns` constant defined outside the component instead of inline `[]`.
- [x] Set password for dev testing user `import.export@newsolutionsth.com` to `p@ssw0rd`.
- [x] Run type-check and lint validation checks.
- [x] Verify complete interactivity, date picker, search box, and sidebar links responsiveness via browser subagent.

## Admin Users Profile / Contact Data

- [x] เพิ่ม 4 user ฝ่ายคัดแยกใน Supabase Auth และ `app_users`
- [x] เพิ่ม optional profile/contact columns ใน `app_users`
- [x] อัปเดต Users & Permissions API ให้รับ/ส่ง profile/contact fields
- [x] อัปเดต Users & Permissions UI ให้กรอกและแสดงคำนำหน้า ชื่อ นามสกุล รูป profile และ contact
- [x] อัปเดต page flow doc ของ `/admin/users-permissions`
- [x] Apply migration กับ dev database และเติมข้อมูลชื่อแยก field ให้ 4 user
- [x] รัน `git diff --check`, type-check, lint และ build

## Split Users And Roles Admin Pages

- [x] เพิ่ม `/admin/users` สำหรับรายชื่อพนักงาน/user profile
- [x] เพิ่ม `/admin/roles-permissions` สำหรับ role/permission summary
- [x] เปลี่ยน `/admin/users-permissions` ให้ redirect ไป `/admin/users`
- [x] ปรับ navigation และ permission map ให้ route ใหม่ใช้ `system.users.manage`
- [x] อัปเดต flow doc ให้แยก User, Role และ Department semantics

## Sales Bill WTO Return / Loss Flow

- [x] ตรวจ schema/helper ปัจจุบันของ `stock_holds`, `stock_ledger`, `weight_ticket_usage_logs`, และ Sales Bill detail
- [x] เพิ่ม write path รับคืน active `pending_out` จาก WTO ที่ผูกกับ SB
- [x] ถ้ารับคืนจริงน้อยกว่า pending_out ให้บังคับเหตุผลและลง `stock_ledger` เป็น loss
- [x] ปิด/split `stock_holds` ให้ไม่เหลือ pending_out ค้างผิดสถานะ
- [x] เพิ่ม usage/timeline facts ให้ดูย้อนกลับได้ว่า return/loss มาจาก SB/WTO ไหน
- [x] ย้าย UI รับของคืนออกจาก Sales Bill detail และให้เปิดจาก detail ของ WTO เฉพาะกรณีถูกนำไปออก SB แล้วบางส่วนและยังมี pending_out เหลือ
- [x] เพิ่มสถานะ WTO `ออกบิลแล้วบางส่วน` สำหรับใบส่งของที่ถูก SB ใช้บางส่วนและยังมี pending_out รอรับคืน
- [x] อัปเดตเอกสาร Sales/WTO/Stock ให้ตรงกับ flow ใหม่
- [x] รัน validation อย่างน้อย `type-check` และ `git diff --check`

## Stock Sale Pending Out / Allocation Hardening

- [x] ยืนยันว่า durable allocation tables มีอยู่แล้ว: `sales_bill_lines`, `sales_bill_source_allocations`, `sales_bill_po_sell_allocations`, `sales_bill_customer_advance_allocations`
- [x] เปลี่ยน service/API caller สำคัญจากชื่อ concept เก่าเป็น `pending_out` โดยไม่ rename technical table `stock_holds`
- [x] ลบ compatibility alias ชื่อเก่าใน service เพื่อให้ type-check จับ caller ที่ยังไม่ใช้ `pending_out`
- [x] แก้ SB cancel หลังรับคืนแล้วให้ไม่ reopen pending_out และไม่คืน WTO summary ซ้ำ
- [x] อัปเดต docs/tasklist ให้ใช้คำว่า `pending_out / รอออก` เป็น business language เดียว
- [x] ตรวจ validation หลังเปลี่ยน terminology

## Sales Bill Export Order No

- [x] เพิ่ม column `sales_bills.export_order_no` สำหรับเลขที่ order ส่งออก
- [x] เพิ่ม validation/API create ให้บังคับกรอกเฉพาะบิลขายลูกค้า `ต่างประเทศ` และไม่รับค่าจากบิลในประเทศ
- [x] เพิ่มช่องใน Sales Bill modal หลัง `ช่องทางขาย`
- [x] แสดงเลขที่ order ส่งออกใน detail/print เมื่อมีค่า
- [x] อัปเดต Sales Bill flow doc และรัน validation

## Sales Bill Correction Policy

- [x] ยืนยัน requirement ว่า Sales Bill correction ต้องใช้ diff-only correction เป็นหลัก ไม่ reverse ทั้งบิลแล้ว repost ทั้งบิล เพราะมีผลต่อ stock value, COGS, WAC, PO Sell remaining, AR, VAT, และ customer advance
- [x] อัปเดต `docs/notes/Sales Bills Page Flow.md` ให้ระบุว่าแก้จำนวน/ราคา/สิ่งเจือปนต้อง append เฉพาะ delta fact/ledger rows ที่จำเป็น

## Sales / Stock / Finance Follow-up Batch

- [x] Fix AR API ให้ใช้ `sales_bills.receivable_balance` และ `sales_bills.received_amount` เป็น source หลัก
- [x] Fix AP API ให้ใช้ `purchase_bills.payable_balance` และ `purchase_bills.paid_amount` เป็น source หลัก
- [x] ตัด AP channel filter contract ที่ไม่มี purchase channel จริง
- [x] เพิ่ม AR drilldown ใน API/modal จาก SB, active RCP allocation, และ Customer Advance allocation
- [x] เพิ่ม AP drilldown ใน API/modal จาก PB, PMA, active PMT allocation, และ Supplier Advance allocation
- [~] เริ่ม write path `STOCK` Sales Bill correction: เปิดบันทึก edit slice แรกสำหรับ `Spot Sale <-> PO Sell`, ราคา, ส่วนลด, หมายเหตุ และ header totals โดยไม่เปลี่ยน WTO/source/น้ำหนัก; ยังเหลือ qty/deduct/customer advance correction
- [x] Lock Sales Bill edit modal business identity ตาม pattern บิลซื้อ: ห้ามเปลี่ยน Stock/Trading, สาขา, Customer, WTO source, และ Trading PB source จาก edit modal
- [ ] ตรวจ line-level export/dashboard/report ให้ใช้ durable facts ไม่อ่าน JSON snapshot เป็น source หลัก
- [x] เพิ่ม history/snapshot contract สำหรับ bill/PO/finance/stock ย้อนหลังและ stock as-of WAC

## Sales Bill Full Correction Slice 2

- [x] เทียบบิลซื้อ edit pattern: release/supersede/recreate facts ใน transaction เดียว ไม่แก้ ledger/fact เก่าทิ้ง
- [x] แยก contract บิลขายว่า `จำนวนที่ขายได้/หักสิ่งเจือปน` เป็น commercial correction ก่อน ไม่ใช่ stock source correction
- [x] อัปเดต Sales Bill flow doc/tasklist ให้ระบุ scope slice 2
- [x] เปิด `PATCH /api/sales/bills` ให้แก้ `จำนวนที่ขายได้`, `หักสิ่งเจือปน`, ราคา, ส่วนลด, PO Sell, VAT/header โดยไม่เปลี่ยน WTO/source/stock ledger
- [x] เพิ่ม Customer Advance allocation correction: release allocation เก่า, validate available, create active allocation ใหม่, update AR snapshot
- [x] ปรับ Sales Bill edit modal ให้ unlock เฉพาะ field ที่ API รองรับ และยัง lock identity/source ตาม pattern บิลซื้อ
- [x] รัน validation: type-check, lint, build, และ diff check

## Sales Bill STOCK Quantity-First Documentation Hardening

- [x] อัปเดตเอกสารให้ชัดว่า `WTO pending_out` เป็น stock-side source of truth ของ `STOCK` Sales Bill
- [x] อัปเดตเอกสารให้ชัดว่า `จำนวนที่ขายได้/หักสิ่งเจือปน/น้ำหนักขายสุทธิ` เป็น commercial fields และห้ามเพิ่ม stock consumption เกิน WTO
- [x] อัปเดตเอกสาร correction policy ให้ชัดว่า ถ้าแก้แล้ว source quantity ลดลงต้องคืน stock ตาม delta และถ้าเพิ่มขึ้นภายใน WTO ต้องตัด stock เพิ่มตาม delta
- [x] อัปเดตเอกสารให้แยกกรณี WTO/source swap หรือ post-return/loss correction ออกเป็น stock correction flow คนละประเภท

## Sales Bill STOCK Correction Runtime

- [x] ตรวจ patch ค้างใน `apps/next/src/app/api/sales/bills/route.ts` และ `stock-holds.ts` ให้ครบทั้ง flow
- [x] ปิด `PATCH /api/sales/bills` ให้รองรับ stock delta correction บน WTO เดิมใบเดิม
- [x] ตรวจ update `sales_bill_source_allocations`, `weight_ticket_usage_logs`, `weight_ticket_product_summaries`, `weight_tickets.status`, และ `stock_ledger` ให้สอดคล้องกัน
- [x] รัน `type-check`, `build`, และ `git diff --check`

## Open Bill From Weight Ticket List

- [x] อัปเดต flow docs ของ `WTI -> PB` และ `WTO -> SB` ให้รองรับปุ่ม `เปิดบิล`
- [x] เพิ่มปุ่ม `เปิดบิลซื้อ` / `เปิดบิลขาย` ในหน้า `/daily/weight-ticket-list` ตาม status/runtime rule
- [x] ให้ `/purchase/bills` รองรับ `?new=1&wti=...` แล้วเปิด modal พร้อม preload `WTI`
- [x] ให้ `/sales/bills` รองรับ `?new=1&wto=...` แล้วเปิด modal พร้อม preload `WTO`
- [x] รัน `type-check` และ `git diff --check`
