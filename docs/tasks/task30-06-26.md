# Task 30-06-26

## Sales Bill WTO Average Cost Display

- [x] Show WTO cost snapshot as read-only average cost labeled `ราคาต้นทุนเฉลี่ย` after the PO/Spot reference column in Sales Bill item tables.
- [x] Rename Sales Bill selling price label from `ราคา/หน่วย` to `ราคาขาย/หน่วย`.
- [x] Validate Prisma generate, type-check, and diff whitespace for the Sales Bill UI/API change.

## WTO Per-Scale Cost Snapshot Flow Docs

- [x] Document that WTO owns per-scale pending_out cost snapshots and product-level weighted average cost summary.
- [x] Document edit-after-confirm history rules for unchanged, edited, increased, decreased, new, and removed scales.
- [x] Document that Sales Bill consumes the WTO-prepared cost summary/snapshots and must not recalculate from current WAC.
- [x] Add WTO detail history section for pending_out status and per-scale cost snapshots.
- [x] Show WTO average cost on Weight Ticket detail/modal at product summary, real scale/line, and pending_out history levels.
- [x] Replace WTI/WTO user-facing Gross/Net labels with `น้ำหนักรวม` and `น้ำหนักสุทธิ`.
- [x] Document that WTO customer edit affects document ownership and Sales Bill validation, not pending_out qty/cost by itself.
- [x] Move WTO pending_out/cost audit rows into the document timeline as collapsed expandable change tables, while keeping current cost visible in the product breakdown only.
- [x] Split WTO pending_out audit into `weight_ticket_pending_out_events`; keep `stock_holds` as the operational stock/current pending_out table and read timeline change tables from the immutable audit table.
- [x] Append WTO pending_out audit rows for Sales Bill consume, edit release, cancel reopen, stock return, and return loss flows.

## WTI/WTO Write Separation Refactor

- [x] Extract field-level edit audit builder out of the edit route into a shared server helper.
- [x] Extract WTI/WTO-specific write validation guards out of route files so supplier/customer/warehouse/impurity rules are easier to review.
- [x] Extract internal write handler decisions for party snapshot, warehouse resolution, stock validation, and pending_out side effects out of API route files.
- [x] Extract WTO edit pending_out/cost preservation plan out of the edit route into the WTO write helper.
- [x] Keep existing route URLs, DB tables, and business behavior unchanged in this batch.
- [x] Validate with Next type-check and diff whitespace check.

## WTO Product/Warehouse Grouping

- [x] Allow the same product to appear in separate WTO cards only when the warehouse differs.
- [x] Auto-merge a duplicate WTO parent card with the same product and warehouse into the existing card as a new scale/lot.
- [x] Add API/schema validation so WTO cannot save duplicate parent cards for the same product and warehouse.
- [x] Validate with Next type-check and diff whitespace check.

## Browser QA Cleanup

- [x] Verify dev login from environment credentials can open `/daily/weight-ticket-list`.
- [x] Trace the remaining console 404 to `/favicon.ico` rather than the weight-ticket API.
- [x] Add app favicon metadata and static SVG favicon so the browser no longer requests missing `/favicon.ico`.

## WTO Pending Out Quantity and Cost Guard

- [x] Keep WTO draft/save guarded by stock availability validation before pending_out rows are created.
- [x] Aggregate stock ledger qty/value by normalized WTO bucket before calculating average cost snapshots.
- [x] Block WTO cost snapshot when average cost is missing, zero, negative, or not finite instead of saving `0`.
- [x] Validate with Next type-check.

## Sales Bill WTO Edit References

- [x] Keep Sales Bill edit form using the original WTO delivery line/summary references instead of generated edit-only ids.
- [x] Verify `SB2606-0008` detail payload now returns original WTO delivery line/summary references for edit.
- [x] Validate with Next type-check and diff whitespace check.
