# NS Scrap ERP Task Checklist (2026-06-16)

## 📌 งานประจำวันที่ 16 มิถุนายน 2569 (2026-06-16)

### 🏭 1. แก้ไขบั๊กช่องค้นหาและเลือกสินค้า (SearchCombobox Focus Trap Fix)
- [x] สลับกิ่งมาที่ `codex/combobox-focus-trap-fix`
- [x] แก้ไขตรรกะใน [SearchCombobox.tsx](file:///c:/new-ns-scrap-erp/apps/next/src/components/ui/SearchCombobox.tsx) เพื่อนำแนวทางแก้ปัญหากลไก Focus Trap ของ Radix UI Dialog กลับคืนมา:
  - [x] กู้คืน `containerRef` และเปลี่ยนการจับอีเวนต์ปิด Panel ด้วย `handleOutsideClick` แทนการใช้ `onBlur`
  - [x] คืนค่าให้ `scopedPortalHost` ค้นหาทั้ง `[role="dialog"]` และ `[data-combobox-portal-root="true"]` เพื่อให้ dropdown เรนเดอร์ภายใน Dialog เดียวกัน
  - [x] เปลี่ยนอีเวนต์คลิกของตัวเลือกสินค้าใน Dropdown จาก `onMouseDown` เป็น `onMouseDownCapture` และ `onTouchStartCapture` พร้อมเรียกใช้ `event.stopPropagation()` เพื่อบล็อกการสกัดโฟกัสจาก Radix UI
  - [x] รักษาฟีเจอร์ `hideSelectedOptionFromList` ที่เพิ่มเข้ามาใหม่ให้อยู่ครบถ้วน
- [x] รันการทดสอบคอมไพล์โค้ดใน Next.js Workspace เพื่อให้แน่ใจว่าไม่มีข้อผิดพลาดทาง Syntax หรือ TypeScript:
  ```bash
  npm run type-check --workspace @ns-scrap-erp/next
  npm run lint --workspace @ns-scrap-erp/next
  ```
- [x] รันระบบและทำการทดสอบแบบโต้ตอบ (UAT) ผ่านหน้าต่างสร้างใบสั่งผลิตใหม่:
  - [x] ทดลองเลือกสินค้าในแบบฟอร์ม "ใบสั่งผลิตใหม่" เพื่อยืนยันว่าปุ่ม dropdown สามารถคลิกเพื่อเลือกตัวสินค้าได้จริง
  - [x] ยืนยันการบันทึกเอกสารใบสั่งผลิตและตรวจสอบว่าสามารถเลือกข้อมูลสินค้าได้เรียบร้อย
  - [x] บันทึกภาพหน้าจอยืนยันผลการทำงานลงใน Walkthrough

### 🏭 2. ปรับปรุงการเลื่อนรายการ (Scroll) บนมือถือใน SearchCombobox
- [x] ประกาศ Refs บันทึกตำแหน่งและพฤติกรรม Touch เพื่อแยกแยะ Tap (เลือก) กับ Drag/Scroll (เลื่อน)
- [x] ปรับแต่งอีเวนต์ปุ่มตัวเลือกใน Dropdown:
  - [x] ใช้ `onTouchStartCapture` บันทึกจุดพิกัดเริ่มต้น
  - [x] ใช้ `onTouchMoveCapture` คำนยณระยะการเลื่อนนิ้ว (ถ้านิ้วเลื่อนเกิน 10px ให้ถือว่าเป็นการ scroll)
  - [x] ใช้ `onTouchEndCapture` เพื่อเลือกสินค้าเฉพาะตอนที่ไม่มีการเคลื่อนที่นิ้ว (Tap) และบล็อกอีเวนต์ซ้ำซ้อน
- [x] รันการตรวจสอบ compilation (`type-check` และ `lint`) ใน Workspace

### 🔄 3. ระบบปรับสถานะสินค้าข้ามคลังสินค้า (Status Convert Warehouse Flow)
- [x] สลับกิ่งมาที่ `codex/status-convert-warehouse-flow`
- [x] ปรับปรุง Zod Schema ใน [stock.ts](file:///c:/new-ns-scrap-erp/apps/next/src/lib/stock.ts):
  - [x] เพิ่มฟิลด์ `targetWarehouseId` เพื่อรองรับคลังปลายทาง
  - [x] ปรับให้ `fromStatus` และ `toStatus` ยอมรับค่าเริ่มต้น (`default('RM')` และ `default('FG')`)
- [x] ปรับปรุงระบบ API ใน [route.ts](file:///c:/new-ns-scrap-erp/apps/next/src/app/api/stock/status-convert/route.ts):
  - [x] ปรับปรุง `GET` เพื่อดึงและแมปคู่รายการฝั่ง IN เพื่อดึง `targetWarehouseName` มาแสดงผล
  - [x] ปรับปรุง `POST` เพื่อตรวจสอบ `targetWarehouseId` ว่ามีอยู่จริงและสังกัดสาขาเดียวกัน
  - [x] ปรับปรุง `POST` ตอนบันทึก `stock_ledger` โดยให้รายการฝั่ง IN ลงคลังสินค้าปลายทาง (`warehouse_id: values.targetWarehouseId`)
- [x] ปรับปรุงหน้าจอ UI ใน [StockOperationPageClient.tsx](file:///c:/new-ns-scrap-erp/apps/next/src/components/stock/StockOperationPageClient.tsx):
  - [x] ปรับฟอร์ม `StatusConvertForm` ให้ยกเลิกการใช้ `BranchWarehouseFields` และแสดงฟิลด์ "สาขา", "คลังต้นทาง", "คลังปลายทาง"
  - [x] ลบฟิลด์ "จากสถานะ" และ "เป็นสถานะ" ออกจาก UI
  - [x] ปรับปรุงส่วนแสดงผลตารางคอลัมน์คลังให้แสดง `คลังต้นทาง → คลังปลายทาง`
  - [x] ปรับปรุงส่วนแสดงผลการ์ดสรุปบน Mobile ให้แสดงคลังต้นทางและปลายทางด้วย
- [x] รันการตรวจสอบ compilation (`type-check` และ `lint`) ใน Workspace

### 📝 4. เพิ่มกฎการเริ่มต้นทำงานสำหรับเอเจนต์ (Agent Startup & Flow Summary Rule Configuration)
- [x] อัปเดตไฟล์ [AGENTS.md](file:///c:/new-ns-scrap-erp/AGENTS.md) เพื่อกำหนดกฎ `MANDATORY STARTUP CHECK` (Peach) และกฎการเขียนสรุป Flow หลังการพัฒนา/ทดสอบ
- [x] อัปเดตไฟล์ [.agents/rules/peach.md](file:///c:/new-ns-scrap-erp/.agents/rules/peach.md) เพื่อใส่กฎเริ่มต้นภาษาไทยและเพิ่มกฎสรุป Flow หลัง UAT
- [x] ตรวจสอบความถูกต้องของเวิร์กสเปซด้วย `git diff --check`

### 🌐 5. ทดสอบ UAT ระบบทรัพย์สินถาวรผ่านเบราว์เซอร์ (Fixed Assets Browser UAT Testing)
- [x] ล็อกอินเข้าระบบด้วย User: watcharathat@gmail.com / Pass: p@ssw0rd บน localhost:3000
- [x] ตรวจสอบหน้าทะเบียนทรัพย์สิน (Asset Register) ทั้งบน Desktop และ Mobile (มี Parity ครบถ้วน)
- [x] ทดสอบการกรอกข้อมูลใน Register Modal และตรวจสอบการลบ Browser Focus Ring สีดำ
- [x] ทดสอบการคิดค่าเสื่อมราคา (Depreciation) ทั้ง Preview, Commit Run และการย้อนกลับประวัติ (Reverse)
- [x] ตรวจสอบหน้าจำหน่ายทรัพย์สิน (Asset Disposal) และรายละเอียดของปุ่ม Modal/การ์ด KPI สถิติ
- [x] บันทึกหลักฐานรูปภาพหน้าจอ (Screenshots) และจัดทำเอกสาร [walkthrough.md](file:///C:/Users/pc/.gemini/antigravity-ide/brain/34e65c78-f864-46fc-8a6e-2f01f7ee2107/walkthrough.md)
