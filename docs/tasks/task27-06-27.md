# Task Checklist 2026-06-27

## ภารกิจ: แก้ปัญหา Playwright Chromium binary หายใน runtime container (ns-dev.devkub.com)

### บริบทปัญหา
- LINE notification (`/api/daily/weight-tickets/[id]/notify-line`) คืน 502 บน devkub
- error: `browserType.launch: Executable doesn't exist at /root/.cache/ms-playwright/chromium_headless_shell-1223/...`
- สาเหตุ: `.playwright-browsers/` (build artifact จาก `scripts/install-playwright.js`) ไม่อยู่ใน runtime container
  เพราะ Docker multi-stage ไม่ copy ข้าม stage, และ runtime path logic ปัจจุบันเป็น best-effort guess
  (commit 70270279, b98a6c44) ที่ไม่ throw เมื่อหาไม่เจอ → Playwright fallback ไป default path ที่ว่างเปล่า
- localhost ใช้ได้เพราะ `.playwright-browsers/` อยู่ในเครื่องจากการรัน build ครั้งล่าสุด

### Checklist (แผน A — แก้ฝั่งโค้ดให้ resolve path แข็งแรง + health endpoint)
- [ ] Sync upstream `new-origin/dev` (0/0 clean แล้ว)
- [ ] แก้ `apps/next/src/lib/server/weight-ticket-line-notification.ts` (~L435-453):
      resolve PLAYWRIGHT_BROWSERS_PATH ตามลำดับ env var → candidates → throw ชัดเจนเมื่อหาไม่เจอ
- [ ] แก้ `scripts/install-playwright.js`: เขียน marker file `.playwright-browsers/.installed` + log path ชัดเจน
- [ ] แก้ `apps/next/src/app/api/health/route.ts`: เพิ่ม playwright binary check
- [ ] รัน `npm run type-check --workspace @ns-scrap-erp/next` ผ่าน
- [ ] รัน `npm run lint --workspace @ns-scrap-erp/next` ผ่าน
- [ ] รัน `npm run build --workspace @ns-scrap-erp/next` ผ่าน
- [ ] บันทึก Flow summary ลง `docs/notes/`
- [ ] รายงานผล + ขออนุมัติ commit/push ขึ้น `new-origin/dev`

### ขอบเขตที่ทำ (Scope) — batch 1 (Playwright path)
- ✅ แก้โค้ดฝั่ง runtime path resolution + health endpoint
- ❌ ไม่รัน browser UAT (รอ Dockerfile แก้ฝั่ง infra)
- ❌ ไม่ commit/push เอง (รออนุมัติ)
- ✅ commit `7ccb0af8` push ขึ้น new-origin/dev แล้ว

---

## Batch 2 — Build Version Badge บนหน้า /admin/line-settings

### บริบทปัญหา batch 2
- หลัง deploy batch 1 (commit 7ccb0af8) พบว่า worker บางตัวยังรันโค้ดเก่าอยู่ (job 65 WTI012606-0020 ตอน 10:07 ยังขึ้น error Playwright ภาษาอังกฤษเดิม)
- ปัญหา: ไม่มีทางยืนยันด้วยสายตาว่า "หน้า/worker ที่กำลังดู" ใช้โค้ด commit ไหนอยู่จริง
- ทางแก้ของ user: เพิ่มเลข version + commit hash บนหน้า line-settings

### Checklist (batch 2)
- [x] Sync upstream `new-origin/dev` (0/0 clean)
- [x] bump `apps/next/package.json` version `0.1.0` → `1.0.0`
- [x] แก้ `apps/next/next.config.mjs`: inject `NEXT_PUBLIC_BUILD_COMMIT` + `NEXT_PUBLIC_BUILD_VERSION` ตอน build ด้วย `execSync('git rev-parse --short HEAD')`
- [x] แก้ `apps/next/src/app/admin/line-settings/LineSettingsPageClient.tsx`: เพิ่ม badge `v1.0.0 · 7ccb0af8` ข้าง environment badge
- [x] รัน `npm run type-check --workspace @ns-scrap-erp/next` ผ่าน
- [x] รัน `npm run lint --workspace @ns-scrap-erp/next` ผ่าน
- [x] รัน `npm run build --workspace @ns-scrap-erp/next` ผ่าน (verify commit hash ถูก inject)
- [x] commit `acdd03f8` push ขึ้น new-origin/dev แล้ว

### ปัญหาที่พบหลัง deploy batch 2
- badge version `v1.0.0` ขึ้นถูกต้อง ✅
- แต่ commit hash ขึ้นเป็น `unknown` ⚠️ — เพราะ Docker build ใน watchpc ไม่มี `.git/` directory
  (shallow clone / copy source แบบไม่มี git metadata) → `git rev-parse` พัง → fallback เป็น `unknown`

---

## Batch 3 — Multi-fallback commit hash (จับ commit hash ให้ได้ในทุกกรณี)

### Checklist (batch 3)
- [ ] Sync upstream `new-origin/dev` (0/0 clean)
- [ ] แก้ `scripts/install-playwright.js`: เขียน commit hash (จาก git rev-parse) ลง marker file `.installed` ตอน build
- [ ] แก้ `apps/next/next.config.mjs`: เปลี่ยน `readShortCommitHash()` เป็น multi-fallback:
      env var → git rev-parse → marker file `.installed.commit` → build time short stamp
- [x] รัน type-check + lint + build + verify commit hash จับได้ใน local
- [x] commit `9aa72388` push ขึ้น new-origin/dev แล้ว (marker commit=null เพราะ Docker ไม่มี .git/)

---

## Batch 4 — Runtime Visibility + Process Consistency + Graceful Degradation

### บริบท batch 4
- หลัง deploy batch 1-3, health บอก `ready: true` แต่ worker ยัง error `/root/.cache/...`
- ChatGPT diagnosis: warm worker + env ตอน runtime
- สำรวจจริง: call site เดียว (line 492), ไม่มี worker แยก

### Checklist (batch 4)
- [x] Sync upstream `new-origin/dev` (0/0 clean)
- [x] Step 1: ทำ `launchManagedChromium()` helper — resolve + set env + throw fingerprint
- [x] Step 2: เพิ่ม runtime fingerprint log `[LINE_SEND_WORKER_BEFORE_PLAYWRIGHT]`
- [x] Step 4: Graceful degradation — PDF gen แยก try/catch, พังก็ยังส่ง LINE ได้
- [x] รัน type-check + lint + build ผ่าน 100%
- [x] อัปเดต flow summary ลง docs/notes/
- [ ] รายงานผล + ขออนุมัติ commit/push ขึ้น `new-origin/dev`
