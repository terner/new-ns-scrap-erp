# Active FCD Foreign Receipt Batch 2026-07-30

Objective: เพิ่ม foreign receipt/FCD โดยใช้ native currency เป็น subledger และ book amount เป็นยอดหลัก โดยไม่มี fallback หรือ hardcode สกุลเงิน/rate/account mapping.

Active batch: `finance_currency_policies` เป็น source of truth แบบ singleton สำหรับ functional currency และตั้งค่า `THB` จาก Currency Master แล้วทั้ง dev-target/SIT. Runtime reader fail closed หากไม่มีหรือมีเกินหนึ่ง policy row. Receipt จะใช้ rate จาก API ตามวันรับเงิน, อนุญาตกรอก/แก้เอง และบันทึก snapshot ที่ใช้จริงโดยไม่มี latest-rate fallback. ยอด native/book คำนวณและแสดง 2 ตำแหน่ง; FX rate รับได้สูงสุด 3 ตำแหน่ง. FCD OD ใช้วงเงินต่อบัญชี. Carrying rate ใช้ moving weighted average ต่อ account+currency. เพื่อรักษา compatibility ของรายงานเดิม foreign receipt จะเขียนยอด THB ที่คำนวณแล้วลง `bank_statement.amount_in/out` และ mirror กับ `book_amount_*`; native/rate อยู่เฉพาะ FCD subledger/audit ไม่ย้าย reader เดิมไปอ่าน `book_amount_*`.

Blockers: เหลือ `FCD-908` รอคำสั่ง browser/UAT เท่านั้น. ห้ามใช้ account currency, `THB` หรือ `USD` เป็น fallback ใน runtime. GL journal, chart-of-account mapping และ GL reconciliation ไม่อยู่ใน active FCD scope: ระบบปัจจุบันไม่มี GL posting engine และยังไม่มี requirement ให้สร้าง.

Write areas: FCD schema/ledger migration, money/rate/posting services, Customer Receipt API/UI, Bank Statement/Cash Position readers, FCD conversion/revaluation and their reconciliation/tests.

Validation: policy migration plus FCD contract migration `20260730120000_add_fcd_transaction_ledger_contract.sql` applied/recorded in dev-target and SIT. Migrations `20260730130000_persist_customer_receipt_fx_rate_type.sql`, `20260730140000_enforce_canonical_bank_statement_facts.sql`, `20260730150000_lock_fcd_posted_revaluation_periods.sql`, `20260730160000_enforce_foreign_customer_receipt_contract.sql` and `20260730170000_allow_foreign_customer_advance_receipts.sql` are also applied/recorded in Dev/SIT. The period lock blocks receipt/conversion ledger events dated on or before an active posted revaluation for the same account+currency; the revaluation/reversal events themselves remain append-only exceptions. Deferred receipt/split guards require foreign RCP to reconcile settlement/carrying/bank fee/allocation values, require each split to link matching FCD/BST facts, and reject mixed SB/CADV allocation. Migration 170 extends that contract to CADV: its settlement THB must reconcile exactly to CADV allocation, without AR settlement FX or automatic overpayment. Dev/SIT preflight was zero invalid rows; no transaction was backfilled. Prisma generate, targeted lint/type-check, `git diff --check`, focused FCD tests `31/31`, and focused receipt tests `13/13` passed before the current UI/read-model follow-up.

Immediate next task: ทุก task ที่ทำได้ถูกปิดและ promote Dev/SIT แล้ว; `FCD-909` ยืนยัน migration history `20260730210000/20260730220000`, lint/type-check/build artifact และ push `new-origin/dev` + `sit-origin/main`. เหลือ `FCD-908` รอคำสั่ง browser UAT เท่านั้น. GL posting tasks ถูกปิดออกจาก FCD scope เพราะระบบไม่มี GL engine และไม่มี requirement แยก. `FCD-903` service integration ครอบคลุม THB receipt/cancel, foreign partial/multiple bills, Bank Fee, settlement FX และ foreign cancel. Multi-bill customer receipt ใช้ named 30-second interactive transaction option เพื่อให้การเขียน RCP/BST/FCD/allocation อยู่ใน transaction เดียวโดยไม่หมด default 5 seconds. Migration `20260730220000_allow_cancelled_foreign_receipt_contract.sql` ทำให้ deferred guard ตรวจ reconciliation เฉพาะ receipt ที่ active; cancellation ยังคง append reversal และเก็บ allocation history เป็น cancelled. `FCD-904` ยืนยัน concurrent conversion บน Dev/SIT แล้ว: OD สูงไม่ถูกนำมาแทน native FCD balance และ cleanup เป็นศูนย์. `FCD-905` ยืนยัน lifecycle receipt -> revaluation -> conversion -> reversals แล้วทั้ง Dev/SIT. Migration `20260730210000_fix_foreign_receipt_split_statement_currency.sql` แก้ deferred foreign-receipt split trigger ให้ใช้ `bank_statement.movement_currency_code` ตาม schema จริง; ก่อนแก้ trigger นี้ foreign receipt จะ commit ไม่ได้. `FCD-113` applied/recorded ใน Dev/SIT แล้ว: Account Master ไม่มี currency opening balance และยอดคงเหลือเริ่มจาก Bank Statement/FCD ledger ที่ post เท่านั้น. RCP history/print/LINE ใช้ named THB book amounts; native currency/rate อยู่เฉพาะ foreign audit snapshot. Cash Position อ่าน THB ที่ persist แล้วเท่านั้น; FCD native เป็น projection แยก account+currency ไม่รวมเข้ากับ THB. `/sales/receipts` รองรับ foreign ทั้ง SB และ CADV ตาม snapshot rate/date/type, manual/override reason, FCD split ที่รองรับ currency, และแยก Bank Fee จาก settlement FX. Conversion/revaluation ใช้ append-only reversal และ internal transfer classification เดียวกัน. FX report drilldown เปิด FCD conversion/revaluation ตามเลขเอกสาร และเปิด FCD ledger ตาม account+currency+entry ที่ persist; FX row เก่าเปิด Bank Statement ต้นทางเท่านั้น.

## Form Safety SIT Checkpoint 2026-07-30

`codex/nserp-176-form-safety-sit` adds the shared unsaved-change and destructive-action confirmation contract across the selected form flows, while preserving the current SIT WTI/WTO product-card, picker, sheet-animation, and Escape-close behavior. The receipt source guard treats an empty THB/FCD receipt as blank and still protects genuine foreign-currency/rate data. Receipt source type, currency, date, branch, customer, Sales Bill replacement, and CADV replacement now defer only their destructive resets to confirmation; an unchanged value, including reselecting the same source type or the same SB/CADV document while retaining its manually edited row values, or blank/retained state remains free to change. After the `sit-origin/main` semantic merge, focused Vitest passed 82 tests across 20 files; 4 integration tests were skipped because this worktree has no integration environment. Workspace lint has 0 errors (10 existing warnings), the 4 GB workspace type-check passes, and `git diff --cached --check` passes. Production build remains unproven in this isolated worktree because its dependency symlink is invalid for Turbopack and cannot resolve Tailwind for Webpack.

SIT push blocker: local Git HTTPS used SChannel and `git-remote-https.exe` crashed with `SEC_E_NO_CREDENTIALS`; GCM also could not use the Windows Credential Manager store. This worktree now uses the local-only reversible settings `http.sslBackend=openssl` and `credential.credentialStore=dpapi`, which restore GitHub reads and credential-store access, but GCM device OAuth still fails at the Windows security provider. No push has been attempted after this failure.

# Active Dev/SIT Transaction Reset Checkpoint 2026-07-29

Objective: ลบข้อมูล transaction และ derived transaction data ทั้งหมดบน Dev และ SIT เพื่อเริ่มทดสอบ flow code ใหม่ โดยคง master data, auth/permission, settings และ migration history ไว้.

Completed: truncate แบบ transactional ของ transaction tables 79 ตารางใน Dev และ SIT สำเร็จ; postflight ทุกตารางเป้าหมายเป็น 0 แถว. Accounts 12, products 236, branches 2 และ migration history ยังคงอยู่ครบทั้งสอง environment. Dev มี app users 27 ราย, SIT มี 24 ราย.

Immediate next task: ตรวจ SIT runtime smoke หลัง reset แล้วเริ่มสร้าง transaction ใหม่ตาม flow ที่ไม่มี fallback.

## Super Admin Authorization Checkpoint 2026-07-29

`system_admin` is now the Super Admin role. The application authorization context treats it as an admin bypass, and migration `20260729100000_promote_system_admin_to_super_admin.sql` updates the role label/flags and backfills every active permission grant in Dev and SIT. Postflight confirms `system_admin = Super Admin`, 122 active permission grants, and `daily.weight_tickets.open_bill` present in both environments. No transaction data changed.

WTI open-bill handoff fix 2026-07-29: `/purchase/bills?new=1&wti=...` now loads purchase options before resolving the WTI source. Previously the auto-open effect searched the still-empty client option state and showed a false `ไม่พบ WTI` error even when the SIT WTI was `received`, had product summaries, and had no active allocation. What is what: the list button only creates the deep link; the purchase page owns source preload. Why it has to be like this: preload must finish before source lookup so the handoff uses the same option contract as manual WTI selection.

---

# Active Production PO Event Identity Batch 2026-07-29

Objective: ทำให้ `PO` เป็นเลขเอกสารการผลิตเพียงตัวเดียว โดย PO ต้องมีรหัสสาขา; input/output/return/void เป็น event ภายใน PO และรอบรับผลผลิตแสดงเป็น `PO.../01`, `PO.../02` โดยไม่มีเลข `PI`, `PO2`, `PI-REV`, `PO2-REV` และไม่มี fallback.

Plan: `docs/superpowers/plans/2026-07-28-production-event-identity.md`. Batch 1 เพิ่ม contract helper และ unit test ผ่าน `6/6`; Batch 2-4 ปรับ schema/write path/API/UI/ledger reference แล้ว.

Write areas: `apps/next/src/lib/server/production-orders.ts`, production API/UI/report/reconciliation, Prisma schema/migration, stock ledger contracts, production flow notes. ไม่ backfill ข้อมูลเก่า.

Validation: focused production tests `25/25`, full lint `0 errors/6 warnings`, type-check, production build และ `git diff --check` ผ่าน. Full Vitest ยังมี 37 failures จาก 12 suites และต้อง triage ก่อนปิด batch.

Environment/migration checkpoint: แก้ env ให้เหลือชุด canonical ใน `apps/next/.env.local`, `apps/next/.env.sit.local`, `apps/next/.env.uat.local`; ลบ root `.env.local` และ `apps/next/.env` ที่ซ้ำ/เก่า. Migration `20260728110000_add_production_event_identity.sql` apply และ record ใน Dev/SIT/UAT แล้ว; postflight ผ่าน 3 columns, 3 indexes และ 1 migration-history row ต่อ environment.

Git checkpoint: commit `0a0d7a24` ถูก push ไป `new-origin/dev` และ `sit-origin/main` แล้ว; worktree สะอาด. Customer UAT ยังไม่ได้รับ code promotion.

Immediate next tasks after reset: (1) verify deployed SIT runtime and production flow smoke, (2) triage/fix or explicitly classify the 37 full-test failures, (3) re-run full validation, and (4) promote code to customer UAT only after SIT sign-off.

# Active Strict Branch-Coded ADV Batch 2026-07-28

Objective: เอา fallback `00` ออกจากเลข ADV และบังคับใช้รหัสสาขาที่ active เท่านั้น.

Checkpoint: `nextAdvanceDocNo` reject เมื่อ branch code ไม่มี/ผิดรูปแบบ; ไม่มี migration เพิ่มเพราะ `supplier_advance_payments.branch_id` เป็น required อยู่แล้ว. Type-check, lint และ `git diff --check` ต้องผ่านหลังแก้.

# Active Branch-Coded Payment Approval Batch 2026-07-28

Objective: เพิ่ม `branch_id` ใน PMA, ออกเลข `PMA<branch><YYMM>-####`, และกรองตาราง PMA ด้วยสาขาของ PMA โดยตรง โดยไม่ใช้ fallback.

Checkpoint: migration `20260728100000_add_branch_to_payment_approvals.sql` apply/record ใน dev-target กับ SIT แล้ว. แก้ PMA ทุก source และ petty-advance return ให้บังคับสาขา; type-check, lint และ `git diff --check` ผ่าน. ยังไม่ได้ทำ browser UAT หรือ push code.

# Active Branch-Coded Document Flow Batch 2026-07-28

Objective: เพิ่มเลขสาขาในเลขที่เอกสาร BST, TRF, TCS และ SP ให้ flow code บังคับเลือกสาขา โดยไม่ใช้ fallback และไม่ backfill ข้อมูลเก่า.

Checkpoint: implementation และ migration `20260728090000_add_branch_to_bst_trf_tcs_sp.sql` เสร็จแล้วและ apply/record ใน dev-target กับ SIT. Type-check, lint และ `git diff --check` ผ่าน. ยังไม่ได้ทำ browser UAT หรือ push code ตามคำขอในรอบนี้.

# Active Production Dashboard Query Separation Batch 2026-07-23

Objective: แยก query/service ของ `/production/dashboard` ออกจาก shared `production-reports.ts` โดยคง API contract เดิมของหน้าไว้ และแก้ branch scope, WIP scope, aggregation correctness, BigInt serialization, cache header และ test coverage.

Approved design: batch นี้โฟกัสเฉพาะ `/production/dashboard` และ `GET /api/production/dashboard`; ยังไม่ refactor `/production/report`, `/api/production/report` หรือ `/api/production/machine-utilization`. ใช้ `production-dashboard.service.ts` และ query module เฉพาะหน้า โดยใช้ shared module เฉพาะ scope/ledger/serialization ที่ไม่เปลี่ยน behavior ของหน้าอื่น.

Task list: `docs/notes/page-flows/production-production-dashboard.md` หัวข้อ `Dashboard Query Separation Task List 2026-07-23`.

Write areas: `apps/next/src/app/api/production/dashboard/route.ts`, new dashboard service/query/serializer modules, focused dashboard tests, and only targeted DB migration/index changes after `EXPLAIN ANALYZE`.

Checkpoint: Dashboard query separation batch completed for `DASH-01` through `DASH-12`. Focused production tests `19/19`, production formula verification `ok: true` for 7 rows, workspace type-check, production build, and `git diff --check` pass. Workspace lint has zero errors with four existing warnings. No DB migration was needed after `EXPLAIN ANALYZE` review.

# Active Profit & Cost Performance Batch 2026-07-19

WTI/WTO gallery checkpoint 2026-07-22: detail row galleries now form one continuous sequence across image-bearing lots in table order. Opening a row starts at that lot, Next moves from the last image of one lot to the first image of the next, and the heading follows the active lot; the separate document-level album is unchanged. This batch intentionally excludes working-draft autosave, team visibility, recovery, APIs, and migrations from the SIT promotion.

Shared row-action SIT checkpoint 2026-07-22: runtime `จัดการ` columns use the shared Desktop ellipsis and full-width Mobile button. The opened menu matches the Mobile trigger width, centers each label between a fixed left icon and equal trailing spacer, uses subtle row dividers, keeps destructive commands red, and stops portalled menu clicks from opening the parent row/modal. Existing permissions, disabled states, confirmations, and business handlers remain unchanged.

## Accountant Default Landing And PO Permission Split 2026-07-24

Implemented locally and applied transactionally to dev-target: Role
`accountant` uses `/daily/expense-dashboard` as its default landing page, while
PO Buy and PO Sell use independent `view`, `create`, `update`, `cancel`, and
`short_close` permissions instead of `finance.cash.view`. Migrations
`20260724020000_split_po_buy_sell_permissions.sql` and
`20260724120000_set_accountant_expense_dashboard_landing.sql` are recorded in
dev-target migration history. The PO migration preserved existing access with
10 active permission rows and 60 copied grants across 6 Roles; the accountant
migration fails closed unless the Role already holds
`reports.expense_dashboard.view`.

Objective: cut `/profit-cost-analysis` over from transaction-wide Node aggregation to strict PostgreSQL fact/daily read models with split APIs, applied filters, request cancellation, branch scope, and server pagination/sort.

Active batch: implementation is promoted to Dev and SIT, and migration `20260719160000_create_profit_cost_reporting_read_model.sql` is applied to both dev-target and SIT. SIT backfill produced 266 facts and 135 daily rows; reconciliation reports zero issues and exact purchase/revenue/COGS parity. Focused Vitest is 16/16 after the Dev/SIT semantic merges, full lint and source type-check pass, and `git diff --check` passes. Summary, rankings, and the active table fail independently; alerts are evaluated directly from the full scoped daily read model rather than a capped product page. No browser/UAT run was requested.

Latest decisions: financial/report facts are L5 and remain `private, no-store`; no Redis/browser fact cache; daily rollup owns summary/product numeric aggregation; fact ledger remains the source for distinct document counts; AP/AR remain direct PB/SB balance reads; old aggregate endpoint is retired with `410 Gone`.

Blocker: the full Next build reaches successful Webpack compilation but fails route-type validation on the pre-existing export `getCostPoolRowsData` in `dual-costing/cost-pool/route.ts`; this file is unchanged by the batch. Immediate next task: perform deployed SIT smoke verification when explicitly requested; customer UAT migration/promotion remains separate and pending explicit instruction.

## Previous Active Access Control Batch

Objective: implement the approved Access Control Module design across User Admin, Security Admin, multiple roles, action permissions, and branch-scoped finance flows.

## Active Sales Plan UX Batch 2026-07-19

Objective: ปรับ `/sales-plan` ตาม UX audit โดยคงหน้าเดียวและ line-tab วิเคราะห์ 2 มุมมองเดิม ไม่แยกเป็น 4 tabs ระดับหน้า; แก้ wording/status, LME/assumption grouping, KPI, modal, filter, table, dark-mode classes และ no-fallback calculation contract.

Write areas: `apps/next/src/components/main/MainSalesControlClients.tsx`, `apps/next/src/lib/server/main-sales-control.ts`, `docs/notes/page-flows/main-dashboard-reports-sales-plan.md`.

Completed locally: UI restructuring and Thai status wording; server analysis now uses only same-product Sales Plan price/% LME and returns no projected price/profit when no plan exists.

Validation checkpoint 2026-07-27: fixed the stale `periodFrom`/`periodTo` references in `buildSalesCommission`; the API now returns its existing string date range (`from`/`to`). Workspace type-check and production build pass; lint has zero errors with the existing warnings listed in the command output.

Completed locally: effective-permission helper/tests; action catalog and legacy-permission mapping migrations; User Admin/Security Admin split; multi-role user assignment; action checks for petty advance, payment approval, WTI open-bill buttons, purchase/sales bills, supplier payment, customer receipts, supplier ADV and daily expenses.

Validation: targeted ESLint, workspace type-check, focused Vitest `17/17`, and scoped diff checks pass.

Completed: four access-control migrations are now applied to dev-target and SIT with controlled Supabase CLI workdirs; postflight confirmed the checked action catalog and admin/owner grants in both environments.
Blocker/next: continue the remaining broad finance-route audit documented in `docs/notes/access-control-broad-permission-audit-2026-07-19.md`.

# 00 Current Work

## Active Production Output Posting Batch 2026-07-24

Production output draft persistence fix 2026-07-24: draft loading no longer overwrites edits made while the initial GET is still pending, and PUT saves are serialized per modal so an older autosave cannot overwrite a newer added/removed row. The draft table migration is applied to dev-target and the local Next server was restarted after Prisma generation. Required validation after this client fix: workspace type-check, lint, build, and diff-check; authenticated browser UAT remains pending.

Production output table alignment 2026-07-24: `ผลลัพธ์จากการผลิต` and `รายการผลผลิตที่เตรียมส่งเข้าคลัง` now use the same columns and order: product, category, receiving warehouse, output quantity, loss, production date, WIP used, notes, and action. Draft rows use the current form date/WIP/notes; posted rows use their persisted values and expose the void action.

Objective: ทำให้การรับผลผลิตเป็น posted movement ที่รับเข้าคลังทันที รองรับการผลิตหลายรอบ และแก้ไขด้วย void/repost โดยไม่แก้ข้อมูล posted เดิม.

Decision correction 2026-07-28: ใบสั่งผลิตมีสินค้าหลักที่ต้องการผลิต แต่ actual output อาจมีสินค้าหลักและสินค้าอื่นร่วมกันได้. WIP ledger ต้องผูกกับสินค้า/ประเภท/คลังต้นทางของวัตถุดิบที่เบิกเข้า WIP ไม่ใช่ target product. การตรวจปริมาณใช้ `ผลผลิตรวม + loss` เทียบกับ WIP ที่ใช้; loss และผลผลิตที่ต่ำกว่าวัตถุดิบเป็นกรณีปกติที่ต้องยืนยันได้ ไม่ใช่ validation error โดยตัวมันเอง. Migration/เอกสารเดิมที่ normalize WIP เป็น target product ต้อง audit ก่อนใช้งานต่อ.

Latest checkpoint 2026-07-24: เพิ่ม advisory lock ระดับใบสั่งผลิตและ stock scope, ป้องกัน WIP source ซ้ำทั้ง draft/final, เพิ่มรายละเอียด source WIP ใน timeline, และเมื่อกดจบงานขณะ WIP เหลือ ระบบถามยืนยันแล้วคืนยอดคงเหลือกลับคลังต้นทางด้วย `PI-RETURN` ใน transaction เดียวกันก่อนตั้ง `Completed`. แก้ UAT bug ที่พยายามเขียนสถานะ input เป็น `returned` ซึ่ง DB ไม่อนุญาต; input คง `active` เพื่อรักษาประวัติ และยอดคืนอ่านจาก `production_input_returns`. Migration `20260724150000_reconcile_production_wip_ledger_product_dimension.sql` ถูก apply และบันทึกใน dev-target. UAT ผ่านกรณีปิดงาน PO2607-0010: dialog แสดง, API สำเร็จ, WIP เป็น 0, stock-in/ledger และ timeline มีรายการคืนอัตโนมัติ. Focused contract test 3/3, type-check, lint, build 323/323 และ diff-check ผ่าน.

SIT DB parity checkpoint 2026-07-24: backed up SIT to `/tmp/ns-erp-sit-before-dev-apply-20260724-155927`, applied the missing production input/output migrations through `20260724150000`, and patched schema parity items that dev already had through direct DDL. Dev-target migration history was also marked for `20260716190000` after confirming the schema was already present and valid. Follow-up parity sync backed up SIT permission/role/history data to `/tmp/ns-erp-sit-before-history-data-sync-20260724-163104`, synced missing permission catalog rows, role grants, role landing defaults, and 12 migration-history versions from dev to SIT, then backed up dev role grants to `/tmp/ns-erp-dev-before-sit-extra-grants-20260724-163251` and copied the 11 SIT-only role grants back to dev. Final comparison found no dev/SIT drift for checked schema catalog, auth seed rows, or migration-history versions/names.

Completed in this batch: แยกการจบงานออกจากการบันทึกผลผลิต, เพิ่ม server validation กัน WIP source ซ้ำ, เพิ่ม `POST /api/production/orders/[docNo]/outputs/[outputDocNo]/void`, เพิ่ม action `ยกเลิกผลผลิต` ในตารางผลลัพธ์, เพิ่ม staging table `รายการผลผลิตที่เตรียมส่งเข้าคลัง` ก่อนโพสต์, กระจาย WIP allocation ให้หลายรายการผลผลิตโดยไม่ตัดซ้ำ, และเพิ่ม confirmation เมื่อ `ผลผลิต + สูญเสีย` ไม่เท่ากับ WIP ที่ใช้ ก่อนส่งเข้าคลัง.

Production input return cost policy 2026-07-24: when different input rounds are pooled in WIP and the original layer cannot be identified, `คืนวัตถุดิบ` uses the current WIP WAC per separate `สินค้า + RM/FG + คลังต้นทาง` pool. The modal shows WIP average cost and estimated return value; paired WIP-out/source-stock-in ledger rows use the same WAC, preserving total inventory value while recalculating destination-warehouse WAC. One return button opens all input documents in the same Pool.

Remaining: รัน authenticated browser UAT flow เพิ่มผลผลิต/void/downstream guard/reconciliation และจบงานพร้อมคืน WIP; ตรวจ duplicate/unique contract ของ `production_orders.doc_no` และ event identity แบบ read-only ก่อนเพิ่ม constraint. ไม่ migrate/backfill ข้อมูลทดสอบตามที่ตัดสินใจไว้.

Write areas: `apps/next/src/lib/server/production-orders.ts`, production output route/UI, `docs/notes/page-flows/production-production-orders.md`.

Required validation: workspace lint, type-check, build, `git diff --check`, followed by authenticated browser UAT before promotion.

Production multi-source WIP entry 2026-07-24: the production form now supports adding multiple WIP source rows, each with source product/category/warehouse and quantity. The API accepts `sourceWipLines[]`; output/loss consumption is allocated across those sources and persisted in `production_outputs.source_wip_allocations`. Migration `20260724110000_add_production_output_wip_allocations` is applied, recorded, and postflight-confirmed on dev-target.

Production output action cleanup 2026-07-24: disabled the shared WIP-summary action column in the `ผลผลิต` tab as well as the output-result table. `คืนวัตถุดิบ` remains available only in the `วัตถุดิบเบิก` tab.

Production modal header wrapping 2026-07-24: the detail header now keeps document number, status, product, and branch on one flex line when space allows, wrapping only the remaining text when the available width is insufficient.

Production output target field 2026-07-24: removed the read-only `สินค้าที่ผลิต` field from the production-entry form because the target product is already fixed by the production order and does not need to be selected or repeated.

Production WIP source label 2026-07-24: the production form's WIP selector now displays only `สินค้า - RM/FG`; the separate read-only `ประเภทสินค้า` field was removed because the category is already part of the selector label. Source warehouse and category values remain persisted and are used by the server.

Production output tab order 2026-07-24: moved `ผลลัพธ์จากการผลิต` to the top, followed by `สรุปวัตถุดิบใน WIP` and `ข้อมูลการผลิต`. Runtime behavior and API contracts are unchanged.

Production output correction policy 2026-07-28: removed all public `reverse` routes and terminology. Input correction uses `คืนวัตถุดิบ`; posted output correction uses `void` and writes internal append-only compensation events under the same PO. Historical reverse/ref-type values remain read-only for audit and no data migration is performed in this batch.

Production output section layout 2026-07-24: the `ผลผลิต` tab now renders `สรุปวัตถุดิบใน WIP` before the `ข้อมูลการผลิต` entry section. The optional `จบงานหลังรับ` checkbox was removed from the form; output posting and explicit order completion remain separate actions.

Production timeline notes 2026-07-24: creation notes are attached to the `created` status event and production notes to the `output_created` / `completed` event, so both are shown in the persisted production-order timeline.

Production WIP table layout 2026-07-24: moved WIP source and production-use quantity into editable table rows, matching the purchase/sales bill pattern. The table keeps an entry row visible, supports `+ เพิ่มรายการ`, row editing, and row removal before save.

Production input multi-line entry 2026-07-24: the input tab now lets users add multiple raw-material/product, source-warehouse, category, and quantity rows before one save. The existing transactional `lines[]` API is reused; each row is validated and costed independently. Type-check, lint, production build, and diff-check pass.

Production input return fix 2026-07-24: `POST /api/production/orders/[docNo]/inputs/return` first returned 403 because dev-target lacked `production.orders.input_return` and its grants, then returned 500 because the dev schema lacked the three stock-receipt cost columns used by `production_input_returns`. Applied and recorded `20260723173500_add_production_input_return_permission`, `20260723200000_grant_production_input_return_to_system_admin`, and `20260723190000_separate_production_and_stock_receipt_costs` in dev-target. Postflight confirms the permission is granted to the four production roles and all three return-cost columns exist. No production order, stock, or ledger rows were changed; retry the real return flow next.

Production order timeline detail 2026-07-24: `input_created` history events now expose document number, issued quantity, total/average issued cost, and per-line product/category/warehouse/cost details for expandable display. Timeline actor labels resolve from `app_users` first/last name or display name and no longer show the actor email. No ledger or transaction behavior changed.

Production order date semantics 2026-07-24: create modal no longer accepts `วันที่ใบสั่งผลิต`; the server sets the order date/document period from Bangkok time at save and stores the exact creation timestamp. List/card/detail now label the first user-selected output date as `วันที่เริ่มผลิต`; orders with no output show `-` and do not fall back to the order creation or input date.

Production output form 2026-07-24: the output tab now presents WIP summary before the production-entry form and result table. The form selects the WIP source/type, records `sourceWipQty` separately from output quantity, validates loss against the used WIP, and the result table groups output/loss rows by production round. Migration `20260724100000_add_production_output_wip_source` is applied and recorded in dev-target so each new output round stores source product/category/warehouse traceability.

Production Orders review batch 2026-07-23: completed the list/detail, permission, input, return, output, timeline, and reconciliation work recorded in `docs/notes/page-flows/production-production-orders.md`. The current contract is PO-only: input correction uses a return event under the PO, preserves the original RM/FG pool identity and current WIP WAC, and does not create a new reverse document. The detail API can read PO input payloads, and the detail modal shows creation date/time from `created_at`; `คลังรับผลผลิต` stays blank until an active output has a destination warehouse, with multiple destination warehouses shown as unique names without branch codes or round counts.
After creating an order, the list client now resets to page 1 and requests page 1 explicitly before showing the refreshed table; this does not require restarting the local tmux dev server.

WTI concurrent draft design checkpoint 2026-07-23: ออกแบบให้ WTI รองรับผู้ใช้ 2 คนจาก 2 ตราชั่งเพิ่ม/แก้/ลบสินค้า เต๋า น้ำหนัก รูป และสิ่งเจือปนใน draft เดียวกันด้วย line-level operations + server-side summary + realtime event ต่อเอกสาร; ข้อมูลหัวเอกสารและข้อมูลอื่นยังใช้ปุ่มบันทึกแบบเดิม และปุ่มยกเลิกถามยืนยันเฉพาะ manual changes ที่ยังไม่ถูกบันทึก. Task list อยู่ใน `docs/notes/WTI-WTO Flow.md` ตั้งแต่ `WTI-00` ถึง `WTI-55`; รอบแรกเปิดใช้เฉพาะ WTI แต่ operation/version/event/reconnect contract ต้องออกแบบให้ WTO นำไปใช้ต่อได้. ยังไม่แก้โค้ดหรือ schema; รอผู้ใช้ review/อนุมัติ task list ก่อนทำ implementation plan.

Dashboard report query-slimming checkpoint 2026-07-19: Dashboard Overview และ Daily Report ใช้ lightweight sales-line reader เฉพาะ amount/product/qty และไม่เรียก allocation/trading/stock-cost pipeline ที่ไม่ใช่ consumer ของหน้านี้; สี่ report API เพิ่ม `Server-Timing` (`auth`, `service`, `total`) เพื่อแยกคอขวดจาก SIT. Type-check, lint (มี warning เดิมที่ `qa-thai-font.tsx`), production build `312/312` และ diff-check ผ่าน. Local re-measurement หลังแก้ยังติด `P1000` จาก database credential ใน env ปัจจุบัน; ต้องแก้ connection แล้ววัด parity/performance ซ้ำก่อน promotion. Browser/UAT ยังไม่ได้รันใน coding batch นี้.

Dashboard API separation checkpoint 2026-07-18: `/api/owner-daily`, `/api/daily-report`, `/api/dashboard-overview`, and `/api/analytics-dashboard` now have separate route/service boundaries and `private, no-store` report responses. Owner Daily no longer invokes finance dashboard, historical, sales line analytics, or production detail queries; Dashboard summary now disables payment/receipt, daily bank movement, loan, trading and production-detail reads while retaining dashboard KPI/aging/stock/historical plus dashboard-used analytics. Analytics Dashboard now reads only the four sales-line fields needed for quantity/product analytics, builds trend labels from PB/SB, and no longer reads the full allocation/trading/stock-ledger fact pipeline or bank statements. Local warm Owner Daily requests measured about 0.2-0.4s application time and returned HTTP 200. Current validation after the analytics optimization: workspace type-check, workspace lint (one existing Thai-font warning), production build `311/311`, focused contract test, and scoped diff check passed. Browser/UAT was not run in this coding batch.

Purchase bills options performance checkpoint on 2026-07-18: `/api/purchase/bills` now returns list rows and totals without modal options; `/api/purchase/bills/options?scope=reference` serves branch/warehouse references separately, while the full options endpoint is loaded only when the purchase form is opened and cached per authenticated browser user for five minutes. What is what: list data follows table filters, reference options follow branch permission scope, and full form options follow the modal lifecycle. Why it has to be like this: the previous combined request took about 4.7 seconds and blocked the branch dropdown behind ADV, PO Buy, WTI and approval work. Validation target: type-check, lint, build, and a repeat SIT waterfall measurement. Remaining risk: the full modal options query still loads up to 500 ADV, 500 PO Buy and 300 WTI records and needs a second profiling batch after this split.

## Purpose

ไฟล์นี้เป็น handoff สั้นสำหรับงานที่กำลัง active เท่านั้น ไม่ใช่ changelog

SIT `main` pre-push semantic-merge checkpoint on 2026-07-18: local checkpoint `d7a4301b2` was composed onto `sit-origin/main` base `99001d49f` in the isolated `codex/sit-main-promotion-20260718-012952` worktree. The resolution preserves the SIT reference-master cache, P&L WAC/Deal Cost/Compare surface, Balance Sheet layout and legacy-aligned Cash Flow Statement while retaining the accepted local finance branch authorization, strict filter hydration/stale-response guards, mobile draft/apply filters, Excel export, source drilldowns, asset-disposal P&L, Cash Flow Analysis layout, shared dropdowns, all six LINE notification flows and removal of `/daily/design-mockup`. What is what: this is a semantic composition of two valid histories rather than a whole-file `ours`/`theirs` replacement. Why it has to be like this: a direct overwrite would either discard newer SIT business/cache behavior or roll back accepted local UX/UI and LINE behavior.
Pre-push validation for the semantic merge: the six focused regression files passed `67/67`, the LINE Flex/post-commit contracts passed `14/14`, the production-orders cache contract passed `6/6`, full ESLint finished with zero errors and the existing `qa-thai-font.tsx` warning, the 4 GB TypeScript check passed, and the Next 16.2.10 Webpack production build compiled, type-checked and generated `307/307` static pages. Full Vitest from `apps/next` finished at `272/274`; the two remaining failures are the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, whose CSS/component/test files have no diff from `sit-origin/main`. The default Turbopack build could not run inside this temporary worktree because its root `node_modules` is an external junction; this is a worktree-only dependency-layout limitation, not a source compile error, and the temporary dependency junctions are ignored and excluded from the commit. Browser/DOM UAT was not run because this promotion request did not explicitly request browser testing.

Cash Flow Analysis independent-column layout checkpoint on 2026-07-18: the two row-aligned 2:1 analysis grids that left 97px and 210px holes between unequal-height cards were replaced by one three-column desktop shell with two content-height stacks. The primary 2/3 column now keeps profit-to-cash comparison followed immediately by the cash forecast, while the supporting 1/3 column keeps capital structure followed immediately by OD risk; mobile collapses the same stacks to one column with the existing 16px rhythm. What is what: the left column is the primary cash decision path and the right column is supporting composition/risk context. Why it has to be like this: independent stacks remove the broken mid-page whitespace without stretching short cards into empty containers or narrowing the forecast graph. This is layout grouping only; panel content, calculations, payloads, filters, links, permissions, exports, APIs, database schema and DB state are unchanged.
Validation for the independent-column layout: the focused finance report suite was confirmed red at `15/16` before the new grouping and green at `16/16` after it. Full ESLint passed with zero errors and the existing `qa-thai-font.tsx` warning; workspace type-check passed; `npm audit --audit-level=high` found zero vulnerabilities; and the 4 GB production build generated `307/307` static pages. Full Vitest finished at `229/231`; the two failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this layout batch and unchanged here. Post-change browser/DOM UAT was not run because the implementation request did not explicitly request browser testing.

Cash Flow Analysis forecast-summary typography checkpoint on 2026-07-18: the three horizon summaries below the stock-style forecast chart now use a readable decision hierarchy instead of rendering every supporting row at 11px. Horizon labels are 14px, projected cash balances are 15px, forecast inflow/outflow rows are 13px with 20px line height and tabular money, and net movement is 13.5px with stronger emphasis. What is what: the chart remains the trend surface, while these rows provide the exact current/7-day/30-day values needed to validate the graph. Why it has to be like this: live rendered measurement confirmed the former support labels and values were only 11px despite ample column width, making exact financial figures unnecessarily difficult to scan. This is typography and spacing only; graph geometry, formulas, payloads, filters, permissions, exports, APIs, database schema and DB state are unchanged.
Validation for the forecast-summary typography: the focused finance report suite passed `15/15`; targeted and full ESLint passed with zero errors and the existing `qa-thai-font.tsx` warning; workspace type-check passed; and, after the default 2 GB Node heap exhausted during a fresh TypeScript build pass, the unchanged 4 GB retry generated `307/307` static pages. `npm audit --audit-level=high` found zero vulnerabilities, and scoped whitespace/diff checks passed for all four touched files, including the untracked finance report test. Full Vitest finished at `228/230`; the two failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this typography batch and unchanged here. A later explicit layout review inspected the authenticated rendered page at `1440x900` and `390x844` with no console warning/error: the summary typography is readable, mobile stacks continuously, while the existing desktop analysis pairs remain an open layout gap because their card heights differ by 97px and 210px.

Cash Flow Analysis stock-style forecast chart checkpoint on 2026-07-17: after the user clarified that the forecast should look like a stock chart, the intermediate variance-bar direction was replaced with one responsive line chart for current, 7-day and 30-day projected cash. The chart uses a thin directional line, subtle horizontal/vertical grid, compact 3.5px points, proportional 0/7/30-day spacing, no gradient/area fill, and a disclosed locally scaled Y axis so a small cash movement remains visible beside a large balance. Red/green reflects whether final projected cash is below/above current cash, while exact projected cash, forecast inflow, forecast outflow and net movement (`expectedIn - expectedOut`) remain visible below the graph. What is what: this is a management forecast trend across three scenario checkpoints, not an actual daily price series. Why it has to be like this: the original thick blue line, oversized dots and filled area looked promotional, while the professional chart keeps the stock-style reading language without hiding exact finance context or pretending the Y axis starts at zero. This is presentation only; projection formulas, payload values, filters, permissions, exports, source facts, database schema and DB state are unchanged.
Validation for the stock-style forecast chart: the revised line-chart contract was confirmed red against the variance graph and green after the stock-style replacement; the focused finance report suite passed `15/15`, covering line direction, valid path geometry, compact points, no gradient/area fill, exact net movement and non-finite safety. Targeted and full ESLint passed with zero errors and the existing `qa-thai-font.tsx` warning; workspace type-check passed; after one transient Windows build-worker crash at static-page generation, the unchanged retry completed `307/307` pages; `npm audit --audit-level=high` found zero vulnerabilities; and scoped `git diff --check` passed. Full Vitest from `apps/next` finished at `228/230`; the two failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this chart and unchanged by this batch. Browser/DOM UAT was not run because the implementation request did not explicitly request browser testing.

Cash Flow Analysis profit-to-cash comparison checkpoint on 2026-07-17: the former midpoint diverging bars were replaced with two full-width comparison rows that share one absolute scale, keep each value beside its own label, and add explicit positive/negative badges. A concise semantic banner now states whether profit and operating cash move together or in opposite directions, while the footer shows the absolute gap between them. The paired analysis grids now use content-height cards instead of stretching the shorter panel to the taller neighbor. What is what: this is presentation only; PBT, OCF, the gap formula, filters, source facts, exports, permissions, and API payloads are unchanged. Why it has to be like this: the midpoint chart gave each sign only half the track, made the smaller value nearly disappear, separated negative bars from their values, and left large blank card areas that slowed comparison.
Validation for the comparison redesign: the new red/green shared-scale regression and the focused finance report suite passed `14/14`; targeted and full ESLint passed with zero errors and the existing `qa-thai-font.tsx` warning; workspace type-check and scoped `git diff --check` passed; and the 4 GB production build generated `307/307` static pages. Full Vitest finished at `227/229`; the only failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this page and unchanged by this batch. Browser visual UAT was not run because the session had no available browser binding.

Cash Flow Analysis disclosure and mobile-filter accessibility checkpoint on 2026-07-17: `/finance-accounting/cash-flow-analysis` now starts its working flow with the filtered KPI overview, followed by a collapsed `ดูเกณฑ์และข้อจำกัด` disclosure instead of a 598px mobile notice that pushed every KPI below the first viewport. The report basis, projection assumptions and limitations now use Thai business language without exposing database table/field names; mobile and desktop both use `ส่งออก Excel`, and desktop/mobile date controls share the standard `h-9` height. The shared `MobileFilterSheet` now renders a labelled modal dialog, keeps its footer actions as the single visible exit path, closes with `Escape`, traps `Tab` focus, prevents background scroll/focus and restores focus to its trigger. What is what: the disclosure preserves the management-report and forecast limitations required by the finance flow, while the KPI and analysis panels remain the primary decision surface. Why it has to be like this: finance transparency must remain available without turning implementation details into customer copy or hiding the first useful numbers below a full-screen explanation. Final validation passed: the final scoped Vitest suite finished `38/38`, targeted ESLint and full ESLint passed with zero errors and one existing `qa-thai-font.tsx` warning, workspace type-check passed, and the production build compiled, type-checked, and generated `307/307` static pages. Full Vitest finished at `226/228`; the two remaining failures are the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this design batch. Final mobile browser QA confirmed the KPI in the first viewport, the disclosure collapsed by default, 36px date controls, one footer exit path, working focus trap/Escape restoration, zero overflow, and zero console, request, page, or HTTP errors.

Financial Dashboard laptop KPI breakpoint checkpoint on 2026-07-17: the one-layer six-KPI overview now keeps three columns throughout `lg` and `xl`, then expands to six columns only at `2xl`; the loading skeleton uses the same breakpoint contract. What is what: this is a responsive presentation correction only and does not change KPI values, formulas, filters, permissions, drilldowns, or database state. Why it has to be like this: at the former `xl:grid-cols-6` threshold, a 1280px viewport had only 1,153px of content width after the Sidebar, leaving each KPI card 179px wide and forcing financial values into 2-3 line fragments. The corrected runtime renders 374px-wide, 112px-high cards with every KPI value on one line at `1280x720`; mobile `390x844` remains a two-column grid with 167px-wide cards, one-line values, the compact `ตัวกรอง` action, and no document overflow.
Validation for the KPI breakpoint correction: a red/green Financial Dashboard design-contract assertion failed before the class change and the focused suite then passed `16/16`; targeted ESLint passed; workspace type-check passed; full lint finished with 0 errors and the existing `qa-thai-font.tsx` warning; the production build generated all 307 static pages; and live browser verification at `1280x720` plus `390x844` reported no console warning/error or document-level horizontal overflow. Full Vitest finished at `221/223`; the two failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this KPI breakpoint correction.

P&L / Cash Flow Analysis formula-and-scope correction checkpoint on 2026-07-17: `/finance-accounting/pl-statement` now owns one management PBT calculation from operational facts: sales revenue after header discount and before VAT, WAC COGS, approved/paid expense amount, posted non-reversed depreciation, account-scoped loan interest, realized FX only when branch-unrestricted, and approved non-reversed asset-disposal gain/loss. Cash Flow Analysis consumes that PBT and now calculates OCF from active Customer Receipt `net_cash_in`, PMT `net_amount` classified by approval source, and account-scoped loan-payment interest; an Expense PMT and duplicated fee fields are counted only once. What is what: PBT is an accrual management calculation, OCF is a cash-document calculation, 7/30-day projection is a conservative scenario, and related-report links are navigation rather than transfer of formula ownership. Why it has to be like this: gross legacy receipt rows, duplicated PMT fee storage, and subtracting both PMT and `expenses.paid_at` overstated or understated cash conversion and could make a professional finance surface internally inconsistent.

The same checkpoint applies one effective finance branch scope to P&L, Cash Analysis, Forecast Calendar and the Financial Dashboard. Admin/Owner and roles explicitly marked `branch_scope=all` remain unrestricted; `own/custom` roles intersect explicit branch mappings and fail closed to an empty scope when no mapping exists. Forbidden branch requests return `403`; JSON, branch options, projections and XLSX share the same scope. Financial Dashboard now excludes nominal FCD units from THB total assets/equity, uses the shared THB cash-position builder, and omits company-level Loan/Equity figures when a branch-constrained result cannot prove their branch. Forecast Calendar now validates strict `YYYY-MM-DD` and 7/30/90 horizons, preserves URL scope on the first request, renders API projection assumptions/limitations, and does not classify missing/non-finite balances as healthy or generate invalid SVG geometry. Current evidence after final review: focused finance Vitest passed `127/127` across 17 files; workspace type-check passed; full lint finished with 0 errors and the existing `qa-thai-font.tsx` warning; the production build generated 307 static pages; `npm audit --audit-level=high` found 0 vulnerabilities; and the full local HTTPS/login/RSC guard passed 213/213 checks across 106 navigation routes with zero login, SSL, RSC, or HTTP failures. Full Vitest finished at `221/223`; the two failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this finance batch. Browser QA passed the main `80/80` checks plus a final `37/37` narrow scope/overflow/network smoke; the latter artifact is `finance-browser-qa/finance-narrow-smoke.json` in the session visualization directory.

Final finance review hardening on 2026-07-17: Asset Overview now applies the caller's effective branch scope to both its Financial Dashboard and Cash & Others sources, fails closed for empty `own/custom` mappings, returns `403` outside scope, and returns a client error for unknown/inactive branches instead of falling back or surfacing a 500. AR/AP aging compares Bangkok business dates rather than runtime instants, so 00:00–06:59 Bangkok no longer lags one day. Dashboard navigation now forwards the exact month/as-of/branch to P&L and Cash Analysis, forwards as-of outstanding scope without a false month-start lower bound to AR/AP, and hydrates Asset Overview before its first request. Cash Position, Stock Finance and Balance Sheet links are intentionally non-clickable on this Dashboard until those destination APIs gain equivalent branch/as-of authorization contracts. The Cash Flow Statement mobile `ปีนี้` shortcut now formats local 1 January without UTC ISO conversion. What is what: these are authorization, date-boundary and navigation-contract corrections; they do not change the PBT/OCF formulas or write business data. Why it has to be like this: a professional finance surface must not expand scope during drilldown, expose global aggregates through a related route, omit older open AR/AP, or move a Bangkok date into the previous calendar day.

Shared Modal header palette checkpoint on 2026-07-17: the shared `DialogHeader`, `DialogTitle`, and `DialogDescription` now use the same neutral slate palette as runtime table headers (`slate-100` background, `slate-200` divider, `slate-700` header text, and `slate-800` title) instead of the Sidebar's `slate-900` surface. Important shared classes keep older page-local `bg-slate-900` / `text-white` header classes from restoring the former dark appearance, while Dark mode follows the semantic table-header tokens. What is what: this changes shared Modal/Dialog header presentation only; modal content, actions, forms, validation, APIs, permissions, and business data are unchanged. Why it has to be like this: the Modal must remain visually distinct from the persistent Sidebar while matching the established table hierarchy across the application.
Validation for the shared header change: the new legacy-override regression test plus the existing Financial Dashboard suite passed `15/15`; targeted and full ESLint passed with zero errors and the existing `qa-thai-font.tsx` warning; workspace type-check and scoped `git diff --check` passed. The production build compiled successfully, then its integrated type-check stopped on a pre-existing modified `cash-flow-analysis/route.ts` implicit-`any` error outside this batch; that unrelated file was left untouched.

Financial Dashboard instant P&L tooltip checkpoint on 2026-07-17: the six-month grouped bar chart now opens one application-owned month tooltip in the first rendered frame, with exact full values for revenue, COGS, and profit before tax. Desktop tooltips are anchored beside the month hit area instead of under the pointer and keep an approximately `11.5px` positive gap from the active group; on narrow screens the tooltip becomes an in-flow panel `8px` below the chart so it cannot cover the bars. Mouse hover, keyboard focus, and first mobile tap open the same content; `Escape`, leaving the desktop chart, or tapping a non-group chart area dismisses it. The delayed browser-owned SVG `<title>` was removed to prevent a second native popup, and the month hit area uses a visually imperceptible `0.001` fill opacity so its pre-hover geometry remains stable without a light overlay in dark mode. What is what: this is presentation/accessibility behavior only; P&L values, formulas, source queries, filters, permissions, and drilldown behavior are unchanged. Why it has to be like this: users need exact numbers immediately, but the information panel must not hide the bars they are comparing.
Live browser QA passed all six desktop month groups and mobile `390x844`: tooltip DOM mutation occurred in `5.09–7.59ms`, all values matched the payload, one tooltip stayed inside its frame/card, active-group intersection was zero, hover-to-tooltip persistence and document-level `Escape` passed, mobile tap/keyboard/dismissal passed, and console/page/request/HTTP errors were zero. Evidence: `reports/playwright/financial-dashboard-chart-professional/custom-tooltip-final-v3-metrics.json` and the `custom-tooltip-final-v3-*.png` captures.
Validation after the tooltip change: focused Vitest passed `25/25`; targeted ESLint and workspace type-check passed; full lint finished with 0 errors and one existing warning in `scripts/qa-thai-font.tsx`; the production build generated all 307 static pages; `npm audit --audit-level=high` found 0 vulnerabilities; and scoped `git diff --check` passed. Full Vitest finished with `116/118` passing; the two failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this chart and unchanged by this batch.

Financial Dashboard empty-month tooltip follow-up on 2026-07-17: a month whose Revenue, COGS and PBT contain no finite non-zero value no longer receives the full-height invisible hit rectangle, pointer/click/focus handlers or keyboard tab stop. Moving the pointer from a real bar group into blank chart space also dismisses the current tooltip, while months with at least one visible positive or negative bar keep the immediate hover/focus/tap behavior. What is what: zero remains valid report data and stays in the accessible chart description, but an all-zero month has no visible geometry to inspect. Why it has to be like this: showing a `0.00` tooltip over empty space implies that the user selected a plotted mark when no bar exists. The user-provided screenshot was the visual reproduction; no new browser UAT was requested. Red/green regression coverage passed, the Financial Dashboard suite passed `16/16`, focused finance passed `127/127`, targeted ESLint and workspace type-check passed, the production build generated 307 pages, and full Vitest finished `221/223` with only the same two pre-existing shared-table failures.

Financial Dashboard P&L chart geometry follow-up on 2026-07-17: the six-month chart no longer uses a fixed `600px` SVG coordinate box inside a wide responsive panel. All six month groups now span the plot with percentage-based positions, bars use a responsive percentage width, five grid lines have five Y-axis labels, and the Y-domain includes zero while following the actual finite minimum/maximum so positive-only data uses the full vertical plot instead of reserving an empty negative half. Mixed positive/negative data keeps a proportional zero baseline, non-finite series values are omitted instead of producing invalid SVG geometry, and sub-baht values are not compressed by an artificial one-baht minimum range. Narrow screens retain a keyboard-focusable, labelled `720px` minimum chart width with local horizontal scrolling. What is what: this changes only chart geometry, robustness, accessibility, and presentation; P&L values, source queries, formulas, permissions, filters, and drilldown behavior are unchanged. Why it has to be like this: the former fixed view box used only about half of the available card width and a symmetric scale left half the plot empty even when every visible value was positive, making valid financial data look sparse and unfinished.
Live visual QA at `1920x1080` measured a `1190x338.7px` card, `1148x210px` SVG, and `1044.7x159.1px` plot (`90.8%` of content width), with five grid lines/five Y labels and average visible bar width `25.3px`. There was no document overflow, console/page error, failed request, or HTTP error response. Evidence: `reports/playwright/financial-dashboard-chart-professional/desktop-1920x1080.png`, `pl-chart-card.png`, and `metrics.json`.
Mobile live QA at `390x844` measured a `309px` scroll viewport over the `720px` chart. The labelled region accepted keyboard focus, `ArrowRight` moved it from `0` to `40px`, repeated navigation reached the `411px` maximum, and the final month remained visible without document-level horizontal overflow or console/page/request/HTTP errors. Evidence: the `mobile-*.png` captures and `mobile-metrics.json` in the same report directory.

Financial Dashboard decision-hierarchy and data-truthfulness checkpoint on 2026-07-17: `/finance-accounting/financial-dashboard` now starts with one compact desktop as-of/branch scope card and a mobile summary plus `MobileFilterSheet`, followed by a one-layer six-KPI overview, P&L and principal-asset analysis, one server-owned 7/30-day cash projection, non-duplicated management summaries, and source-report insight navigation.
What is what: this is a read-only presentation/read-model correction; existing P&L, balance-sheet, cash-analysis, working-capital, stock-finance, permission, and transaction sources remain unchanged. THB cash/projections include only cash and bank accounts. FCD uses `accounts.opening_balance` only because current `bank_statement` rows do not contain foreign amount fields; known currencies are aggregated only within the same currency, an account with no currency is kept as its own labelled row, no cross-currency scalar is returned, and the UI labels these values as starting balances outside THB totals/projections until a foreign-movement and FX policy exists. THB cash/bank/OD balances use bounded database `groupBy` movement totals plus each account opening balance rather than truncating or loading the statement ledger into application memory.
The Bangkok business date is normalized before month calculations and end-of-day cutoffs, failed filter reloads cannot display prior figures under the new scope, missing/non-finite money, percentage, OD, or projection values show `ไม่มีข้อมูล` rather than numeric zero or a positive status, non-positive cash bases report stock/cash as not calculable, OD usage stays visible even when no limit is configured, and a real zero projection is labelled `สมดุล`.
Why it has to be like this: a financial dashboard must not mix currencies, infer foreign units from THB movements, relabel stale data, claim snapshot completeness it cannot prove, hide actual overdraft use, or repeat the same values until risks are obscured. Regression coverage locks Bangkok cutoff and month boundary, FCD separation/source boundary, statement aggregation, insight edge cases, stale-filter failure, server projection, missing/zero states, OD display, mobile filter, and accessibility.
Final validation: focused Vitest passed 25/25; targeted ESLint passed; workspace type-check passed with a 4 GB Node heap; full lint finished with 0 errors and one existing warning in `scripts/qa-thai-font.tsx`; production build completed all 307 static pages; `npm audit --audit-level=high` found 0 vulnerabilities; and scoped `git diff --check` passed. Full Vitest finished with 116/118 passing; the two failures remain the pre-existing shared-table contracts in `resizable-table-width-contract.test.ts` and `ResizableTableHead.test.tsx`, outside this page and unchanged by this batch.
Chart-focused browser QA was run after the user challenged the rendered design; the measured result is recorded above. A later scoped navigation smoke covered Cash Analysis, Cash Flow Statement, AR, AP and Forecast. Supported Dashboard destinations now hydrate the source scope before their first request: P&L and Cash Analysis receive month-start/as-of/branch, AR/AP receive as-of with no artificial lower bound, and Asset Overview receives as-of/branch. Cash Position, Stock Finance and Balance Sheet remain displayed as information but are not linked until their destination authorization/filter contracts can preserve the same scope; WTO keeps its existing `type` navigation.

PB/SB LINE product-summary checkpoint on 2026-07-17: the shared Purchase Bill and Sales Bill Flex now shows at most the first three product rows. When the bill contains more than three products, the fourth visible row is `และสินค้าอื่นๆ อีก N รายการ`, where `N` is the number of remaining products; a bill with exactly three products has no summary row. What is what: this changes only the product-list presentation inside the existing PB/SB Flex and keeps all document totals, settlement state, routing, target groups, queue/retry behavior, and save transactions unchanged. Why it has to be like this: long product lists made the financial snapshot unnecessarily tall, while the first three products plus an explicit remaining count communicate the hidden list size without overwhelming the LINE card. A red/green renderer test covers six products (`อีก 3 รายการ`), four products (`อีก 1 รายการ`), and the exactly-three boundary. Live verification used real four-product Sales Bill `SB2606-0008`. Initial job `181` correctly exposed that LINE rejects the summary row's unused right-side `text: ''`; that empty node was removed and a no-empty-text regression assertion was added. Retry job `182` then reached `sent` in `Peach, NAMPEC Official, Meng Watcharathat🤖` on attempt 1 with LINE HTTP 200 and a request ID. Validation passed for the changed scope: the focused renderer test and file ESLint, full ESLint with 0 errors and one unrelated existing warning, 4 GB TypeScript type-check, production build with all 307 static pages, and scoped `git diff --check`. The full Vitest run finished with 110/112 passing; the two failures are existing table-contract tests for committed `globals.css`/`ResizableTableHead` behavior, whose files are clean and outside this LINE change.

PB/SB edit-save LINE checkpoint on 2026-07-17: the regular Purchase Bill and Sales Bill `PATCH` flows now enqueue a fresh LINE notification only after their database transactions commit. What is what: create still uses duplicate protection, while edit uses `force: true` because the same document/target already has a prior `sent` job; LINE or routing failures remain outside the business transaction and cannot roll back a valid edit. The shared Flex title is now the neutral `🛒 บิลซื้อ` / `🧾 บิลขาย` so an edited current-state snapshot is not mislabeled as a new bill. Why it has to be like this: both edit handlers previously returned immediately after commit, so clicking edit and save updated ERP data but never created a LINE job. A red/green contract test now locks both call sites. Live dev-target verification sent real PB `PB012607-0013` through job `179` to `ทดสอบ` and real SB `SB2607-0011` through job `180` to `Peach, NAMPEC Official, Meng Watcharathat🤖`; both completed on attempt 1 with LINE HTTP 200 and a LINE request ID. Validation passed: 5/5 targeted tests, ESLint with 0 errors and one unrelated existing warning in `scripts/qa-thai-font.tsx`, TypeScript with a 4 GB Node heap after the default 2 GB process ran out of memory, production build with all 307 static pages, `git diff --check`, and local `/api/health` HTTP 200.

Six-card LINE group preview checkpoint on 2026-07-16: WTI, WTO, PB, SB, PMT, and RCP were sent to the same temporary LINE group shown as five participants in LINE (four user members from the LINE API plus the Official Account bot). PB job `173`, SB job `174`, PMT job `175`, and RCP job `176` completed on attempt 1 with LINE HTTP 200. Initial WTI/WTO jobs `171` and `172` exposed a graceful-degradation defect: when PDF/album generation failed, photo-bubble actions received an empty URI and LINE rejected the entire Flex. The photo action now falls back to the valid ERP detail URL; retry jobs WTI `177` and WTO `178` then completed on attempt 1 with LINE HTTP 200 and request IDs. What is what: the fallback changes only the photo tap destination when no PDF/album URL exists. Why it has to be like this: a PDF outage must not block the operational Flex notification. Remaining fact: local PDF generation still reports `Cannot read properties of undefined (reading 'S')`, so these WTI/WTO previews were delivered without a PDF attachment.

Compact PMT/RCP LINE Flex checkpoint on 2026-07-16: Supplier Payment now uses the same dark-header, light-body, white-footer, green-tab theme as Customer Receipt. Both cards suppress repeated amounts for a single source document and a single company account, show per-row amounts only when multiple documents/accounts need comparison, omit zero discount/WHT/fee rows and blank notes, and always keep the net cash total as the primary summary. Gross and AR-reduction totals remain visible only when their values differ from the net or receipt amount. What is what: this changes LINE presentation only; PMT/RCP source snapshots, accounting calculations, save transactions, routing, targets, queue/retry behavior, and privacy filtering remain unchanged. Why it has to be like this: the former RCP example repeated `฿53,500` across allocation, receiving account, and summary rows even though each represented the same single-item value, which obscured the decision-making total. All six operational Flex headers now carry a distinct document emoji: WTI `📥`, WTO `📤`, PB `🛒`, SB `🧾`, PMT `💸`, and RCP `💰`.

PB/SB LINE Flex readability checkpoint on 2026-07-16: Purchase Bill and Sales Bill notifications now use the wider LINE `mega` bubble and a white high-contrast body while preserving the existing dark header, near-black footer, cyan accent, status behavior, data fields, five-item cap, and detail link. What is what: only the shared PB/SB Flex presentation changed; PMT, RCP, WTI/WTO, routing rules, target groups, job/retry behavior, and save transactions are unchanged. Why it has to be like this: the customer-supplied LINE reference uses a wider card, and a white body makes dense party/product/amount rows easier to scan while the unchanged header/footer retain the established document identity. Live dev-target verification then resent `PB012607-0013` through job `169` to `ทดสอบ` and `SB2607-0011` through job `170` to `Peach, NAMPEC Official, Meng Watcharathat🤖`; both completed on attempt 1 with LINE HTTP 200 and a LINE request ID.

Design mockup removal checkpoint on 2026-07-16: `/daily/design-mockup` and its unused `DesignMockupPageClient` implementation were removed from the active Next app, and the route was removed from the active menu/page-flow catalogs and proof tracker. What is what: this was an internal static UI comparison playground, not a business transaction flow. Why it has to be like this: shared production UI rules now live in `docs/design.md` and shared components, so keeping a separate routed gallery created a visible exception that could drift from the real system without adding business value. No API, permission, database, stock, payment, accounting, or document behavior changed.

Dropdown design standardization checkpoint on 2026-07-16: the active Next app no longer renders page-owned native `<select>` controls; the audited 199 occurrences across 41 files were migrated to the shared custom dropdown family. What is what: short fixed choices now use the shared `Select` popup, searchable master/document choices use `SearchCombobox`, branch choices use the shared non-searchable branch dropdown, status list filters use the canonical segmented buttons, and pagination keeps `PageSizeDropdown`. Purchase/sales PO references in `/purchase/bills` and role/department/user selectors in Admin are searchable while preserving their empty/default values and existing update handlers. Filter triggers use `h-9`, form/workflow triggers use `h-10`, and the shared controls now use the same compact panel rows, neutral focus treatment, dark-mode selected surface, keyboard navigation, and error/disabled states. Why it has to be like this: a native browser select owns its popup panel and could not keep option spacing, focus/selected states, or dark-mode contrast consistent across browsers; centralizing the visual layer preserves each page's existing values, `onChange` side effects, validation, numeric casts, dependent resets, API payloads, permissions, schema, and database behavior while making the rendered dropdown family predictable. Regression coverage now includes controlled and empty selections, native-style uncontrolled first-option selection, options loaded after mount, required placeholders, optgroups, disabled/error states, keyboard typeahead, read-only focus, arrow navigation, Enter/Escape, and ARIA relationships; targeted tests, source lint, full type-check, and production build pass.

Advance payment modal checkpoint on 2026-07-15: Supplier `ADV` create/edit and Customer `CADV` create now open as modal forms from `/purchase/advance-payments` instead of replacing the register surface. What is what: the page remains a document register for both tabs, while the modal is the temporary editing surface for the selected source document. Why it has to be like this: users compare/search/filter advance documents from the list, so creating or editing one document should not remove the surrounding register context; the modal also keeps ADV and CADV visually aligned while preserving separate business APIs and validation.

## Active Objective

ทำ reference master cache ให้เป็น execution ที่ชัดเจนและย้าย consumer จริงทีละ batch โดยไม่ cache business fact:

- read path ใช้ `short-lived server cache -> Redis -> DB`
- DB ยังคงเป็น source of truth
- write path invalidate key ที่เกี่ยวข้องหลัง DB สำเร็จ
- ไม่ใช้ runtime fallback เพื่อกลบข้อมูลหรือ contract ที่ผิด
- browser memory cache เปิดเฉพาะ branch, warehouse และ L1 global lookup ที่ allowlist แล้ว; API runtime ยังคง `private, no-store`
- รายงาน, document detail, stock, balance, price, cost, WAC, permission และ transaction ยังคงอ่าน source ปัจจุบัน

## Canonical References

- cache design + completed history: `docs/notes/Reference Master Cache Flow.md`
- execution tracker: `docs/migration/09-implementation-tasklist.md`
- design baseline: `docs/design.md`
- environment/deploy target: `docs/migration/10-environment-status.md`

## Completed Scope In This Working Tree

- shared Redis/server cache service และ invalidation foundation
- active and historical readers สำหรับ branch, warehouse, customer, supplier, account และ lookup masters ที่ระบุใน cache flow
- master write routes ที่เกี่ยวข้อง invalidate cache key แล้ว
- branch/warehouse master GET ใช้ full master contract (active + inactive) แล้ว
- consumer option/filter/read-only หลายชุดถูกย้ายแล้ว; รายละเอียดอยู่ใน cache flow เพื่อไม่ให้ซ้ำที่นี่

## Latest Completed Batch: CACHE-P1 Product Reference Contract

## Latest Completed Batch: CACHE-P2 Sales Bill Payload Boundary

- `/api/sales/bills` now returns only list rows/totals; reference options moved to `/api/sales/bills/options?scope=reference`.
- Browser memory cache is user-scoped with 5-minute TTL for branch/customer/product/sales-channel/warehouse references only.
- WTO, PO Sell, Trading cost, customer advance, VAT effective rate, usage and remaining balances load fresh from `/api/sales/bills/options` when create/edit opens; no persistent browser cache.
- Validation: workspace type-check and lint passed; lint retains one unrelated warning in `scripts/qa-thai-font.tsx`.

## Latest Completed Batch: CACHE-P3 Weight Ticket Reference Boundary

- WTI/WTO form now deduplicates and caches branch/customer/supplier/impurity/product/thumbnail references in tab memory for 5 minutes, scoped by authenticated user.
- Weight-ticket list branch filter uses the same client reference cache.
- Stock options, WTI/WTO list/detail, transaction writes and attachment uploads remain fresh/no-store; no binary image is stored in browser cache or Redis.
- Validation: workspace type-check and lint passed; lint retains one unrelated warning in `scripts/qa-thai-font.tsx`.

Product option/search มี contract แยกชัดเจน:

1. `ProductReferenceRecord` มีเฉพาะ `id`, `code`, `name`, `unit`, `type`, `metalGroup`, `active`
2. ราคา, ต้นทุน, WAC, stock และ image URL ไม่อยู่ใน product reference cache
3. thumbnail storage key เป็น cache metadata คนละ key และ `/api/daily/weight-tickets/products` เป็นผู้ประกอบ URL เอง
4. product create/update/active-toggle/import ล้าง product option/search/thumbnail keys หลัง DB write สำเร็จ
5. consumer แรกคือ `GET /api/daily/weight-tickets/products`; shape ของ response เดิมยังคงเดิมสำหรับ WTI/WTO form

Validation completed:

- `reference-master-cache.test.ts`: 33 tests passed
- full `npm run type-check --workspace @ns-scrap-erp/next` passed
- `npm run lint --workspace @ns-scrap-erp/next` passed
- `npm run build --workspace @ns-scrap-erp/next` passed (Next.js 16.2.10, 308 routes)
- `git diff --check` passed

## Latest Completed Batch: CACHE-M1 Instrumentation Foundation

- production จะ emit structured cache read/error logs โดย redaction key ที่มี search query, branch code หรือ id
- ไม่เพิ่ม Redis counter write บน hot path; ใช้ Vercel runtime log เพื่อตรวจ hit/miss/error ก่อนตัดสินใจปรับ TTL
- `REFERENCE_CACHE_OBSERVABILITY_ENABLED=false` ปิด production logging ได้ชั่วคราว; local ต้อง opt-in ด้วย `true`
- `reference-master-cache.test.ts`: 33 tests passed รวม telemetry redaction และ full master branch/warehouse contract

## Current Cache Batch Queue

การ audit และ code migration ของ reference cache ปิดแล้วใน CACHE-M1 ถึง CACHE-M4; งานที่เหลือถูกแตกไว้เป็น `CACHE-M5-A` และ `CACHE-M5-B` ใน `docs/migration/09-implementation-tasklist.md`:

1. `CACHE-M5-A` เก็บหลักฐาน runtime จาก SIT/UAT, ตรวจ invalidation, scope isolation, request reduction และ retire key ที่ไม่มีหลักฐานใช้งาน
2. `CACHE-M5-B` ตรวจ image delivery, thumbnail/original loading, Storage/CDN metrics และ cleanup legacy `products.image_names`
3. บันทึกผล TTL, key, image policy และ blocker ใน `docs/notes/Reference Master Cache Flow.md`

สถานะปัจจุบัน: shared readers และ invalidation ของชุดหลักถูกเพิ่มแล้ว; direct reads ที่เหลือถูกแยกเป็น CRUD, import/export, write-time validation, historical inactive label, tax effective-date lookup หรือ transaction/business fact จึงยังคงอ่าน DB ตรงตาม contract.

M5 checkpoint 2026-07-18: เพิ่ม server/client cache duration telemetry และ audit script `apps/next/scripts/audit-product-image-assets.mjs`. Dev/SIT/UAT ตรวจพบสินค้า 236 รายการ: 62 รายการมี original+thumbnail ครบ 124 objects, 174 รายการไม่มีรูปโดยตั้งใจ, และไม่มี missing/orphan asset. Migrations `20260718140000_clear_legacy_product_image_names.sql` และ `20260718143000_drop_legacy_product_image_names.sql` ล้างข้อมูลและ drop legacy column ในทั้งสาม environment หลัง Prisma/schema consumer audit ผ่าน. เพิ่ม `image_delivery` telemetry สำหรับ WTI/WTO attachment load/error โดยไม่ส่ง URL/เลขเอกสาร/scope และถอด placeholder รูป hardcode จาก LINE notification แล้ว. เพิ่ม `audit:weight-ticket-image-assets`; audit dev/SIT/UAT พบไม่มี data URL, invalid URL หรือ missing storage key, แต่ `weight-ticket-pdfs` ยังเป็น public และมี legacy filename-only references จึงยังไม่เปลี่ยนเป็น private.

Validation checkpoint 2026-07-18: targeted cache tests ผ่าน `37/37`; หลัง M5 lint และ workspace type-check ผ่าน. ยังต้องเก็บ runtime logs/telemetry จาก SIT/UAT และทำ browser/DOM UAT เมื่อผู้ใช้สั่ง.

### Explicitly Out Of Scope

- write-time validation ของ stock/WTO/production และ transactional reads ที่ต้องเห็น state ปัจจุบันทันที
- product price/cost/stock/WAC ไม่อยู่ใน reference-cache scope; binary image ไม่เก็บใน Redis หรือ browser memory และติดตาม delivery แยกใน `CACHE-M5-B`
- browser UAT และ deploy: ทำเมื่อ user สั่ง

## Validation Baseline For Next Batch

```bash
npx vitest run --config apps/next/vitest.config.ts apps/next/src/lib/server/reference-master-cache.test.ts
npm run type-check --workspace @ns-scrap-erp/next -- --pretty false
git diff --check -- <batch-files>
```

## Immediate Next Steps

1. เก็บ `CACHE-M5-A` runtime evidence จาก SIT/UAT โดยอ่าน structured logs และตรวจ scope/invalidation.
2. ตั้งค่า/ตรวจการรับ `image_delivery` telemetry จาก deployment และวัด request/bytes/latency/broken-image.
3. ออกแบบ mapping ของ legacy filename-only references และ signed URL สำหรับ WTI/WTO attachments ก่อนเปลี่ยน bucket จาก public เป็น private; ต้องทดสอบ LINE URL lifecycle แยก.
4. อัปเดต TTL/key decision จากหลักฐานจริง; ยังไม่เปิด persistent browser cache.

## Working Tree Boundary

- current uncommitted work is the CACHE-M5 image telemetry and task documentation; push/promote only after validation and explicit user request.
- current branch: `dev` against `new-origin/dev`; follow `docs/agent-rules/git-communication.md` before any mutation.
