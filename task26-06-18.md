# งานปรับปรุงระบบใบชั่งน้ำหนัก (Weight Ticket) วันที่ 18-06-2026

- [x] 1. เพิ่มฟังก์ชัน `calculateAdjustedLineTotals` ใน `WeightTicketsPageClient.tsx` เพื่อคำนวณหักสิ่งเจือปนของเต๋าลูกทั้งหมดจากเต๋าแม่
- [x] 2. ปรับปรุง `ticketToFormState` เพื่อเชื่อม `parentId` ค้นหาเต๋าแม่ย้อนหลังเมื่อโหลดข้อมูลจาก API (เช็คจากน้ำหนักรวมเป็น 0)
- [x] 3. ปรับปรุงฟังก์ชัน `updateLine` เพื่อให้เมื่อมีการเปลี่ยนสินค้า (productId) หรือคลัง (warehouseId) ในเต๋าแม่ จะทำการซิงค์ไปยังเต๋าลูกที่เป็นสิ่งเจือปนโดยอัตโนมัติ
- [x] 4. ปรับปรุงฟังก์ชัน `removeLine` ให้ลบทั้งเต๋าแม่และเต๋าลูกที่เกี่ยวข้องกันทั้งหมด
- [x] 5. ปรับปรุงฟังก์ชัน `addImpurityLine` ให้ผูก `parentId` ไปยังเต๋าแม่ และไม่ย้าย `activeLineId` ไปยังเต๋าลูก
- [x] 6. ปรับปรุง `errors` validation เพื่อยกเว้นการตรวจสอบน้ำหนักรวม/รูปภาพสำหรับเต๋าลูก และตรวจสอบกรณีหักรวมเกินน้ำหนัก Gross โดยระบุความผิดพลาดลงในเต๋าลูกตัวสุดท้าย
- [x] 7. ปรับการคำนวณ `calculateTicketTotals` ใน `weight-tickets.ts` เพื่อประมวลผลสิ่งเจือปนของเต๋าลูกและหักลด Net ของเต๋าแม่
- [x] 8. ปรับการคำนวณ `buildWeightTicketLineRows` ในเซิร์ฟเวอร์ `server/weight-tickets.ts` เพื่อให้สอดคล้องกันในการหักน้ำหนักเต๋าลูกออกจากเต๋าแม่ก่อนบันทึก
- [x] 9. ออกแบบหน้าจอ UI ส่วนหักสิ่งเจือปนของเต๋าด้านขวามือ ให้แสดงเป็นรายการแถว (Row List) ตามรูปภาพกรอบสีแดง โดยมีปุ่ม `+ เพิ่มเต๋าหักสิ่งเจือปน` อยู่ระดับเดียวกับหัวข้อ "สิ่งเจือปน"
- [x] 10. ทดสอบการคอมไพล์ในเครื่องผ่าน `type-check`, `lint` และ `build`
- [x] 11. เพิ่มฟังก์ชัน `addSameProductLot` เพื่อสร้างล็อตใหม่โดยสืบทอดสินค้าและคลังจากล็อตเดิม
- [x] 12. เพิ่มปุ่ม "+ เพิ่มล็อต" เคียงข้างปุ่ม "ลบ" ในส่วนหัวข้อรายละเอียดเต๋า/ล็อต (Active Line) ด้านขวามือ
- [x] 13. ปรับปรุงตรรกะคำนวณ `calculateTicketTotals` และ `buildWeightTicketLineRows` ให้รองรับ Secondary Lot Child (ไม่หักน้ำหนักจาก Parent, มี Net ตัวเอง) และ Impurity Child (หักจาก Parent)
- [x] 14. ปรับปรุง `ticketToFormState` เพื่อเชื่อมโยง parent-child ระหว่างล็อตสินค้าตัวเดียวกัน
- [x] 15. ปรับปรุง UI แผงรายละเอียดด้านขวาใน `WeightTicketsPageClient.tsx` ให้แสดงช่องเลือกสินค้า/คลังแยกด้านบน และลิสต์ของ Lots ทั้งหมดของสินค้านั้นอยู่ด้านล่างในแบบการ์ดย่อย
- [x] 16. ทดสอบการคอมไพล์ในเครื่องผ่าน `type-check`, `lint` และ `build`
- [x] 17. ปรับปรุง Layout แถวสิ่งเจือปนย่อยให้มีหัวคอลัมน์ด้านบน (Table Column Headers) บน Desktop และซ่อน label ย่อยในแต่ละช่องเพื่อป้องกันช่องกรอกโยกขยับเมื่อมีข้อความแจ้งเตือน (Error text)
- [x] 18. เพิ่มป้ายเมนูย่อย "รายการสินค้าสิ่งเจือปน" (`/master-data/impurity-products`) เข้าไปในเมนูหลัก Sidebar (`navigation.ts`)
- [x] 19. สร้างเพจและ Component ใหม่ `impurity-products` เพื่อแยกการจัดการสินค้ากลุ่มนี้โดยเฉพาะ
- [x] 20. อัปเดต `ProductsPageClient.tsx` (สินค้าปกติ) เพื่อกรองซ่อนสินค้าในกลุ่ม "สินค้าสิ่งเจือปน"

- [x] 21. แก้ไขความสูงขั้นต่ำ (min-height) ของส่วนเนื้อหา Dialog "รับซื้อสิ่งเจือปนเป็นสินค้า" เพื่อป้องกันไม่ให้ตัวเลือก Dropdown (SearchCombobox) ถูกตัดขอบล่าง
- [x] 22. รันคำสั่งตรวจสอบ type-check, lint และ build บนเครื่องโลคอล
- [x] 23. ปรับปรุงรายละเอียดในหมายเหตุ (note) ของรายการรับซื้อที่แปลงมาจากสิ่งเจือปน ให้ระบุรหัสสินค้า (Product Code) ของรายการสินค้าแม่ด้วย

- [x] 24. เปลี่ยนสีปุ่ม "บันทึก" (Save) และปุ่มยืนยันหลักทั้งหมดในระบบให้เป็นสีฟ้าสดใส (bg-blue-600 hover:bg-blue-700)
- [x] 25. รันคำสั่งตรวจสอบ type-check, lint และ build บนเครื่องโลคอล
- [x] 26. ทำ Git Commit และ Git Push ขึ้นสาขา peach และ dev ตามคำสั่งของผู้ใช้งาน
